# BLOCKED — ModuleA 2앵커 개선 작업 (작업지시서 §5 기록)

형식: (항목, 질문/애매점, 임시 우회 여부)

## 번호 규약 (2026-08-31 · F-11e-3 정리)

제목은 `## N.`, 본문 참조는 `§N` — **같은 번호를 가리킨다.**

한동안 그러지 않았다. 모듈 F 작업 중 새 항목을 `## §16.` 처럼 § 를 붙여 달아서
옛 `## 16.` 과 번호가 겹쳤고, 본문의 `§16` 이 둘 중 어느 것인지 알 수 없었다
(`17`·`18`·`19` 도 같았다). 겹친 넷 중 **참조가 적은 옛 항목** 을 23~26 으로
옮기고, 저장소가 널리 참조하는 모듈 F 항목(지시서·docs·시험·템플릿)은 번호를
유지했다. 옮긴 것:

    옛 16 통합망 KFP        → 23        옛 18 직류/분류티        → 25
    옛 17 S4 수치 정확도    → 24        옛 19 압력표 표고 기준면 → 26

`core/remote30_full_network.py`(2곳)·`core/remote30_constants.py`(1곳)의
`BLOCKED.md §19` 를 `§26` 으로 고쳤다. 그 셋이 옛 19 를 가리키는 저장소 내
유일한 참조였다.

`6.` 과 `6-1.` 은 서로 다른 항목이라 그대로 뒀다(중복이 아니다).

## 1. W4 — shapely 전제 오류
- **항목**: W4 (HeadRegion)
- **질문**: 작업지시서는 "shapely `Polygon` 기반 — extractor가 이미 shapely 의존"이라 명시하나,
  실제로는 shapely 미설치 + 저장소 전체에 shapely import 0건 (전제가 사실과 다름).
  신규 외부 의존성(shapely)을 추가할지는 배포 환경(anaconda system site-packages) 정책 판단 필요.
- **임시 우회**: 예 — 동일 API(`from_rects`/`from_polygon`/`contains`/`dilate`)의
  순수 파이썬 HeadRegion으로 구현. 추후 shapely 도입 결정 시 내부 구현만 교체 가능.

## 2. 회귀 기준선 — golden 1건이 작업 시작 전부터 불일치
- **항목**: §3 비트동일 회귀 증명 (전 항목 공통)
- **질문**: `tests/characterization` 실행 결과, 작업 시작 시점(HEAD=ae05799)에 이미
  `combined_build__plane_daemyeong` 이 golden과 불일치 (직전 커밋 ae05799 적응형 tol/
  라우팅 penalty의 의도된 변경 + golden 재생성은 "사용자 육안 확인 후"로 보류 중인 상태).
  stale golden 을 기준으로는 비트동일을 증명할 수 없음.
- **임시 우회**: 예 — 작업 시작 시점 HEAD의 실제 산출을 `data/_w_baseline/`(비추적)에
  동결하고, 각 W 항목 후 전 케이스를 이 baseline과 diff 하여 비트동일을 증명.
  golden 파일 자체는 수정하지 않음(사용자 확인 게이트 유지).

## 3. W1.2 — audit 기록 시점
- **항목**: W1 (미도달 헤드 보고)
- **질문**: "파이프라인 후단에서 audit(→W7)에 기록" — audit 객체(ExtractionAudit)는 W7에서
  신설되고, anchored 파이프라인 경로 자체도 W2·W3에서 형성되므로 W1 시점에는 기록할
  장소가 없음.
- **임시 우회**: 예 — W1에서는 계산 함수(`find_unreachable_region_heads`)까지 신설·테스트하고,
  anchored 경로/audit 배선은 W2~W7 진행에 맞춰 완성.

## 4. W3 — 도면 전제 불일치 → 브릿지 양단 W-창 한정
- **항목**: W3 (표적 브릿지)
- **질문**: 지시서 전제("세대별 배관망이 분리돼 있고 가짜 봉합은 브릿지에서만 발생")가
  실측과 부분 불일치 — 대명동 fixture 는 weld 후에도 세대 경계를 넘는 컴포넌트가 존재
  (최대 comp x=245k→284k, comp12 x=252k→262k). 헤드 보유 컴포넌트만 대상으로 좁혀도
  그 컴포넌트의 *동측* 지점에서 봉합되면 동측 우회 경로가 최종망에 유입됨
  (수용기준 FAIL 재현: 노드 (259436, -229271) 포함).
- **임시 우회**: 예 — §0 설계원리("앵커가 봉합 방향을 유도")에 따라 브릿지 양단 후보를
  작업창 W(=convex_hull(head_region ∪ {alarm_xy}) + ANCHOR_W_MARGIN_MM 팽창) 내부로
  한정하는 `within` 파라미터를 `bridge_targeted` 에 추가. 짝짓기 대상(comp(source) ↔
  헤드 보유 컴포넌트)·tol 계단·merge-and-reevaluate 는 지시서 그대로.
  head_region 이 `.pts` 를 노출하지 않으면 W 제한 없이 동작(W4 HeadRegion 이 정식화).
- **후속 (2026-07-29, P1)**: 가짜 봉합의 원인이 브릿지 하나가 아니었음이 확인됨.
  `_weld_dangling_endpoints` 는 노드↔노드만 이을 수 있어, 가지관 끝점이 주배관
  *중간* 에 닿는 실제 T분기를 구조적으로 복원할 수 없었다. 그 끝점들은 전방
  콘 안의 엉뚱한 노드(주배관 반대편 끝, 다른 조각)에 붙어 오접합이 됐다.
  대명동 서측 세대 실측: 그런 후보 47건 중 32건이 오접합, 허위 배관 10.7m.
  해소: `_split_tee_branches`(P1) 를 weld *이전* 에 두어 edge 를 제자리에서
  쪼갠다. 배출망 79.25m → 73.54m, weld 364 → 299, 헤드 25/25 유지.
  잡힌 148건의 갭 중앙값 0.0mm / 최대 0.1mm — 전부 정확한 CAD T분기였다.

## 5. W5 — 음성 유형(SPLINE 등) 판정 전제와 파서 현실 불일치
- **항목**: W5 (공간한정 조건부 재선별)
- **질문**: 지시서는 재선별 범위에서 "SPLINE/ELLIPSE/… 음성 유형 승인 금지"를
  요구하나, 파서(parse_dxf_bundle)는 SPLINE/ELLIPSE 를 flattening 해 "PL" 타입
  dict 로 담으므로 파이프라인 입력(entity dict) 수준에서는 원 dxftype 을 구분할
  수 없음. entity dict 에 원천 태그를 추가하면 골든 characterization 의
  entities_sig(raw dict 해시)가 바뀌어 비-anchored 비트동일 요구를 위반.
- **임시 우회**: 예 — 재선별 헬퍼(collect_spatial_reselect_segments)가 DXF
  원본을 ezdxf 로 직접 스캔해 실제 dxftype 으로 승인/음성 판정. 이 때문에
  anchored 함수에 dxf_path 인자가 추가됨(재선별 발동 시에만 사용). 블록(INSERT)
  내부의 OTHER 선분은 이 스캔 범위 밖 — 저-prior 후보 특성상 수용.

## 6-1. W7 — bridges "layers" 필드는 그래프 수준에서 복원 불가
- **항목**: W7 (ExtractionAudit)
- **질문**: 스키마의 `bridges [{p1, p2, len_mm, layers}]` 중 `layers` — 브릿지는
  그래프(노드 좌표 + edge 길이만 보유) 수준에서 생성되므로 양단이 유래한 원
  entity 의 layer 정보가 이미 소실됨. 노드→layer 역매핑을 만들려면 _build_graph
  변경(골든 위험) 또는 중복 자료구조 신설이 필요.
- **임시 우회**: 예 — `layers: null` 로 스키마 자리만 채워 기록. 추후 필요 시
  _build_graph 에 선택적 node→layer 출력 kwarg 를 추가해 채울 수 있음.

## 7. 유속 수렴 사이징 — 골든 1건 재생성 보류 + 피팅 등가길이 미모델링
- **항목**: 통합 유속 수렴 (`core/hydraulic_solver.py`, V1~V6)
- **질문 1**: 내경 산정 근거가 "수요유량 1패스"에서 "과토출 유량 수렴"으로 바뀌어
  `combined_build__plane_daemyeong` 골든의 `dia`/`velocity_mps` 가 필연적으로 달라짐.
  골든 재생성은 §2 와 같은 이유(사용자 육안 확인 게이트)로 아직 못 함.
- **임시 우회**: 예 — 골든 파일 미수정. 대명동 201동 실측으로 무해함을 확인:
  `changed=0` (기존 내경 그대로 통과), 위반 0건, 과토출 1.27배, 소스압 2.10 bar.
  즉 이 도면에서는 내경이 하나도 안 바뀌고 stamp 값(`velocity_mps`)만 과토출 기준으로
  갱신됨 (max 1.92 → 1.97 m/s).
- **질문 2**: `<Fittings>` 등가길이를 해석에 넣지 않음(equipment 의 명시 `eq_len` 만 반영).
  손실 과소 → 헤드 간 압력차 과소 → 과토출 과소 평가 = **비보수측**.
- **임시 우회**: 예 — 사이징 판정에만 `safety=1.1` 유량 할증으로 상쇄. 피팅 등가길이
  테이블을 해석에 직접 넣는 것은 별도 과제.
- **질문 3**: 극단적으로 얇은 초기망(전 구간 25A 등)은 최원단 0.1 MPa 를 물리적으로
  만족시킬 수 없어 소스압이 폭주(실측 1772 bar)하고 고정점 반복이 발산함.
- **임시 우회**: 예 — 발산 회차에는 유량의 **크기**를 믿지 않고 방향("너무 얇다")만
  써서 한 치수씩만 승급. 대명동 백지설계 기준 최종 내경이 150/200A 일색 → 32~100A
  등급 분포로 개선됨 (승급 1279회 → 316회).
- **질문 4 (domain-slim 포팅)**: domain-slim 에만 있는 `size_combined_bores`
  (역할별 내경: 평면도=규약 유지 / 입상관=단일 균일경 / 기계실=한 단계 굵게)와
  유속 수렴이 같은 값을 두 번 정한다. 두 정책의 우선순위가 지시서에 없음.
- **임시 우회**: 예 — 수렴 루프를 `size_combined_bores` **뒤**에 두고
  `keep_existing=True` 로 실행. never-shrink 라 역할별 정책은 보존되고, 그 결과가
  유속을 못 견딜 때만 승급된다. 대명동 실측 `changed=0` (역할별 배정이 이미
  유속을 만족) — 두 정책이 충돌하지 않음을 확인.

## 8. AV 표고 기준 통일 — 정수압 변화 + 골든 1건 재생성 보류
- **항목**: `stitch_riser_and_heads` (core/remote30_full_network.py)
- **질문 1**: 평면도 추출은 전 노드에 평탄한 상대표고(2.8m)를, 계통도 추출은 수원=0
  기준 절대표고를 준다. 두 추출의 유일한 공통 노드인 AV 에서 이 기준 차이가 그대로
  남아, AV 를 잇는 배관 하나에 가짜 낙차가 실렸다 (대명동 실측: 라이저 AV −78.15m
  vs 헤드망 +2.8m = **80.95m**). 3D 통합 화면에서 계통도 밸브는 아래로, 평면도 밸브는
  위로 갈라져 보였고, 수리해석에는 없는 정수압이 들어갔다.
- **임시 우회**: 아니오 — 정식 조치. 헤드망 노드 전체를 `riser_av_elev − head_av_elev`
  만큼 평행이동해 AV 지점의 z 를 양쪽에서 동일하게 만듦. 배관의 `elev` 는 구간
  낙차(delta)라 기준 이동의 영향을 받지 않아 손대지 않는다. `prepend_machine_room_to_riser`
  가 이미 쓰는 rebase 패턴과 동일. 대명동 재측정: AV(10)·평면도측 이웃(71) 모두 −78.15m,
  헤드평면 145개 노드가 AV 와 같은 표고로 정렬.
- **질문 2**: 헤드망 표고가 바뀌므로 정수압이 달라져 SDF/KFP/HAS 수치 산출이 변한다
  (표시만의 변경이 아님). `combined_build__plane_daemyeong` 골든이 추가로 벌어진다.
- **임시 우회**: 예 — §2·§7 과 같은 이유(사용자 육안 확인 게이트)로 골든 미수정.
  테스트 결과는 조치 전후 동일하게 `1 failed, 80 passed` (실패는 그 1건뿐).

## 9. 기계실 이음매 정합 — 회귀 테스트 공백
- **항목**: `_layout_machine_room_plan` (core/remote30_full_network.py)
- **질문 1**: 기계실 평면 군집을 bbox 중앙 + 고정 gap 으로 펌프 노드 옆에 띄워
  배치해, 입상관 연결점(mK)이 펌프 노드에서 도면 대각의 **18.14%**(고가수조) /
  **14.47%**(펌프가압) 만큼 떨어졌다. mK 는 `prepend_machine_room_to_riser` 가
  라이저 Input 과 병합해 없앤 노드라 그 자리가 곧 펌프 노드여야 한다.
- **임시 우회**: 아니오 — 정식 조치. 변환의 영점을 bbox 중앙이 아니라 mK 의 raw
  좌표로 잡아 `_tf(mK) == pump_xy` 가 되게 함(AV 정합과 같은 패턴). 재측정 결과
  이음매 Δx=0.0, Δy 는 도면상 실제 배관 길이와 일치. 좌표만 바뀌므로 수리결과 불변.
  z 는 조치 불필요 — 고가수조는 `mr_ref_elev = riser_input_elev` 로 이미 동일하고,
  펌프가압의 차이(= `source_drop_below_lowest_m`)는 의도된 흡입 고저차로
  `core/hydraulic_solver.py:311` 이 노드 표고에서 실양정으로 소비한다.
- **질문 2**: 기계실 경로를 태우는 golden/fixture 가 하나도 없어(전 골든이
  `machine_room_labels: []`) 이 경로는 회귀 테스트로 보호되지 않는다. 실측은
  합성 기계실 dict 를 쓴 일회성 프로브로만 했다.
- **임시 우회**: 예 — 기계실 픽 좌표(수원·입상관 연결점)는 사용자 클릭 입력이라
  fixture 화하려면 `data/201동 기계실.dxf` 기준 픽 좌표 확정이 필요. 사용자 지정 대기.

## 10. 평면도 rooted traversal — tee 개수 증가 + 골든 1건 재생성 보류
- **항목**: `build_input_tables` (remote30_prototype.py)
- **조치**: 계통도·기계실처럼 시작노드(AV)에서 말단으로 뻗는 형태로 통일. AV 를 루트로
  BFS 를 한 번 돌려 (a) 노드 라벨을 BFS 순으로, (b) 배관 행 순서를 BFS 순으로,
  (c) 배관 `in`/`out` 을 부모→자식(상류→하류)으로 맞췄다. 기존엔 라벨 숫자 크기순이라
  흐름과 무관했다. meta 에 도달/미도달 노드, 최대 깊이, 분기 노드 수, **트리 밖 edge 수**
  (= "길이 잘못 트인" 후보)를 실었다.
- **질문 1**: 방향이 일관되면서 tee 규칙(`in` 노드 차수 ≥ 3)이 분기점마다
  하류 가지 수만큼 tee 를 내게 되어 대명동 기준 fittings **56 → 70** 으로 늘었다.
  기존 56 은 라벨 숫자 운에 좌우된 값이라 기준선으로 삼기 어렵다.
- **임시 우회**: 아니오 — 결정적 값으로 확정. 좌표 기준 서명 대조 결과
  pipes(144)·nodes(145)·nozzles(30)·equipment(31) 는 **완전 동일**, fittings 만 변경.
  자체 solver 산출(`source_pressure_bar` −5.844, `max_velocity_after` 1.97)도
  변경 전후 **바이트 동일** — 피팅 등가길이는 아직 미모델링(§7)이라 내부 수리결과 불변.
  단 SDF 의 Fittings 섹션은 늘어나므로 **PIPENET 재해석 시 등가길이가 증가**한다.
- **질문 2**: 골든 `combined_build__plane_daemyeong` 은 라벨·행순서 변경으로 계속 불일치.
- **임시 우회**: 예 — §2/§7/§8 과 동일하게 사용자 육안 확인 전까지 재생성 보류.

## 11. L2 — "격자배관은 네 변 모두 부하 ≥ 1" 전제가 L1 부하 정의와 모순
- **항목**: L2 (`prune_by_load`), 유량누적 작업지시서 §0·§2
- **질문**: §0 은 "부하 0인 간선만 자르므로 격자배관은 네 변 모두 부하 ≥ 1이라
  보존된다"고 하지만, L1 의 부하는 §1 이 못박은 대로 **단일 최단경로 트리 위에서만**
  정의된다. 사각 루프에서 최단경로 트리는 항상 변 하나를 비트리로 남기고 그 변의
  부하는 정의상 0 이다. 따라서 L1 출력을 그대로 L2 에 넣으면 **모든 사이클이 반드시
  하나 이상의 부하 0 변을 가져** 루프가 보존되지 않는다. L2.3 의 "잔여 사이클"
  (모든 변이 부하 ≥ 1) 은 L1 출력에서는 발생할 수 없다.
- **임시 우회**: 예 — 지시서 문면대로 구현하고 모순은 여기 기록한다. `prune_by_load`
  는 부하 dict 를 인자로 받으므로 루프를 보존하는 다른 부하 정의(예: 다중경로/유량
  분배 기반)를 넣으면 §0 의 의도대로 동작한다. L1 부하로 실행할 때의 실제 효과는
  **"SPT + 죽은 가지 제거"** 이며, 이것만으로도 `force_spanning_tree` 대비 개선이다
  (도달 가능하지만 헤드에 기여하지 않는 드레인·시험배관·막다른 가지를 제거).
  격자·환상 배관 지원은 §1 이 명시한 범위 밖 — `docs/load_extraction.md`(L6)에
  폴백 조건과 함께 문서화한다.
- **질문 2**: L2.1 은 제거 간선의 "소속 레이어" 기록을 요구하나 `graph`/`edge_len`
  은 좌표 키만 보존해 레이어를 복원할 수 없다(§6-1 과 동일한 제약, §3 자료구조
  변경 금지).
- **임시 우회**: 예 — `edge_layers` 선택 인자를 두어 호출측이 줄 때만 기록하고,
  주지 않으면 레이어 필드를 생략한다.

## 12. L3 — 담당 헤드 수는 통합했으나 "헤드 집합"이 두 곳에서 다르다
- **항목**: L3 (`compute_edge_load` → S430 담당 헤드 수), 유량누적 작업지시서 L3.2
- **실측(대명동 201동 서측 세대, `scripts/_l3_load_vs_s430.py`)**:
  같은 트리(`rooted_traversal` parent_map)·같은 헤드 집합을 주면 현행
  `_subtree_calc` 와 `compute_edge_load` 는 **79개 pipe 전 구간 일치, NFPC 별표1
  최소 호칭경 불일치 0** → L3.1 통합은 수용기준 PASS 로 수행함.
- **질문**: 남은 차이는 *헤드 집합*이다. S430(=별표1 최소 호칭경)은 **선정된 top-K
  헤드**만 세고, L1 을 파이프라인 앞단(L4)에서 쓰면 **승인된 전체 헤드**를 센다.
  대명동은 승인 25 = 선정 25(K=30 미만)라 두 값이 같지만, 헤드가 K 를 넘는 도면에서는
  선정 밖 헤드가 담당 수에서 빠져 **최소 호칭경이 과소 산정될 수 있다.**
  같은 실측의 [B] 대조에서 부하가 달라지는 구간 11/79 가 나왔으나(헤드 좌표 부착
  차이) 호칭경이 바뀌는 구간은 0 이었다.
- **임시 우회**: 예 — L3.1 은 "같은 트리·같은 헤드 집합" 조건에서만 통합했고,
  헤드 집합 자체는 **통합하지 않았다**(지시서 L3.2: 잘못된 통합은 법정 최소
  호칭경을 틀리게 만든다). L4 의 파이프라인 부하는 추출 소속 판정용, S430 의 부하는
  호칭경 산정용으로 분리 유지한다. K < 승인 헤드 수인 도면에서 별표1 적용이
  타당한지는 도메인 판단이 필요 — 사용자 확인 대상.

## 13. O1·O2·O3 직교화/평면화 — §11 격자 보존 모순을 트리 채택으로 종결 + 골든 영향
- **항목**: O1 (루프 금지 게이트), O2 (`orthogonalize_edges`),
  O3 (`planarize_edges`)
- **사용자 지시**: "모든 배관망의 각도는 꼭 90도가 되도록 조치하고, 일단은 루프는
  안생겨도되니까. 루프만 안생기게 조치해" → §11 의 격자 보존 요구는 **철회**되고
  가지식 트리가 정식 채택됐다. `on_residual_cycle` 기본값을 `preserve` →
  `force_tree` 로 뒤집었고, 최단경로 합집합 단계에 union-find 게이트를 넣어
  load_mode 와 무관하게 최종망이 항상 트리다.
- **실측(대명동 201동 서측 세대, `scripts/_ortho_probe.py`)**:
  원시 그래프 비직각 287/856(33.5%) → 병합 후 64.6% → **직교화 후 0/116(0.0%)**.
  독립 사이클 0, 총연장 79248.813mm 불변(오차 0.000000%), 소스·헤드 좌표와
  거리 배열 완전 동일, elbow fitting 34개 보존. 스냅 44노드(최대 17.3mm),
  L자 분해 37간선, 충돌 되돌림 2노드, 미해결 대각 0.
- **부작용(수용)**: 25mm 이하 짧은 세그먼트가 11개 → 13개, 최소 길이 3.65mm →
  1.70mm. 직교화 전에도 mm 급 간선이 있었고 수리적 영향은 무시 가능하다.
- **후속(O3 평면화) — 사용자 판단 필요**: "여전히 루프처럼 보이는 구간이 있어"
  보고로 재측정한 결과, 그래프 사이클은 0인데 **간선이 노드를 관통(T분기 19)·
  서로 교차(X 5)·같은 축선에서 겹침(5)** 하여 화면에 닫힌 영역이 생기고 있었다.
  `planarize_edges` 로 관통·교차점을 절단해 실제 분기로 만들고, 포개진 중복은
  한 줄만 남기고, 생긴 사이클을 소스 기준 SPT 로 자르고, 급수 대상 없는 막다른
  조각을 제거했다. 세 결함 모두 **0**(대명동 서측: 간선 112 · 노드 113 ·
  연결성분 1 · 사이클 0 · 비직각 0 · 헤드 25개 전원 잔존 · source 불변).
  **총연장이 79248.813 → 65406.836mm (−17.5%) 로 줄어든다** — ①② 는 여전히
  길이 보존이지만 ③ 은 중복 17 · 사이클 5 · 막다른 조각 8 간선을 실제로 걷어낸다.
  걷어낸 것이 전부 "포개진 배관·우회 배관·죽은 조각"이라는 판단은 형상 기준이며
  **수리계산 결과가 바뀐다**. 시각 확인 후 사용자 승인 필요.
  사라진 노드를 가리키던 추정-edge(브릿지·용접·head-drop) 기록도 함께 제거된다.
  잔여 미조치: 비인접인데 3.65mm·10.61mm 떨어진 노드쌍 2건(도면 축척상 비가시).
- **골든**: `combined_build__plane_daemyeong` 출력이 바뀐다. 단 이 골든은 **작업
  시작 전부터 이미 불일치**(§2·§7·§8·§10)이므로 신규 회귀가 아니다. 재생성은
  사용자가 5051 에서 대명동/B1F 를 눈으로 확인한 뒤로 보류.
- **미해결**: 비앵커 경로(`select_worst30_heads`)도 직교화가 기본 on 이지만 이
  경로에는 브릿지·용접 audit 좌표 재매핑 대상이 없어 검증 범위 밖이다.

## 14. R1~R6 추정 연결 폐지 — 실배관만으로 판단 + 조각 정직 보고
- **항목**: 사용자 지시 "추정 용접 추정 브릿지 싹 다 없애버려 그냥. 무조건 실배관으로
  판단해. 제대로." (2026-07-30)
- **조치**: 평면도 3개 경로(`select_worst30_heads` / `select_worst30_heads_anchored` /
  `run_stages_0_2` Stage 3)에서 배관↔배관 추정 연결(용접·브리지)을 전부 제거.
  남는 추정은 기호↔배관 head/source drop line 뿐이다(점선·별색 렌더 유지).
  §4 의 `bridge_targeted`·`_weld_dangling_endpoints` 는 평면도에서 더 이상 호출되지
  않는다(계통도 `extract_pipe_network(force_connect=True)` 경로에만 잔존).
- **삭제한 목발을 대체한 것**:
  - **R1 `auto_snap_eps`** — `SNAP_TOL_MM=50mm` 고정값이 실제 이음매 간격보다 작아
    연결 배관이 조각나던 것이 근본 원인이었다. 후보를 훑어 최대 연결성분을 키우는
    값을 채택(총연장 90% 하한). 실측 채택값: 대명동 75 · LH306 130 · B1F 90 ·
    LH지하 160mm.
  - **`_split_tee_branches` 무조건 실행** — 끝점 클러스터는 노드↔노드만 합칠 수
    있는데 실제 가지관은 주배관 *중간* 에서 갈라진다. 새 좌표를 만들지 않으므로
    추정이 아니라 실배관 복원이다. `tee_split` 플래그는 삭제했다.
  - **`attach_source` 순위 교체** — 망이 조각난 채로 남으므로 "최근접 헤드 보유
    컴포넌트"는 알람밸브 옆 헤드 2개짜리 파편을 뽑는다. `(-헤드수, 거리)` 로 정렬
    (`method="head_component_most_heads"`). 대명동 도달 헤드 2/25 → 23/25.
- **잠복 결함 1건 동시 해소(`planarize_edges`)**: 부동소수 오차로 1.25e-8mm 떨어진
  쌍둥이 노드가 남아, 그 사이 미세 간선의 양 끝이 최단경로 트리에서 **서로를 부모로
  지목**했다. 무방향 키로 접으면 1개가 되어 `force_spanning_tree` 가 서브트리를
  붙들고 있던 진짜 간선을 사이클로 오판해 잘랐다(노드 27 · 헤드 4 유실).
  좌표를 축별로 0.01mm(`_PLANAR_MERGE_MM`) 이내끼리 모아 해소. 헤드·소스 좌표는
  대표값으로 고정해 앵커는 안 움직인다.
- **R3 정직 보고**: `ExtractionAudit.fragments`
  (`count`/`attached_nodes`/`detached_nodes`/`detached_len_mm`) 신설. 안 붙은 조각은
  봉합하지 않고 센다. 통합 빌드 결과창에도 "소스 미연결 조각 N점 / Xm, 미도달 헤드
  M개" 경고 줄을 노출.
- **R4 실측(4도면, 비앵커 경로)**: 대명동 eps75 헤드 30/30 · 137.5m /
  LH306 평면도 eps130 헤드 26/26 · 77.5m / LH 지하층 eps160 헤드 30/30 · 538.0m.
- **R5 남은 느슨한 연결 2종 조이기** (사용자 지시 "둘다 조이고 커밋", 2026-07-30).
  R1~R4 로 배관↔배관 추정은 없앴지만 "멀리 있는 것이 이어진다"는 증상의 잔여 두
  경로가 남아 있었다.
  - **head drop 상한 5000 → 300mm** (`HEAD_DROP_MAX_MM` 신설). 종전 상한은 헤드
    기호가 수 m 떨어진 **남의 가지**에 얹히는 것을 허용했다. 결합선은 배관이 아니라
    "이 기호가 어느 배관에 달렸나"의 판정이므로 기호 도시 오차 수준이어야 한다.
    넘는 헤드는 붙이지 않고 `heads.unreachable` 로 보고. `HEAD_BRIDGE_MAX_MM` 는
    `_finalize_selection` 의 공간 격자 셀 크기로만 남는다(질의 상한과 무관).
    대명동 서측: 결합선 25 → 18개, **최대 2548 → 29.4mm**, 부착 헤드 23 → 16,
    미도달 2 → 9.
  - **평면화 X 교차 절단 폐지**. 끝점이 걸치지 않은 직교 교차가 티인지 층이 달라
    스쳐 지나가는 것인지 평면 좌표로는 못 가린다. 추정 티는 남의 가지에 급수를
    얹는 것과 같아 자르지 않고 `ortho.planar.unmarked_crossings` 로 센다(대명동
    서측 4곳, 결과창에 "층 미상 교차 N곳" 경고). 노드 관통 절단은 유지 —
    끝점이 얹혀 있으므로 티가 맞다.
  - 앵커 경로 대명동 서측 최종: 노드 96 · 배관 95 · 조각 68개(미연결 165노드
    27.6m) · 평면화 99477.977 → 98727.977mm(−0.8%, 관통 2 · 중복 2).
  - 불변식 완화: `test_pipeline_is_plane_embedded` 는 관통 0 · 겹침 0 만 요구하고
    교차는 감사값이 덮는지로 검사한다.
- **R6 알람밸브 결합만 완화** (사용자 지시 "알람밸브랑 가장 가까운 노드 및 엣지쪽
  연결은 좀 유하게 처리해줘(그것만)", 2026-07-30). 소스 위치는 사용자가 직접 찍은
  확정 입력이라 추정이 아니고, 여기서 어긋나면 추출이 통째로 죽는다 — 추정 연결
  폐지 원칙의 명시적 예외로 둔다. 두 가지:
  - **접속점에 배관 중간(수선발) 편입** (`_source_attach_points`). 종전에는 그래프
    *노드* 만 후보라 주배관 옆에 선 알람밸브가 먼 끝점으로 끌려갔다. 수선발이
    이기면 그 배관을 두 조각으로 쪼개 붙인다(`source_attach.attached_to =
    pipe_interior`). 쪼갠 조각이 `MIN_PIPE_EDGE_MM` 미만이 되는 수선발은 버린다 —
    그건 끝점 접속이고 노드 후보가 이미 덮는다.
  - **완화 단계(escalation=1)를 최근접 → 최대 연장으로**. B1F 실측에서 알람밸브
    0.3mm 옆의 **5.6m 스텁**이 1.6m 떨어진 **134m 주배관**을 이겨 추출이 6노드로
    끝났다. 헤드 보유 컴포넌트가 하나도 없을 때만 발동하므로 대명동(1순위에서
    결정)은 비트동일 — 노드 96 · 배관 95 · 결합선 18/29.4mm 그대로.
- **미해결(B1F) — 원인 확정**: `B1F …_도면정리_최소.dxf` 는 헤드 115개를 검출하지만
  최종 선정이 0 헤드다. 위 완화로 소스는 134m 주배관에 붙지만 **헤드는 여전히 0**.
  원인은 소스 결합이 아니라 도면 작도 규약이다:
  - 헤드 최근접 노드 거리는 중앙 **149mm** 로 전부 `HEAD_DROP_MAX_MM`(300) 안이다 —
    drop 상한과 무관.
  - 가지관 세로 런이 헤드마다 **300mm 씩 끊겨** 그려져 있다(예 x=203063.6 에서
    y=209398 ↔ y=209698). 그 틈에 헤드 기호(148.6mm 십자)가 들어간다. 끊긴 두
    끝점은 **같은 축 위 동일선상**이지만 300mm 는 `auto_snap_eps` 후보 상한
    (200mm)을 넘어 영원히 합쳐지지 않는다.
  - `_split_tee_branches` 간격 스윕(20→200mm)으로도 안 풀린다: 컴포넌트 478 → 148,
    최대망 160.7 → 207.6m 로 나아지지만 그 안의 헤드는 0 → 1 뿐. T분기 문제가
    아니라 **동일선상 300mm 단절** 문제이기 때문.
  - 남은 선택지는 "같은 축 · 동일선상 · 사이에 헤드 기호" 3조건을 모두 만족하는
    끝점쌍만 잇는 규칙(추정이 아니라 규약 해석)뿐 → **R7 에서 채택**.
- **R7 헤드틈 접속** (사용자 지시 "아까하려고했던 그작업 진행해줘 [3조건 규칙]",
  2026-07-30). `_join_head_gap_endpoints` 신설 —
  `HEAD_GAP_JOIN_MAX_MM=400` · `HEAD_GAP_JOIN_TOL_MM=50`.
  - **왜 추정이 아닌가**: ③(간극 안에 그 선 위 헤드) 이 근거다. ①②만 쓰면 "가까운
    두 끝점"이라 폐지한 추정 용접과 같아진다. B1F 1500mm 이내 끝점쌍 5532 중 ①②
    만족이 85, 그중 ③ 까지 만족이 75 — 10쌍이 ③ 에서 걸러졌다. 축직교 오프셋 실측은
    배관 <1mm · 헤드 0mm 라 값에 둔감하다(1~20mm 구간이 7쌍뿐).
  - **루프 안 만듦**: 이미 이어진 조각끼리는 잇지 않는다(union-find). 접속 뒤 끝점이
    아니게 된 노드는 두 번 붙지 않는다.
  - **B1F 효과**: 접속 75건. 헤드 보유 조각 115 → 41, 최대망 33노드 160.7m →
    90노드 220.0m. 앵커 추출이 **0 → 23 헤드**(간선 56 · 216.4m)로 살아나고
    `source_attach` 도 escalation 1(최대 연장) → **0(헤드 23개 보유 컴포넌트)** 로
    올라섰다.
  - **대명동 비트동일** — 접속 0건(헤드가 런 중간이 아니라 가지 끝에 달린 작도).
    노드 96 · 배관 95 · 결합선 18/29.4mm · 조각 68 그대로.
  - 감사 노출: `ExtractionAudit.head_gap_joins` (`[{a,b,gap_mm}]`), Stage 3 요약
    `head_gap_joins` + 라벨 "헤드틈 접속 N".
- **미해결(B1F) 2차 — 관통 교차**: R7 뒤에도 헤드 92개가 미도달이었다. 남은 40개
  조각은 각 3헤드 · 10노드 · 7.4m 의 세대 가지관이고 주망까지 **500~881mm**(대부분
  662mm) 떨어져 있다. 그 틈을 실측하니 비어 있지 않다 — 가지관 세로선
  `(214363.6, 209698.5)→(214363.6, 212398.5)` 이 주배관 가로선
  `(216620.7, 210360.7)→(214170.7, 210360.7)` 의 **중간을 관통**한다. 끝점이 어느
  쪽에도 얹혀 있지 않은 순수 X 교차라 `_split_tee_branches`(끝점→배관중간)도,
  R5 에서 폐지한 평면화 X 절단도 잡지 않는다. 즉 B1F 는 가지↔주배관 접속을
  "선을 겹쳐 긋는" 규약으로 그린 도면이다 → **R8 에서 채택**.
- **폐기한 가설 — 교차점의 티 기호**: "접속인 교차에는 기호가 있다"를 두 도면에서
  재보니 정확히 거꾸로다. B1F 는 INSERT 가 도면 전체에 5개뿐이라 교차 285곳이
  **전부** 기호에서 1m 초과이고, 대명동은 INSERT 372개에 교차 9곳이 **전부** 기호
  300mm 이내다. 기호 게이트를 걸면 고쳐야 할 도면에서 0건, 손대면 안 되는 도면에서
  9건이 발동한다. 기호는 판별 근거로 못 쓴다.
- **R8 관통 교차 티 복원** (2026-07-30). `_split_crossing_tees` 신설 —
  `CROSS_TEE_AXIS_TOL_MM=5`. R5 의 반론("끝점이 안 걸친 직교 교차가 티인지 층이
  달라 스쳐 지나가는 것인지 평면 좌표로는 못 가린다")을 두 게이트로 답한다.
  - **① 합류만** — 두 간선이 서로 다른 연결요소일 때만 절단한다. 그 교차가 유일한
    연결 경로라 잇지 않으면 한쪽이 영영 고아라는 뜻이고, 절단해도 루프가 **절대**
    안 생긴다. 이미 이어진 배관끼리의 교차는 스쳐 지나감과 구분이 안 되므로 둔다.
    실측이 이 구분을 뒷받침한다: B1F 는 285곳 중 283곳이 합류(도면이 이 교차로만
    연결됨), 대명동은 9곳 중 1곳뿐이고 나머지 8곳을 절단하면 없던 루프가 8개
    생긴다(= 스쳐 지나가는 배관). 전부 절단과 합류만 절단의 B1F 결과는 헤드 68개로
    **동일**하다 — 나머지 267곳은 얻는 게 없이 루프만 만든다.
  - **② 표시 획 배제** — 어느 배관과도 안 이어졌고 길이가 기호 크기
    (`HEAD_GAP_JOIN_MAX_MM`) 이하인 선분은 배관이 아니다. B1F 합류 교차 262곳 중
    244곳이 137~288mm 짜리 고립 선분(LINE 으로 그린 헤드 십자 기호)이고 남는 18곳이
    839mm 이상 실배관 — 분포가 3배 벌어져 값에 둔감하다. 길이 조건을 함께 걸어야
    통째로 한 선분인 긴 주배관을 잘못 배제하지 않는다.
  - 끝점에서 `MIN_PIPE_EDGE_MM` 안의 교차는 제외 — 최소길이 미만 토막 금지이자
    그 영역은 `_split_tee_branches` 담당이다.
  - **B1F 효과**: 절단 18건. 헤드 보유 조각 41 → 26, 최대망 90노드 220.0m →
    261노드 331.0m. 앵커 추출 **23 → 68 헤드**(간선 131 · 311.4m), `source_attach`
    는 escalation 0 유지(`comp_head_count` 23 → 68).
  - **대명동 비트동일** — 절단 1건(11.8m 조각이 주망에 합류)이지만 최종 지오메트리는
    R8 on/off 가 완전히 같다(노드 96 · 배관 95). 미도달 조각이 하나 줄 뿐 최종망
    선택은 안 바뀐다. LH306 · LH지하 는 합류 교차 0곳.
  - 감사 노출: `ExtractionAudit.crossing_tees` (`[{p,h,v}]`), Stage 3 요약
    `crossing_tees` + 라벨 "관통 티 N".
- **R9 겹쳐 그린 중복 선분 제거** (2026-07-30). `_drop_covered_edges` 신설.
  R8 뒤 남은 B1F 미도달 헤드 47개를 추적해 보니 25개 조각 중 15개가 주망까지
  **정확히 300mm**(= 헤드 틈 폭)이었다. 그 틈은 R7 이 이미 아는 유형인데도 안 붙었다.
  원인은 틈이 아니라 **차수 오염**이다 — 2400mm 세로 런의 위쪽 300mm 에 짧은 선분이
  통째로 포개져 그려져 있어, 런의 끝 노드 차수가 1 이 아니라 2 다. R7 은 차수 1 인
  노드만 런의 끝으로 보므로 멀쩡한 접속이 통째로 막혔다. 중복 선분은 배관을 늘리지도
  새로 잇지도 않으므로 지우는 게 맞다.
  - **게이트** — 덮는 간선과 공유하지 않는 끝점이 **모두 차수 1** 일 때만 지운다.
    그 끝점에 다른 배관이 걸려 있으면 지우는 순간 고아가 되는데, 포개진 위치는 덮는
    간선의 노드가 아니라 중간이라 옮겨 붙일 자리가 없다. B1F 는 20건 전부, 대명동은
    111건 중 95건이 이 조건을 만족한다.
  - **B1F 효과**: 삭제 20건(전부 정확히 300mm). 앵커 추출 **68 → 98 헤드**
    (간선 131 → 191 · 311.4m → 392.4m), 미도달 47 → 17, 조각 385 → 365.
    `source_attach` 는 escalation 0 유지(`comp_head_count` 68 → 98).
  - **나머지 3도면 비트동일** — 대명동(삭제 95건, 대부분 87~88mm 기호 획) · LH306
    (2건) · LH지하(0건) 모두 R9 on/off 최종 지오메트리 해시가 같다. 이미 다른 경로로
    이어져 있어 잃을 연결이 없다.
  - 감사 노출: `ExtractionAudit.covered_drops` (`[{a,b,len_mm}]`), Stage 3 요약
    `covered_drops` + 라벨 "중복 선분 N".
- **미해결(B1F) 4차 — 950mm 직교 단절**: R9 뒤 남은 미도달은 헤드 17개 · 조각 5개다.
  전부 조각의 매달린 끝점(차수 1)이 주망 간선의 *중간* 을 향해 직교로 950mm(4곳) ·
  500mm(1곳) 떨어져 있다. 기호 크기(≈150mm)와 헤드 틈(300mm)을 훨씬 넘어 작도 규약
  해석으로는 못 읽고, 이 자리에 이어주는 선이 도면에 아예 없다(다른 레이어에 있을
  가능성 미확인). 거리만 보고 이으면 R2 에서 폐지한 추정 브리지로 되돌아가는 것이라
  보류 — 다음 착수 대상(#46).
  - **종결 (2026-08-27)** — 「모듈 A 가 가지배관을 전혀 인식 못한다」 보고로 다시 팠고,
    **코드가 아니라 파일 문제**로 결론났다. 같은 건물 B1F 가 두 벌인데 성질이 다르다:

    | 끊긴 끝점 | 죽전 지하주차장 | B1F _도면정리(1) | B1F 현장조사 원본 |
    |---|---:|---:|---:|
    | 매달린 끝점 | 393 | **70** | **797** |
    | 600~1500mm 틈 | 47 | **4** | **466** |
    | 800~1000mm 봉우리 | 17 | **1** | **257** |

    정리본과 타현장 주차장 도면은 배관이 실제로 이어져 있다. 현장조사 원본만
    가지관이 주관에 안 닿는다. 이을 근거가 없다는 것도 A 자신의 탐지기로 확인:
    R7 상한 400→1200mm 스윕 **전 구간 0건** · 동일선상 앞쪽 노드 153건 중
    «사이에 헤드» **9건** · 494곳 틈의 접속점에 부속 기호 **3%**(봉우리 0%) ·
    틈 안 도형은 25/40 완전히 빔(나머지는 건축선·헤드기호, 배관 0).
    **조치는 정리본을 쓰는 것이다** — 상수를 올리거나 규칙을 늘리지 않는다.
    조사 도구: `scripts/_probe_gap_dist.py` · `_probe_gap_content.py` ·
    `_probe_r7_gate.py` · `_probe_r7_block.py` · `_probe_tee_symbol.py` ·
    `_probe_gap_zoom.py`. 자세한 경위는 `docs/module_f_lanes.md` §7.
- **R10 헤드 틈 지문 배관 레이어 승격** (사용자 지시 "현장조사 셔터라는 레이어의 헤드
  및 배관 그래프는 인식을 못하는 것 같아", 2026-07-30). `_promote_headgap_pipe_layers`
  신설 — `HEADGAP_PIPE_PROMOTE_MIN=3`.
  - **문제**: 사용자가 올리는 `data/uploads/B1F_.dxf` 에는 실제 가지관 한 줄(연장
    85.6m · 2450/2600mm 조각이 300mm 헤드 틈 간격, `drain` 블록 INSERT 2개)이 통째로
    `현장조사#셔터` 레이어에 그려져 있다. 이름 기반 `_categorize_layer` 가 OTHER 로
    떨어뜨려 `filter_pipenet_only` 가 버리고, 그 런에 달린 **헤드 15개**가 주망에서
    빠졌다. 승격으로 새로 붙는 헤드가 그 런에서 11mm 떨어진 y=210350 에 2750/2900mm
    간격으로 늘어서 있어(= 인정된 가지관 레이어와 동일 간격) 배관임이 확정된다.
  - **왜 키워드를 안 늘리나**: "셔터"를 목록에 넣어도 다음 도면의 다음 오분류를 못
    막는다. `레이어는 이름 아닌 연결성으로 판별` 원칙(§LH 지하 FIRE 레이어 건)대로
    지오메트리로 판정한다. 이름 확장 대안도 실측으로 기각했다 — `6-소화-밸브` 승격은
    순 −2 헤드, `6-소화-H-메인`(옥내소화전 주관, 수리적으로 별개 계통) 은 합치면 안
    되는 망을 통해 +375 헤드를 끌어온다.
  - **지문**: R7 이 쓰는 사실 그대로 — 동일선상 두 조각이 헤드 하나를 사이에 두고
    `HEAD_GAP_JOIN_MAX_MM` 안으로 벌어져 있는가. 배관 런을 헤드마다 끊어 그리고 그
    틈에 기호를 넣는 작도 관습의 흔적이라 배관 아닌 도형이 우연히 만들기 어렵다.
  - **선택도 실측** (B1F 업로드본 39레이어 · B1F 최소 13 · 대명동 · LH306 · LH지하,
    약 140 레이어): 지문이 잡히는 비배관 레이어는 `현장조사#셔터` 하나뿐(15건)이고
    건축(`A-B1`/`A-B2`) · 밸브 · 소화전 · 치수(`DIM`) · `NOTE` · `1-PIT-소화` 는 전부
    0건이다. 문턱 3 은 한 쌍이 어쩌다 맞아떨어지는 우연만 거른다(배관 레이어는 수백~
    수천 건).
  - **B1F 업로드본 효과**: 승격 1건(지문 15). 주망 헤드 **1841 → 1856**, 간선 20339 →
    20380, 주망 연장 7531.2m → 7623.2m, 조각 9458 → 9454. 잃은 헤드 0.
  - **나머지 4도면 비트동일** — 대명동 · LH306 · LH지하 · B1F 최소는 승격 대상이
    **0건**이라 `bundle.layers` 가 손도 안 닿는다(해시 A/B 이전에 구조적으로 불변).
  - **캐시**: 승격은 parse 단계에서 일어나므로 `_PARSE_CACHE_VERSION` 을 1 → 2 로
    올렸다. 안 올리면 디스크 캐시가 옛 분류를 그대로 돌려줘 변경이 조용히 무효화된다.
  - **EXCLUDE 는 안 건드림** — 사용자가 설정 키워드로 명시해 뺀 레이어다.
- **R10b 승격 단위를 레이어 → 연결된 entity 로 축소** (사용자 지적 "교차하는 선 중
  부속류 노드(엘보, 티 등)만 분기하고 나머지는 그냥 무시하는 원칙을 잊은건 아니겠지",
  2026-07-30). `_PARSE_CACHE_VERSION` 2 → 4.
  - **증상**: 오분류 레이어에는 배관만 있는 게 아니다. `현장조사#셔터` 에는 지문이
    잡힌 가지관(y=210361, 조각 15개)과 **별개로** 헤드 0개짜리 41.1m 셔터선
    (`PL#42818`, y=210098 — 가지관 263mm 아래를 나란히 통과)과 500mm 세로선이 같이
    들어 있다. 레이어를 통째로 PIPE 로 올리니 이 셔터선이 그래프에 들어왔고,
    `_split_crossing_tees` 가 그 위를 지나가는 2700mm 입상관마다 티를 만들어 **없는
    부속 5개**를 y=210098 에 찍었다(전체 절단 327건 중 승격 레이어 관련 6건 = 진짜 1
    + 허위 5). 부속 근거 없는 단순 교차에 분기를 만든 것.
  - **조치**: 지문이 잡힌 entity 에서 시작해 **끝점을 공유하며 이어지는 것만**
    (`_NodeIndex` 클러스터 기준) 함께 올린다. 노드를 공유하지 않고 교차만 하는 선은
    무시. 연결 확장을 남겨둔 이유는 오분류 레이어에 배관망 전체가 그려진 경우 헤드
    없는 주관·입상관이 잘려 나가는 걸 막기 위함이다(지문 entity 만 올리면 끊긴다).
  - **부분 승격 표현**: 필터·그래프·레이어 패널이 전부 레이어 키 기반이라, 승격분을
    원 레이어에서 떼어 `"<원이름> (배관 승격)"` 파생 레이어로 옮긴다. 사용자가 패널
    에서 무엇이 올라갔는지 그대로 본다. 원 레이어 `count` 도 같이 줄인다.
  - **B1F 재실측**: 승격 17 entity(지문 15 그대로). 주망 헤드 **1856 유지**(잃은 헤드
    0), 승격분 관련 허위 교차 티 **5 → 0**, 절단 총계 327 → 326. 남은 셔터선을 도로
    넣어보면 헤드는 0개 늘고 간선만 6개·연장 43.3m 가 붙는다 — 순수 허위 접속.
  - (당시 "R8 에 부속 심볼 게이트는 못 단다 — INSERT 가 5개뿐"이라고 적었으나
    **틀렸다**. 기호는 INSERT 가 아니라 배관 레이어의 생 ARC 다 → R8d 참조.)
  - 노출: `ParsedDxfBundle.promoted_layers`
    (`[{layer,pipe_layer,prev_category,headgap_count,entity_count}]`), Stage 1 라벨에
    "헤드 틈 지문 N건으로 X(OTHER) 중 M entity 배관 승격" 추가.
- **R8c 교차 접속에 스프링클러 증거 게이트** (2026-07-30) — **R8d 로 대체됨**.
  당시 "B1F 는 진짜 티에도 부속 기호를 안 그린다(절단 후보 326곳 중 기호 60mm 내
  6곳)"고 판단해 헤드 유무를 대리 증거로 삼았다. 이 판단이 틀렸다. 사용자가
  "도면은 진짜 티에도 기호가 없는 경우는 없다"고 정정했고, 실측으로 확인됐다.
- **R8d 부속 기호 × 역할 게이트** (사용자 원 지시 "교차선 중 부속류 심볼이 그려진
  부분은 분기하고 그외의 교차선 무시", 2026-07-30).
  - **기호를 못 찾았던 이유**: B1F 의 티 기호는 INSERT 도 CIRCLE 도 아니라 **배관
    레이어(`6-소화-가지관`)에 직접 그린 ARC 쌍**이다. 교차점을 중심(거리 0.0)으로
    r=180 짜리 호 두 개가 314~46° / 134~226° 로 마주 본다. 블록 참조가 아니라
    depth 0 생 엔티티이고, `_build_graph` 가 호를 노이즈로 버려 그래프에도 안 남는다.
    INSERT/CIRCLE 만 훑던 기존 탐지로는 원리적으로 안 잡혔다. 배관 레이어 ARC 730개.
  - **판별 근거 실측** — 절단 후보 326곳을 (레이어 × 레이어 × ARC) 로 분류:

    | 레이어 조합 | 기호 있음 | 없음 |
    |---|---:|---:|
    | SP-메인 × 가지관 | 241 | 18 |
    | 가지관 × 가지관 | **0** | **21** |
    | SP-메인 × SP-메인 | 2 | 9 |
    | SP-메인(1차) × 가지관 | 0 | 10 |
    | SP-메인 × SP-메인(1차) | 0 | 11 |
    | 기타(ZONE·측벽·승격셔터) | 0 | 14 |

    주관에서 가지관이 갈라지는 티는 **93%(241/259)** 가 기호를 갖고, 가지관끼리
    교차는 **하나도** 안 갖는다. 후자가 사용자가 지적한 "말단 배관망 꼬임"이다.
    주관끼리는 기호 없이 맞물려 그리므로 기호만으로 게이트를 걸면 붕괴한다(→45헤드).
  - **채택 규칙**: 간선 역할은 레이어 이름이 아니라 지오메트리로 판정한다 —
    `HEAD_DROP_MAX_MM` 안에 헤드가 달린 간선 = 가지관, 아니면 주관.
    - 가지관 × 가지관 → **기호 필수**
    - 그 외 → 기호 / 끝점(`CROSS_TEE_END_TOL_MM` 안에서 배관이 끝남 = 진짜 T) /
      양쪽 다 주관 중 하나면 인정
  - **구현**: `_find_fitting_symbols(pipe_entities, layer_categories)` 신설(배관
    레이어 ARC 중심), `_split_crossing_tees(fitting_pts=...)` 게이트 ③ 교체.
    `fitting_pts=None` 이면 게이트 off. 파이프라인 3개 호출부 배선.
    신규 상수 `CROSS_TEE_SYMBOL_TOL_MM=300.0`, `CROSS_TEE_END_TOL_MM=400.0`.
  - **끝점 반경 무릎**: 200mm 에서 주망헤드 1538, 400mm 에서 1856, 900mm 도 1856.
    400 = `HEAD_GAP_JOIN_MAX_MM` 와 같은 값이며 둘 다 "기호 하나가 들어가는 틈"을
    재는 자다. 기호 반경은 5~800mm 스윕에서 판정 불변(중심이 교차점과 정확히 일치).
  - **B1F 재실측** (하네스·실함수 수치 일치): 절단 326 → 320, 주망헤드 **1856 유지
    (무손실)**, 주망 7579.9 → 7573.6m, 조각 9455 → 9461. 대명동(절단 1, 113헤드,
    348.7m)·LH306 은 게이트 전후 비트동일.
  - **남은 의문**: 기호 없는 SP-메인×가지관 18건 + SP-메인(1차)×가지관 10건은 현재
    끝점 근거로 통과한다. 진짜 탭인지 작도 편차인지는 도면 육안 확인이 필요하다.
- **골든**: `combined_build__plane_daemyeong` 출력이 또 바뀐다. §2·§7·§8·§10·§13 과
  같은 건이며 재생성은 사용자가 5051 에서 눈으로 확인한 뒤로 계속 보류.
- **테스트 변경**: `test_w3_anchored_fixture_excludes_other_units` 의 "최종망 노드
  x<255,000" 상한을 270,000 으로 완화했다. 추정 연결을 없앤 뒤 서측 가지 일부가
  공용 주배관(동측 우회, 실배관)을 타야만 소스에 닿는다 — 경유 노드 6개가 실제로
  존재한다. 범례(x≈288,000) 배제는 그대로 유지.

## 15. 요소 인스펙터 — 헤드 설치방향은 SDF 에 실을 자리가 없음 + 골든 영향
- **항목**: 통합 뷰 요소 인스펙터 (좌클릭 조회 / 우클릭 편집 → 각 포맷 방출)
- **질문**: 사용자 요구 "헤드 노드면 상향식/하향식" 을 PIPENET SDF 로 그대로 내보낼
  방법이 없다. `<Nozzle>` 스키마에 방향 속성이 없고, 비표준 속성을 임의로 넣으면
  PIPENET import 가 깨질 위험이 있다.
- **임시 우회**: 예 — 방향은 (a) CSV/XLSX 의 신설 `Orientation` 열, (b) 화면 헤드
  스텁 방향(위/아래)으로만 나간다. SDF/SLF 수치에는 영향 없음. 표준 확장 자리를
  찾으면 정식 방출로 승격.
- **함께 판단한 범위**: 인스펙터가 노출하는 값은 **실제 출력 영향이 있는 것만** —
  내경→SDF `bore`/KFP/CSV, 길이→전 포맷, C계수→SDF `roughness-or-c`,
  재질명→CSV/XLSX `Type`, 부속 type/count→SDF `<Fitting>`,
  등가길이→SDF `<Equipment equivalent-length>`, 방수량→`<Nozzle>` (L/min÷60000).
- **부속/등가길이는 통째 교체**: 이 두 행에는 안정적 식별자가 없고 사용자가 삭제할
  수 있어야 해서, 편집본이 보낸 목록을 그대로 진실로 삼는다(라벨 패치로는 삭제 표현
  불가). 노즐만 라벨 기준 패치(헤드 노드와 생사를 같이 함).
- **골든**: `combined_build__plane_daemyeong` 이 또 벌어진다 — build 응답 geometry 에
  `nozzles`/`fittings`/`equipment` 키와 배관 `type` 이 추가됐기 때문(수치 변경 아님,
  키 추가). §2·§7·§8·§10·§13·§14 와 같은 건이며 재생성은 계속 보류.
  실측 `1 failed, 138 passed` — 실패는 그 1건뿐.
- **실브라우저 검증**: `scripts/_inspect_e2e_check.py` (대명동 201동, 실서버).
  조회/편집/재출력 왕복 전 항목 PASS, JS 오류 0. 재질 CPVC2 → SDF
  `roughness-or-c="150"`, 부속 추가 → `<Fitting count="2" type="elbow"/>`,
  등가길이 7.5 → `equivalent-length="7.5"` 로 실제 방출됨을 파일 파싱으로 확인.

## 23. 통합망 KFP — 좌표 규약(좌표거리==length_m) 충족 (해소됨 — 기록용)
- **항목**: 평면도+계통도+기계실 통합망의 포맷 충실도 감사
  (`scripts/_format_audit.py`, 대명동 201동 + 옥상수조).
- **고친 것**: `kfp_sdf_converter.sdf_root_to_network` 이 SDF 의 `length` 속성을
  노드 좌표거리로 무조건 덮어쓰고 있었다. PIPENET SDF 의 `Position` 은 스키매틱
  캔버스 좌표(레퍼런스 3-1형: 배관 129m 인데 좌표 span 2650 unit, 우리 emit_sdf 는
  최장변 3000 unit 로 정규화)라 미터가 아니다 → 통합망 KFP/HAS 총연장이
  **327.76m → 137.80m** 로 무너져 있었다(개별 배관 예: 0.845m → 0.058m). 이제
  `length/좌표거리` 중앙값이 1 근처일 때만 좌표거리를 신뢰한다. KFP→SDF→KFP
  왕복은 여전히 무손실(`pipe_length_rmse_m=0.0`, DN 100%).
- **결정 (사용자 채택: (b))**: K-Fire Solver 의 KFP 규약은 **좌표거리 == length_m**
  이다(사용자 수작업 파일 2건 모두 비율 1.0000, n=139/122). 통합망도 그 규약을
  따르도록 `_emit_format_bundle` 의 KFP 방출을 `display_geometry=False` 로 전환했다
  (`routes/r30_combined.py`). 표시 왜곡(솔버 화면에서 통합망 모양이 미리보기와
  달라짐)은 감수한다. HAS 는 length 필드를 직접 읽으므로 그대로 둔다.
- **실측(대명동 201동 + 옥상수조)**: 좌표거리/length_m 중앙값 **0.441 → 0.9999**,
  좌표 총연장 **1071.21m → 290.75m** (실배관장 327.76m).
- **남은 산포**: p10 0.143 / p90 1.414. 평면·라이저·FX 스텁을 x,y 전역 스케일
  **하나**로 맞추는 구조적 한계 — FX 스텁은 실배관장이 기하 오프셋보다 훨씬 길어
  단일 스칼라로 평면 주행과 동시에 만족시킬 수 없다. 배관장 자체(`length_m`)는
  정확하므로 솔버가 필드를 읽는 한 영향 없음.
- **회귀 가드**: `scripts/_format_audit.py` ⑤ 에 `좌표거리==length_m (중앙값)`
  단정을 추가(tol 0.02) — 규약이 다시 깨지면 감사에서 즉시 FAIL.
- **감사 나머지는 전 항목 PASS**: SDF 참조무결성·노드/배관/노즐 무손실·
  `bore`=dia/1000·length·rise·`roughness-or-c`·부속 114건·등가길이 총합 480.90m·
  기계실 10노드 편입·Input 경계압력·단일 연결 컴포넌트, SLF schedule 6종 전부 존재,
  PIPENET ZIP = 동일 stem 의 .sdf+.slf 쌍, HAS round-trip 무손실(RMSE 0.0000m).

## 24. S4 수치 정확도 — SDF `bore` 는 호칭경, HW 계수는 kgf/cm² 계열 (감사 3항목 기각)

### 24-1. 기각한 감사 항목 — "SDF `bore` 를 내경으로 바꿔라"
- **기각 대상**: `pipenet_converter/.../models.py SUPPORTED_DIAMETERS_M`,
  `sdf_writer._build_pipe_element` 의 `bore`, `sprinkler_remote30_extractor.py:990`
  의 `diameter_m` — 세 곳 모두 "호칭경을 내경으로 교체" 지시였다.
- **근거 (레퍼런스 실측)**: 레퍼런스 3-1형 SDF 의 `bore` 는 `0.125` / `0.08` /
  `0.05` / `0.065` 로 **호칭경(m)** 이다. 내경은 SLF 가 담당한다 —
  `assets/2. Pipenet_hand_FX28.slf` 의 `<Size-definition external="Unset"
  internal="130.1" nominal="125"/>`. PIPENET 은 `bore` 를 `nominal` 과 매칭해
  실내경을 결정하므로, `bore` 에 내경(105.3mm)을 넣으면 어떤 `nominal` 과도
  안 맞아 관경이 **"Unset"** 이 된다.
- **대신 고친 것**: 실제로 내경을 방출하던 유일한 지점
  `kfp_sdf_converter.emit_sdf_xml` (`bore = diameter_inner_mm/1000`) → 호칭경.
  파싱측(`bore*1000 → inner_mm`)은 `emit_kfp` 가 `abs(inner_mm - nominal_mm) < 0.5`
  일 때 SLF 에서 실내경을 다시 조회하므로 호칭경 왕복이 오히려 정확해진다.

### 24-2. 기각한 감사 항목 — "HW 마찰손실을 `/0.0980665` 로 고쳐라"
- **한 번 적용했다가 되돌렸다.** 계수를 해석적으로 재도출한 결과가 근거다.
  Hazen-Williams SI 형 `h_f[m] = 10.67·L·Q[m³/s]^1.852/(C^1.852·D[m]^4.87)` 을
  Q[L/min]·d[mm] 로 환산하면 계수는 **6.17e5 (kgf/cm²)** / **6.05e4 (MPa)** 이다.
  코드의 `6.174e4` 는 전자의 1/10 이므로 `/0.1` 을 거쳐야 kgf/cm² 가 된다.
  PIPENET 보고서 헤더도 `Frict. Loss (kg/cm2)` 로 같은 척도다
  (`data/docx_text.txt` FLOW IN PIPES). `/0.0980665` 로 바꾸면 **2% 부풀려진다**.
- **남긴 것**: 중간변수명이 `dp_mpa` 였던 게 오해의 원인이라 이름을 `dp` 로 바꾸고
  두 계수 계열을 주석에 못박았다 (`core/pipenet_validator.py`,
  `sprinkler_remote30_extractor.hw_friction_loss_kgcm2`).

### 24-3. 실제로 고친 단위 오류 2건 (골든 영향 있음)
- **정적수두**: `sprinkler_remote30_extractor.py:816` 이 `static_head_m * 0.0980665`
  로 kgf/cm² 를 만들고 있었다. 0.0980665 는 **m→bar** 계수다. 1 kgf/cm² = 10 m 수두
  이므로 `/ M_H2O_PER_KGFCM2 (=10)` 로 교정 → 정적수두 **+1.97%**.
- **솔버 단위 혼용**: `core/hydraulic_solver.hazen_williams_drop_bar` 는 이름과 달리
  kgf/cm² 를 반환하면서, 같은 식에서 표고손실은 `BAR_PER_M_WATER(=0.0980665)` 로
  진짜 bar 를 더하고 있었다(헤드 K 계수도 bar 기준). 마찰항에
  `KGFCM2_TO_BAR(=0.980665)` 를 적용해 bar 로 통일 → 마찰손실 **−1.94%**.
- **골든 영향**: 위 두 건은 통합망 수리해석 수치를 바꾼다. **골든 재생성은 하지
  않았다** (사용자가 :5051 에서 대명동/B1F 를 눈으로 확인하기 전까지 보류).
  현재 `tests`+`pipenet_converter/tests` 225 passed / 1 failed —
  실패는 작업 전부터 실패하던 `combined_build__plane_daemyeong` (FX equipment
  diff) 한 건뿐이고, 나머지 골든은 전부 불변이다.
- `tests/test_hydraulic_solver.py::test_hazen_williams_matches_validator` 는
  bar 와 kgf/cm² 를 직접 등호로 비교하고 있었다(계수가 둘 다 틀려서 우연히 통과).
  환산 계수를 거치도록 고쳤다.

## 16. [F-10a] 채택 헤드 0 개면 조립이 죽는다 — 기본 기준 0.9 의 대가

**증상(실측).** 정찰 «높음(≥0.9)» 띠가 0 인 도면에서 기본 기준 0.9 로 채택하면
헤드가 0 개 찍히고, 그 스펙으로 조립하면 엔진이 죽는다:

| 도면 | 높음 ≥0.9 | 0.9 채택 헤드 | 조립 |
|---|---:|---:|---|
| LH306동_평면도 | 0 / 42 | 0 | **error — `KeyError: 'heads'`** |
| LH306동_평면도 (기준 0.75) | — | 3 | done |
| 대명동 단위세대 | 87 / 119 | 101 | done |
| B1F 현장조사 | 72 / 3,338 | 2 | done |

재현: `python scripts/_probe_f10_commit.py samples/dxf/LH306동_평면도.dxf`

**이번에 한 것.** F-10a 의 자동 시작 게이트에 「기본 기준으로 찍을 헤드가 있는가」를
넣어 **막고 사유를 적는다**(찍기 화면으로 폴백 · 질문 없음, D-F10-1 규약 그대로).

**막힌 질문 — 구현하지 않았다.**

1. 기준을 프로그램이 **자동으로 낮춰야 하나?** 그러면 사람이 모르는 사이에 낮은
   신뢰도 후보가 산출에 들어간다. 반대로 안 낮추면 그런 도면은 자동 흐름의 이득을
   전혀 못 본다. D-F8-4 는 「기본 0.9」만 정했지 «0 일 때 어떻게 할지» 는 안 정했다.
   §14 의 기존 항목(기본값 0.9 의 적정성)과 한 몸이므로 함께 결정해야 한다.
2. 엔진의 `KeyError: 'heads'` 자체는 **고치지 않았다** — `cad_project_editor_g/`
   는 이번 지시서에서 읽기 전용이다. 빈 스펙은 «막을 것» 이 아니라 «명확히
   거절할 것» 이라고 보면 엔진 쪽 수정이 맞지만, 그 권한이 이 작업엔 없다.
   지금은 F 의 문 앞에서 막는다.

**임시 우회: 있음** — 고급의 「채택 기준」을 0.75 로 낮추고 「이 기준으로 다시
채택」을 누르면 그 도면도 정상으로 흐른다.

**해소 — D-F11-2 (2026-08-28 · F-11a).** 위 질문 1 의 답이 정해졌다: **프로그램이
정하되 반드시 말한다.** 절대 임계 0.9 를 «지배 띠 규칙» 으로 바꿨다
(`recon.dominant_band` · `DOMINANT_BAND_RATIO = 0.5`):

    높음이 후보의 50% 이상   → 높음만          (블록 기반 도면)
    아니고 높음+중간 ≥ 1개   → 높음+중간       (직접 작도 도면)
    그래도 0                → 종전 폴백(찍기 + 사유)

「모르는 사이에」가 걱정이었으므로, 발동한 규칙을 정찰 카드와 손질 진입 배너에
반드시 적는다. 사람이 고급에서 고르면 사람이 이긴다(규칙은 기본값이지 잠금이
아니다). 실측:

| 도면 | 띠 | 규칙 | 결과 |
|---|---|---|---|
| 대명동 | 87/119 (73%) | high | 채택 87 — **종전과 동일**(회귀 없음) · 최불리 30 |
| B1F | 72/3,338 (2%) | high_mid | 채택 3,235 · board 헤드 3,235 · **최불리 30 · 최원 416.85 m** (2개 퇴화 해소) |
| LH306 | 0/42 (0%) | high_mid | 채택 40 · **조립 done** (게이트 미발동) |

질문 2(엔진의 `KeyError: 'heads'`)는 **여전히 열려 있다.** 게이트는 최후 방어로
남긴다 — 규칙조차 0 을 내는 도면에서만 닫힌다.

`DOMINANT_BAND_RATIO` 50% 의 적정성은 GT 4~5장이 생기면(F-13) 재검한다.

## 18. [후속] 부속 판정 불가·등가길이 미해결을 «수작업으로» 채운다

**요청 — 2026-08-28 사용자.** F-10f 가 이 둘을 개수로만 표시했다(엔진이 per-item
기록을 안 남기고, 엔진은 그 지시서에서 읽기 전용이었다). 나중에 사람이 손으로
채울 수 있게 하라는 요청. 참조로 **「모듈 A 에서 클릭식으로 하던 그거」** 를 들었다.

**참조 지점 — 모듈 A 의 «직접 입력» 행 덮어쓰기.**
`templates/remote30_prototype.html` 의 표 편집이 그 방식이다:

    8768  if (e.override_flag) return "직접 입력"
                + (e.override_note ? " — " + e.override_note : "");
    8787  h += _roRow("길이 산정", p.length_locked ? "직접 입력(잠금)"
                                                  : "노드 좌표 3D 거리");

즉 ⑴ 행을 눌러 값을 덮고, ⑵ **덮었다는 사실과 사유를 함께 남기고**, ⑶ 화면이
「직접 입력」이라고 그대로 밝힌다. 자동으로 채운 값과 사람이 넣은 값을 절대
같은 얼굴로 두지 않는 것이 이 방식의 핵심이다.

**해야 할 일(요지).**
- 부속·등가길이 미해결 항목을 «어느 배관인지» 까지 내려야 한다 — 지금은 개수뿐.
  엔진 `fitting.py` 가 `unresolved_kind`/`unresolved_length` 를 세기만 하므로,
  per-item 을 얻으려면 엔진이 그 목록을 내보내야 한다
  (「엔진이 세기만 하고 목록을 안 내보낸다」 — §15 헤드 설치방향과 같은 성격).
- 그 위에 A 방식의 행 덮어쓰기(`override_flag`·`override_note`)를 얹는다.
- 덮어쓴 값은 산출에 들어가므로 **표시 전용이 아니다** — F-10 의 D-F10-7
  (산출물 불변)과 성격이 다르다. 별도 지시서로 다루는 것이 맞다.

**상태: 서버까지 완료(2026-08-28).** 화면 입력 UI 만 남았다.

  · 엔진이 «어느 배관인지» 를 남긴다 — `unresolved_kind_items` ·
    `unresolved_length_items` · `unresolved_pairs`. 세는 그 줄 옆에서 쌓으므로
    개수와 어긋날 수 없다.
  · `build_fittings(..., overrides=…)` / `build_design_tables(...,
    fitting_overrides=…)` — 둘 다 **키워드 추가**라 기존 호출자는 그대로다.
  · `POST/GET /api/module-f/design/fitting-override` — 세션에 저장하고,
    고를 수 있는 부속 종류를 내려준다.
  · 산출물 meta 에 「직접 입력 — 부속 판정 n」·「직접 입력 — 등가길이 n」이
    남는다. 자동이 낸 값과 사람이 넣은 값을 같은 얼굴로 두지 않는다.

  실측(대명동 · K=30): 부속 판정 불가 3 → 직접 입력 1건 뒤 **2**,
  등가길이 미해결 **0 유지**, 호칭경 불변, 채운 배관 밖 부속 표 **동일**
  (`scripts/_probe_override_fills.py`).

  ★안전 성질 — 덮어쓰기 조회는 «판정 불가일 때» 안에서만 일어난다. 규칙이
  낸 값은 안 바뀐다. 시험이 소스로 못 박는다
  (`test_직접_입력은_못_가린_자리에만_쓰인다`).

  ★자유 입력 금지 — 「엘베」처럼 라이브러리에 없는 이름을 넣으면 부속 판정은
  풀리지만 **등가길이가 다시 미해결**이 된다(실측 0→1). 그래서 서버가
  고를 수 있는 목록(직선·45° 엘보·90° 엘보·티)을 내려준다.

**완료(2026-08-28).** 화면까지 붙였다 — 「확인할 것」 항목 바로 아래에 종류를
고르는 칸과 사유 칸이 서고, 「직접 입력 저장」이 저장 뒤 표 확정까지 이어 준다.
채운 자리는 목록에 「직접 입력」 무리로 남고 사유가 함께 보인다.

브라우저 실측(`scripts/_verify_override_ui.py` · 대명동):

    확인할 것 65건 · 부속 자리 3칸 · 종류 none/elbow-45/elbow/tee
    채움 → **미해결 3→2 · 직접 입력 0→1** · 사유 「현장 확인 — 직선이다」 남음
    콘솔 오류 0

곁들여: 「확인할 것」이 처음 생길 때 접힘을 한 번 펴 준다(매번은 아니다 —
사람이 접어 둔 것을 되돌리지 않는다). 그리고 `/design/preview` 가 «아직 확정
안 함» 에 404 대신 200 + `view: null` 을 준다 — `auto/network-view` 가 이미 쓰던
규약이다. 404 는 오류가 아닌데 콘솔에 붉은 줄을 남겨 진짜 오류를 묻었다.

## 19. [F-11c] 관경 덮기 — 역참조 없는 배관은 «가리킬 자리» 가 없다

**항목.** F-11c 「관경 직접 입력」. 덮기 키는 board 노드쌍이어야 한다
(D-F11-4) — 배관 라벨은 BFS 순서로 매겨져 corridor 가 바뀌면 다른 배관을
가리키기 때문이다.

**막힌 자리.** 그런데 배관 중 일부는 board 노드쌍이 **아예 없다.** 헤드
접속관·가지 상승관은 도면에 그려진 선이 아니라 전개가 만든 선이라
`edge_ref` 에 항목이 없다. 그 배관은 이 채널로 못 덮는다.

**한 것.** 조용히 빼지 않는다 — 화면이 「고른 배관 n개 · **못 덮는 것 m개**」로
세어 보이고, 안내가 왜인지 말한다. `decide_bores` 의 도크스트링에도 적었다.

**안 한 것과 이유.** 그 배관들에 안정된 키를 주려면 «전개가 만든 선» 을
사람이 가리킬 수 있는 이름으로 바꿔야 하는데, 그것은 관경 채널이 아니라
전개(restrict)의 문제다. 이 지시서의 엔진 권한은 `decide_bores` ·
`build_design_tables` · `source_counts` 의 keyword 추가뿐이다(§3).

**대안이 있나.** 이 배관들의 관경은 담당 헤드 수로 정해지므로(별표1), 값이
틀렸다면 대개 원인이 «담당 헤드 수» 쪽이다 — 그건 손질에서 고치는 것이 맞다.
즉 지금 막다른 길은 아니되, 관경만 손보고 싶을 때 길이 없는 것은 사실이다.

## 20. [F-11c] G 시험이 웹 모듈 F 와 «찍은 스펙» 파일을 공유한다

**증상.** `cad_project_editor_g/tests/test_design_dialog.py` 의
「많이 빠지면 배관을 이으라고 알린다」가 B1F 에서 실패한다.

    제외 1,511 / 도면 3,235 = 0.467  <  _WARN_EXCLUDED_RATIO 0.5

**둘이 어긋나 있었다.** 시험은 「제외가 0 이 아니면 «이어» 조언이 뜬다」를
단정하는데(`test_design_dialog.py`), 창은 비율이 0.5 이상일 때만 그 문장을
붙인다(`dialog_design_input._render`). 어느 한쪽이 옳고 그른 문제가 아니라
**전제가 처음부터 달랐고**, 지금까지는 B1F 의 비율이 늘 0.5 를 넘어서 안
드러났을 뿐이다.

**왜 지금 드러났나.** F-11a 가 B1F 채택을 72 → 3,235 로 늘렸다. 분모가 커지자
비율이 0.5 아래로 내려갔다 — **개선이 잠재 결함을 노출시킨 것이다.**

**진짜 문제는 그 아래에 있다.** 두 창이 같은 «찍은 스펙» 파일을 공유한다
(`cad_project_editor_g/docs/import/0단계_새찍기/…_찍은스펙.json`, git 비추적
작업 산물). 웹 모듈 F 에서 채택·조립을 하면 **데스크톱 G 시험의 입력이 바뀐다.**
G 시험은 밀폐되어 있지 않고, 그래서 「통과/실패」가 그날 무엇을 눌렀는지에
달려 있다. 이번에 실제로 그 일이 났다 — F-11a·F-11b·F-11c 프로브가 B1F 스펙을
세 번 덮어썼다.

**안 고친 이유.** 시험도 창도 `cad_project_editor_g/` 안이고, 이 지시서의 엔진
권한은 `decide_bores` · `build_design_tables` · `source_counts` 의 keyword
추가뿐이다(§3). 문턱값(0.5)을 옮기든 시험의 단정을 고치든 그 권한 밖이다.

**F-11c 와의 관계 — 없다. 실측으로 갈랐다.**

처음에는 「스펙을 프로브 이전 백업으로 되돌리면 통과할 것」이라고 적었는데,
**그 백업은 실행조차 안 된다** — DXF 경로가 끊겨 `DXF를 못 찾음` 으로 죽는다.
추측을 증거처럼 적어 둔 셈이라 지우고, 제대로 된 통제 실험으로 바꿨다:

    픽스처는 그대로 두고 **엔진 두 파일만 F-11c 이전(HEAD)으로 되돌려** 재실행

    F-11c 적용   [FAIL] 제외 1,093 / 후보 2,012 / 도면 3,105
    F-11c 되돌림 [FAIL] 제외 1,093 / 후보 2,012 / 도면 3,105   ← 수치까지 동일

같은 실패, 같은 수치다. `excluded_heads` 는 `select_and_expand` 가 내고
F-11c 는 `decide_bores` · `build_design_tables` · `source_counts` 만 건드리므로
구조적으로도 닿을 수 없다. 이 실패는 **F-11c 이전부터 있던 것**이다.

**다음에 할 것 (권한이 생기면).**
1. G 시험이 제 픽스처를 쓰게 한다 — 공유 작업 폴더를 읽지 않게.
2. 시험의 단정을 창의 실제 규약에 맞춘다(«비율 0.5 이상일 때만 조언»).
   둘 중 어느 쪽이 옳은지는 «조언을 언제 띄울 것인가» 의 설계 결정이다.

## 21. [F-11c 실측] B1F 표 확정이 23분 · 그동안 진행 표시가 **없다**

**실측** (`data/_f11c_b1f.log` · 111 MB · board 노드 21,737 · 헤드 3,235):

    열기 12초 · 채택 7초 · 조립 14초 · 원클릭 <1초        여기까지 40초
    표 확정  1,486 / 1,368 / 1,368 / 1,414 초  (평균 23분)
    저장     0초

**무음 구간.** 마지막 출력은 `convert/planar.py:832` 의 「급수원 Z1 → N7393
펌프」이고, 다음 출력(「노드정리(SSOT)」)까지 **23분간 한 줄도 안 나온다.**
그 사이에 있는 것은 넷뿐이다:

    editor._rebind_managers()
    editor._mark_pipe_key_index_dirty()
    editor.update_all_pipe_lengths_in_pipe_data()
    editor._sync_all_legacy_views_from_graph()
    editor.cleanup_collinear_intermediate_nodes()      ← 규모상 가장 유력

대명동(노드 562 · 배관 561)에서는 순식간이라 안 보였고, B1F 규모(도달노드
11,417 · 배관 9,019)에서 드러났다. 어느 호출인지는 **아직 안 갈랐다** —
가르려면 계측을 넣어야 하는데 그 파일들이 이 지시서에서 읽기 전용이다(§3).

**왜 «성능» 이 아니라 «일반화» 문제인가.** 저장소 규약은 「정직한 진행 표시」다
— 23분 무음은 그 자체로 결함이다. 그보다 더 나쁜 것은 **검증 비용**이다.
한 도면을 완주시키는 데 확정 1회 23분이면, 네 번 재는 데 1시간 반이 든다.
그러면 사람은 «이 도면에선 이것만 재자» 로 기울고, 그 순간 규칙이 도면에
맞춰 편향된다. 실제로 이 작업 중에 그런 제안이 나왔고 사용자가 막았다.
즉 이 항목은 F-11f(3도면 완주)와 F-13(GT 4~5장으로 임계 재검)의 **비용을
직접 정한다.**

**안 한 것.** 엔진 수정 권한이 `decide_bores` · `build_design_tables` ·
`source_counts` 의 keyword 추가뿐이다(§3).

**다음에 할 것 (권한이 생기면).**
1. 다섯 호출에 경과 시각을 찍어 **어느 것인지 먼저 가른다.** 추측으로 고치지
   않는다 — `cleanup_collinear_intermediate_nodes` 가 유력하다는 것도 규모로
   미루어 본 것일 뿐이다.
2. 그 뒤에 진행 콜백을 붙인다. 빨라지지 않더라도 «얼마나 남았나» 는 말해야 한다.

## 22. [F-11d-1 감사] 부속 직접 입력의 키 `(node, pipe)` 는 **안 버틴다**

지시서 F-11d 는 재기 전에 감사하라고 했다 — 「pipe 라벨이 흔들리면 노드쌍 키로
이행」이 조건부였기 때문이다. 쟀고, **조건이 성립한다.**

**왜 흔들리나.** 두 키 모두 제한 전개 결과(corridor)에서 새로 매겨진다:

    label_of[nid] = str(i)  for i, nid in enumerate(bfs_order(net, root))
    pipe_row(pid) → "label": pid          # pid 는 select_and_expand 가 새로 발급

corridor 가 바뀌면 BFS 가 다시 돌아 번호가 재배열된다.

**실측** (`scripts/_probe_f11d_keys.py` · 대명동 · 기준개수 K 를 바꿔 corridor 변경):

| | K 30 → 20 | K 30 → 20 → 30 |
|---|---|---|
| 배관 라벨 (공통 자리) | **105개 중 105개가 옮겨감** | 0개 (완전 복원) |
| 부속 키 `(node, pipe)` | 4 → 2 · **그대로 0** | 4 → 4 · 그대로 4 |
| 부속 키 `(board 노드쌍, where)` | 4 → 2 · **그대로 2** | 4 → 4 · 그대로 4 |

읽는 법:

* `(node, pipe)` 키는 corridor 가 바뀌면 **하나도 안 남는다.** 사람이 「이 자리는
  직선이다」라고 적어 둔 값이 조용히 다른 자리로 옮겨 붙거나 사라진다.
* board 노드쌍 키는 **살아남은 자리를 전부 지킨다.** 4 → 2 로 준 것은 나머지
  둘이 corridor 에서 «빠진» 것이지 이름이 어긋난 것이 아니다.
* 되돌리면(30→20→30) 양쪽 다 완전 복원된다 — **결정적**이다. 즉 흔들림은
  무작위가 아니라 corridor 에 대한 함수다. 그래서 이행이 가능하다.

**결론 — 이행한다.** F-11c 의 관경이 이미 board 노드쌍을 쓴다(D-F11-4).
부속·등가길이도 같은 규약으로 옮기고, 구키는 읽기 호환으로 남긴다.

**★엔진을 고치지 않고 하는 법.** `build_fittings(overrides=)` 는 `(node, pipe)`
키를 받고 그 파일은 이 지시서에서 읽기 전용이다. 그러므로 **세션에는 안정 키로
담고, 엔진에 넘길 때 그 빌드의 `edge_ref` 로 번역한다.** F 가 «판정» 을 다시
하는 것이 아니라 «이름만» 바꿔 주는 것이므로 규칙이 두 벌이 되지 않는다.

**이 프로브가 아직 안 건드린 것.** 배관 살리기(JOIN)·앵커 이동. 둘 다 corridor
를 바꾸므로 같은 결론일 것으로 보지만, **본 것은 K 변경뿐이다.**

**해소 — F-11d (2026-08-31).** 이행했다. 세션은 안정 키
`(board 노드쌍, 그 자리 노드 좌표)` 로 담고, 빌드마다 그 빌드의 `edge_ref` 로
`(node, pipe)` 로 번역한다(`api_design.spot_key` · `spot_index` ·
`fitting_ov_for_engine`). 엔진은 안 고쳤다. 구키만 있는 옛 항목은 적어 둔 이름
그대로 시도한다(읽기 호환).

이행이 실제로 무엇을 고쳤는지가 실측에 그대로 나왔다
(`scripts/_probe_f11d_survive.py` · 대명동 · K 30→20 으로 corridor 변경):

    구키   「그 자리는 이제 «판정 불가» 가 아닙니다 — 자동이 답을 냈습니다」
    안정 키「그 자리가 이번 계산 범위에 없습니다」

구키 `P12` 가 K=20 corridor 에서는 **다른 배관**을 가리켜, 엔진이 그 남의 배관에
대해 답한 것이었다. 앞의 문장은 사실이 아니었고 사람은 그걸 알 길이 없었다.

## 27. [계통도·기계실] 레이어 이름 사전이 **정반대로** 읽는다

**증상(사용자).** 「대명동 계통도를 넣었는데 화면에서 도면 인식도 잘 안 되고
경로 추출도 잘 안 된다.」

**원인 — 분류가 뒤집혔다.** 한 세대를 레이어별로 따로 그려 확인했다
(`scripts/_probe_riser_mr_view.py --split` · `data/_riser_mr/`):

| 레이어 | 자동 분류 | 실제 내용 | 기하 증거 |
|---|---|---|---|
| `SP` | **PIPE** | **헤드 기호** 918개(상향△·하향▽) | 100% 닫힌 폴리라인 · 지름 282mm |
| `0` | OTHER | **배관** | 긴 선분 64% · 지름 중앙값 630mm |
| `LSP`·`HSP` | PIPE | 배관(저층·고층 입상) | 92%·78% 긴 선분 — 맞게 잡힘 |
| `LH`·`HH` | OTHER | 옥내소화전 배관 | 93%·81% 긴 선분 |
| `105-2` | OTHER | 작은 삼각형 224개 | ← 헤드 후보로 **잘못** 잡힌 것 |

이름 사전이 「SP = 스프링클러 배관」으로 읽는데, 이 도면에서 SP 는 스프링클러
**헤드** 레이어다. 증상 둘이 한 원인에서 나온다:

* **인식** — 진짜 헤드 918개가 PIPE 레이어에 있어 검출기가 안 본다. 대신
  `105-2` 의 삼각형을 잡아 높음 띠 **0** · 채택 예정 93개인데 그 93개는
  헤드가 아니다.
* **경로** — 그래프가 헤드 삼각형으로 세워진다. 닫힌 폴리라인은 배관 필터가
  걸러 내므로 남는 것은 LSP+HSP(183 entity)뿐 → **240노드 · 63조각 ·
  가장 큰 덩이 21%**.

**`_build_graph` 의 기하 폴백이 안 돈다.** 조건이 「간선이 **0** 이면」이라,
이렇게 «조금 잡히는데 쓸모없는» 경우에는 발동하지 않는다.

**기계실.** PIPE 로 잡힌 것은 `-소화(SP-고/저)` 54선분뿐. 헤드 후보 8개가 전부
낮음 띠라 규칙이 `none` 을 내고 자동 흐름이 §16 게이트에 막힌다 —
**기계실엔 헤드가 없으니 이건 올바른 동작이다.**

**막다른 길은 아니다.** 찍기 화면의 재료 묶음에는 분류와 무관하게 전부 서
있다(계통도 37개 · 기계실 26개). `FF`·`0` 을 눌러 배관으로 고르면 된다.

**★고치려다 실측이 막은 것.** 묶음마다 「기하로 본 역할(배관다움/기호다움)」을
붙이려 했는데, **선분 단위 지표로는 안 갈린다**
(`scripts/_probe_bundle_role_calib.py`):

    이름 사전이 «맞는» 도면의 진짜 배관
      대명동 -소화(SP가지관) [PIPE]  긴 선분 10% · 중앙값    70mm
      LH306  pipe            [PIPE]  긴 선분 18% · 중앙값   100mm
    계통도에서 «배관 아님» 으로 이미 판명된 레이어
      계통도 4 (층 구획선)            긴 선분 100% · 중앙값 2,100mm

평면도 배관은 부속·꺾임 때문에 짧은 선분이 대부분이라, 「길면 배관」은 정반대로
작동해 격자선·표제란을 1등으로 올린다. entity 단위(닫힘·도형 지름)로는 갈리지만
그 정보는 묶음에 없다. 그래서 **판정을 안 붙이고 잰 값만** 화면에 적었다 —
이름 사전이 틀린 자리에 «틀린 확신» 을 하나 더 얹지 않기 위해서다.

**안 한 것.** `detect_heads`·`_categorize_layer` 는 `remote30_prototype.py`
(특허 규범 참조 · 읽기 전용)에 있다. 기하 폴백의 조건을 「간선 0」에서
「큰 덩이가 너무 작다」로 넓히는 것도 같은 파일이다.

**★연결성 힌트도 재 봤다 — 이것도 못 쓴다.**

「그 묶음이 지금 배관으로 찍은 것과 몇 군데나 맞닿나」를 끝점 격자 해시로
싸게 재고, **떼어놓고 맞히기**로 평가했다 — 이름 사전이 맞는 도면에서 진짜
배관 레이어를 하나 빼고 씨앗을 만든 뒤 그것이 1등으로 돌아오는지
(`scripts/_probe_bundle_touch_calib.py`):

| 도면 | 뺀 배관이 돌아온 순위 | 판정 |
|---|---|---|
| 대명동 | **1위** (접점 16% · 다음이 4%) | ✔ 깨끗이 갈린다 |
| 기계실 | **9위 — 8위 밖** (접점 0) | ✗ 실패 |
| 계통도 | `FF` 가 절대 수로 1등(351접점)이나 진짜 입상관 `0` 은 **0접점** | △ 절반만 |

기계실이 실패하는 이유가 분명하다 — 고층·저층 존과 위생 배관이 **서로 안 닿는
별개 레이어**로 그려져 있다. 그런 도면에서 「접점 0」은 «배관이 아니다» 가 아니라
«이 도면은 묶음끼리 안 맞닿는다» 는 뜻인데, 순위로 보여 주면 사람은 앞쪽으로
읽는다.

**그래서 안 만들었다.** 세 도면 중 하나에서 조용히 틀리는 지표를 순위로 내놓으면
이름 사전이 이미 틀린 자리에 «틀린 확신» 을 하나 더 얹게 된다 — 이번 작업에서
같은 함정을 두 번(선분 길이 · 접점) 재서 피했다.

**남은 길은 둘 뿐으로 보인다.**
1. 모듈 A 의 `_categorize_layer`·`detect_heads` 를 고친다 — 근본이지만
   `remote30_prototype.py` 는 특허 규범 참조라 별도 권한이 필요하다.
2. 사람이 «보고» 고르게 돕는다 — 지금 붙인 수치(연장·선분 수·중앙 길이·원호)에
   더해, 묶음을 누르면 그 묶음만 크게 보여 주는 미리보기. 판정을 안 하므로
   틀릴 수가 없다. **이쪽이 다음 수로 맞다고 본다.**

## 28. [검증기] 도착 화면 판정이 실행마다 흔들린다

같은 실행 안에서 도착 화면을 두 번 재는데 서로 다르게 나온 적이 있다:

    [OK  ] 불러오면 «묻지 않고» 흘러간다 · 도착 = pick
    [OK  ] ★올린 뒤 «사람 결정 0회» 로 도착한다 · 도착 손질

한 번은 그 어긋남 때문에 폴백 분기로 빠져 2건이 **거짓 실패**했다(배너는
손질 문구인데 검사는 찍기 분기를 봤다). 곧바로 다시 돌리면 통과한다.

즉 패널이 뜨는 시점과 검사가 읽는 시점이 경합한다. **검증기가 흔들리면
「이 실패가 내 변경 탓인가」를 매번 다시 재야 한다** — 실제로 그렇게 한 번 썼다.

**해소 (2026-08-31).** 원인은 두 겹이었다.

⑴ 「처음 보이는 화면」을 도착으로 단정했다. 자동 흐름은 **찍기 → 조립 → 손질**
   순으로 지나고 잡과 잡 사이에 진행 표시가 잠깐 꺼지므로, 그 찰나가
   «찍기 도착» 으로 읽혔다. → `_settled()` 로 바꿔 **연속 8번(1.6초) 같은
   화면일 때만** 도착으로 본다. 잡 사이의 틈보다 길고 사람이 기다릴 만한
   시간보다는 짧다.

⑵ 같은 것을 두 번 재는데 **한쪽에만 진행 표시 가드가 있었다.** 가드 없는 쪽은
   조립이 도는 «동안» 떠 있는 찍기 화면에 1.6초 머무는 것을 도착으로 읽어,
   고친 뒤에도 회차마다 `pick` / `손질` 이 갈렸다. → 양쪽 다 `idle="#busy"` 로
   맞췄다.

3회 연속 실행에서 두 판정이 **모두 일치**한다(`edit` / `손질`).

## 29. [산출 누락 · 큼] 모듈 F 의 수리계산 입력에 **기기(FX·알람밸브)가 통째로 없다**

F-11f 의 수정 가능성 매트릭스를 쓰다 「기기표가 빈다」를 찾았고, 크기를 재 보니
작은 빈칸이 아니었다.

**권위 레퍼런스가 싣는 것** (`assets/3-1형_…알람밸브.sdf`):

    <Pipe bore="0.02" label="72" length="0.7" …>
      <Components>
        <Equipment description="FX" equivalent-length="15.6" rel-position="0.44…"/>
      </Components>
    </Pipe>

헤드 접속관(0.7 m)마다 **FX 신축배관 등가길이 15.6 m** 가 붙는다.

| | 배관 총연장 | 기기 등가길이 | 비 |
|---|---:|---:|---:|
| 권위 레퍼런스 | 129.5 m | FX 30개 468.0 m + A/V 12.9 m | **371%** |
| 모듈 F 산출물(대명동) | 134.1 m | **0개 · 0.0 m** | **0%** |

레퍼런스 규약을 우리 망에 대면 헤드 32개 × 15.6 m + A/V 12.9 m =
**512.1 m** 가 빠진다 — **우리 배관 총연장의 382%**.
(도구: `scripts/_probe_equipment_gap.py`)

**등가길이는 마찰손실에 배관 길이와 같은 자격으로 들어간다.** 배관 전체보다
많은 양이 빠졌으므로 압력 계산이 그만큼 낙관적이 된다. 다만 **PIPENET 을 직접
돌려 확인한 것은 아니다** — 여기서 잰 것은 «입력에서 빠진 양» 이지 «결과가 몇 %
틀리나» 가 아니다.

**두 경로가 다른 답을 낸다.** 모듈 A(`remote30_prototype.build_input_tables`)는
헤드마다 FX equipment 를 만들고(`FX_SPEC_PROFILES[…]["eq_len_m"]`) 사람이 규격을
고쳐 넣는 화면(`fx_review`·`_materialize_fx_pipes`)까지 갖췄다. 실제로 A 의
산출물에는 기기가 31개 실려 있다(`cad_project_editor_g/tests/_out/g13_module_a.sdf`).
모듈 F 의 경로는 0개다.

**왜 이렇게 됐나.** G 의 `build_design_tables(valve_nodes=…)` 는 알람밸브만
다루고 FX 는 아예 안 만든다. 그리고 그 `valve_nodes` 조차 **아무도 안 넘긴다** —
웹 모듈 F 도, 데스크톱 G 창(`dialog_design_input.py:317` 이 명시적으로
`valve_nodes=None`)도. 손질 화면에서 알람밸브를 원클릭으로 찍어도 산출에는
안 실린다. `eq_len` 은 `0.0` 하드코딩이라 이었더라도 12.9 m 가 아니라 0 이 된다.

곁따라 `_valve_label(tables)` 이 항상 `None` 이라 「lift 영점 = 알람밸브」 설정이
조용히 폴백된다.

**왜 안 고쳤나.**
1. **산출 값이 통째로 바뀐다.** D-F11-1 은 「사람의 명시적 수정 외에는 산출을
   바꾸지 않는다」이다. 이건 그 규모를 훨씬 넘는 변경이라 조용히 넣을 수 없다.
2. **A 의 FX 로직을 그대로 못 가져온다.** `build_input_tables` 안에 박혀 있고
   그 함수는 A 전용 선정 결과 타입에 묶여 있다 — G 가 표 조립을 다시 짠 바로
   그 이유다(`tables.py` 머리말). F 에서 다시 구현하면 **규칙이 두 벌**이 된다.
3. FX 를 «모든 헤드에» 다는 것이 이 현장에 맞는지는 설계 결정이다(신축배관
   사용 여부·규격). 모듈 A 는 사람에게 묻는 화면을 갖고 있고 F 는 없다.

**정해야 할 것 — 이건 사람이 정할 문제다.**
* FX 를 F 경로에도 실을 것인가. 실으면 어느 모듈이 그 규칙을 소유하나
  (A 에서 꺼내 공용으로 옮기나, G 로 옮기나).
* 실으면 기존 골든이 전부 바뀐다 — 재생성 계획이 필요하다.
* 알람밸브 `eq_len` 0.0 하드코딩도 같이 정해야 한다(레퍼런스는 12.9 m).

## 30. [리팩터링 실측] 수리계산 미리보기의 «앵커 경로» 가 한 번도 안 돈다

**어떻게 찾았나.** 라우트 안전망을 깔다가 `design/preview` 의 본문을 실제로
지나게 하니 `view.anchor` 가 **None** 이었다. 시험의 단정이 과했나 확인하려고
표의 meta 를 직접 봤다(`scripts/_probe_anchor_meta.py` · 대명동):

    원클릭        최불리 30개 · 최원 50.71 m · worst.anchor 있음
    표 meta       기준개수 K '30' · 최원 유하거리 '50.71' · **앵커 노드 '?'**
    미리보기      anchor None · anchor_path 0점 · 노드의 anchor 표시 0개

**원인.** 엔진의 `design/tables._anchor_node()` 가 **무조건 None 을 돌려주는
스텁**이다:

    def _anchor_node(net, worst, meta_nodes):
        an = (worst or {}).get("anchor")
        if an is None:
            return None
        # 앵커는 board 헤드 번호다. 전개 노드와 1:1 이 아니므로 «헤드 노드 중
        # 가장 먼 것» 으로 되짚지 않고, 못 찾으면 솔직히 못 찾았다고 둔다.
        return None

주석이 이미 «못 한다» 고 정직하게 적어 뒀다 — **숨긴 결함이 아니다.** 다만 그
결과가 위쪽까지 이어져 있다는 것은 안 적혀 있었다.

**그래서 무엇이 안 되나.** 화면이 「최원 유하거리 50.71 m」라는 **숫자만** 보여
주고 그것이 **어느 줄인지** 는 못 그린다. `_worst_view` 도크스트링이 그 줄을
따로 싣는 이유로 적어 둔 바로 그 기능이다 — 손질 단계에서는 되고 수리계산
미리보기에서는 안 된다.

**곁따라: 안 도는 코드 40여 줄.** `api_design.module_f_design_preview` 의 급수원
→ 앵커 BFS 블록이 통째로 안 돈다(`if anchor_lab and root:` 가 항상 거짓).
**지우지 않았다** — 못 잇는 원인이 엔진 쪽 좌표계 사정(§17)이고 그것이 풀리면
이 계산은 그대로 맞기 때문이다. 대신 «되는 것처럼 보이는 코드» 가 위험하므로
그 자리에 크게 적어 두고, 시험으로 「아직 안 선다」를 못 박았다
(`test_앵커_경로가_아직_안_선다는_사실을_못_박는다`) — **고쳐지면 그 시험이
실패하면서 알려 준다.**

**고치려면.** board 헤드 번호 ↔ kfp 헤드 노드의 대응이 필요하다. 좌표로 맞추는
길은 §17 이 막았다(설계 좌표계는 board 의 변환이 아니다). 남은 길은 전개
단계에서 «이 kfp 노드는 board 헤드 몇 번» 을 들고 오는 것인데, 그것은
`select_and_expand` 쪽 변경이라 엔진 권한이 필요하다.

### ✅ 해소 — 2026-09-03 (`a140001`)

**막은 것은 §17 이었고, §17 이 틀렸다.** 위 「고치려면」이 「좌표로 맞추는 길은
§17 이 막았다」고 적은 바로 그 문장이 이 항목을 오래 세워 두었다. 좌표로 맞춰
보니 된다 — 실측(B1F · 최불리 30개):

| 되짚는 방법 | 맞은 개수 |
|---|---|
| `node_ref`(kfp 노드 → board 노드) | 30개 중 **12개** |
| 좌표로 맞대기 | 30개 **전부** (6~19mm · 2등/1등 거리비 최소 141.7배) |

`node_ref` 가 안 맞는 이유는 **노드정리 «전» 의 id** 를 담고 있어서다(323 → 80).
즉 못 이을 이유는 없었고 **되짚는 표를 잘못 고른** 것이었다.

세 종류 모두 부착점 x,y 에 헤드 노드가 하나 생긴다(세로 전개는 z 를 옮기지
x,y 를 안 옮긴다. 상하향식만 «하향» 쪽이 combo_2 만큼 밀리는데 «상향» 쪽이
제자리에 남는다). 그래서 `worst_path` 끝 절점을 kfp 좌표로 옮겨 0.10m 안에
**하나뿐일 때만** 받는다 — 없거나 모호하면 종전대로 None 이다.

「고쳐지면 실패한다」고 걸어 둔 시험이 예고대로 실패해 알려 줬고,
`test_최원_유하거리_경로가_선다` 로 뒤집었다. 살아 있는 서버 실측: 기준 헤드
노드 111 · 경로 42절점 · 접속점 1 → 111 · 353.48 m (최원 353.61 m).

## 17. [F-10e] 아이소 밑그림 — 설계 좌표계가 board 와 이어지지 않는다

**항목.** F-10e 「아이소 밑그림 + 그 자리 수정」. 지시서는 이렇게 못 박았다:

> 밑그림 좌표는 corridor 와 **동일한 변환 함수**(`normalize_node_coords` →
> `bake_isometric`, 같은 파라미터)로 만든다 — **별도 수학을 쓰지 말 것.**
> 좌표계가 1픽셀이라도 어긋나면 밑그림의 의미가 없다.

수용 기준도 「공유 배관의 좌표 diff 0」이다. 그런데 그 전제(설계 표가 board
좌표의 변환이다)가 **성립하지 않는다.** 구현하지 않고 기록한다.

**실측 셋 — 대명동 단위세대 평면도, K=30**

| 잰 것 | 결과 | 도구 |
|---|---|---|
| 설계 표 좌표가 board 세계 mm 인가 | **아니다.** 설계 x 1,000~22,650 / board x 242,770~301,570 · 정확히 일치 **0/178** | `scripts/_probe_f10e_coords.py` |
| board → 설계 표에 전역 아핀이 있나 | **없다.** 최소제곱 잔차 중앙값 168.9 · 최대 2,002.7 = 도면 한 변(21,650)의 **9.3%** | `scripts/_probe_f10e_affine.py` |
| K 를 키운 표가 같은 공간인가 | **아니다.** 같은 board 절점이 중앙값 **11,281** 만큼 옮겨진다(정확히 같은 자리 0/155) | `scripts/_probe_f10e_ktable.py` |

원인은 엔진 로그에 그대로 있다 — 표를 만들기 전에 노드를 개별로 옮긴다
(`직선 위치 복원: 109노드` · `노드정리(SSOT): 삭제 185`). 즉 설계 좌표는
board 의 «변환» 이 아니라 **빌드마다 새로 생성되는 스키매틱 배치** 다.

그러므로 밑그림을 어디서 가져오든 등록(registration)이 불가능하다:
board 에서 가져오면 아핀이 없고, 더 넓은 K 표에서 가져오면 그 표 자체가 다른
자리에 있다. 어긋난 밑그림은 지시서 스스로 「의미가 없다」고 한 것이다.

### ⚠️ 정정 — 2026-09-03 · **위 결론은 틀렸다(평면 보기 한정)**

**잔차 9.3% 는 기하가 아니라 «짝짓기» 가 만든 값이었다.**

위 실측은 대응을 `edge_ref`(설계 배관 ↔ board 간선)로 잡았다. 그런데 노드정리가
일직선 중간 노드를 지우면 설계 배관 하나가 board 간선 **여러 개**를 아우르는데
`edge_ref` 는 그중 «첫 간선» 만 기억한다. 그 짝을 쓰면 설계 절점을 사슬 한복판의
board 절점과 맺게 되고, 잔차는 사슬 길이만큼 나온다. §30 이 `node_ref` 로
똑같이 당했다(30개 중 12개만 맞음).

**같은 빌드·같은 표에서 짝짓기만 갈아 끼워 다시 쟀다**
(`scripts/_probe_f10e_affine2.py` · B1F · K=30 · 닮음 3자유도):

| 짝짓기 | 잔차 중앙값 | 최대 | 한 변 대비 |
|---|---|---|---|
| §17 방식 (`edge_ref`) | 18.30 | 1,829.24 | **69.4%** |
| 믿을 수 있는 대응 (헤드 30 + 접속점 1) | **0.05** | **0.47** | **0.018%** |

좌표도 표도 그대로인데 **잔차가 3,905배** 달라진다. 헤드 대응은 §30 에서
6~19mm 로 따로 검증된 것이다.

- **근거 ②(K 의존)도 약하다.** K=10 에서도 잔차 최대 0.02 로 전역 변환이 서고,
  배율은 0.017766876 vs K=30 의 0.017768127 — 사실상 같다. 애초에 밑그림은
  «지금 그 표» 의 변환으로 깔면 되므로 다른 K 표를 끌어올 이유가 없다.
- **근거 ③(아이소)은 옳다 — 다만 이유가 기록과 다르다.** 세우는 헤드를 빼고
  «평면 → 아이소» 를 같은 라벨로 재면(대응을 짝지을 필요조차 없다):

  | 맞춰 본 것 | 잔차 최대 | 한 변 대비 |
  |---|---|---|
  | (x,y) 아핀 | 692.75 | 17.841% |
  | **(x,y,z) 아핀** | **0.00** | **0.000%** |

  아이소는 (x,y,z) 의 **정확한** 아핀이다. 그러므로 평면 (x,y) 만으로는 아이소
  자리가 정해지지 않는다 — 「빌드마다 새로 생성되는 스키매틱 배치라서」가 아니라
  **표고 때문**이다. 평평한 밑그림이 아이소에 안 맞는 것은 원리다.

  실용 수치: z 계수 2,821.9 표시단위/m · 이 도면 표고 폭 0.30m → 한 표고로
  깔면 846.6 단위(한 변의 21.8%) 떠 보인다. 그것은 «어긋남» 이 아니라 실제
  높이차다.

**그래서 지금 아는 것.**

| §17 의 근거 | 판정 |
|---|---|
| ① board → 설계에 전역 변환이 없다 (9.3%) | **틀렸다** — 짝짓기가 만든 값. 실제 **0.018%** |
| ② K 마다 다른 공간이라 못 가져온다 | **약하다** — 배율이 사실상 같고, 애초에 «지금 그 표» 의 변환을 쓰면 된다 |
| ③ 평평한 밑그림은 아이소에 못 깐다 | **옳다** — 다만 이유는 표고다((x,y,z) 아핀 잔차 0.00) |

즉 **평면 보기 밑그림은 막힌 적이 없었다.** 상무의 요구(「밑에 배관이 흐릿하게」)
가 평면 화면으로 우회된 근거 ①이 사실이 아니었다. 아이소에 깔려면 밑그림도
표고를 갖고 같은 아핀을 타야 한다 — 바닥(z=0)에 깔면 망이 제 높이만큼 떠 보이고,
그것이 오히려 옳은 그림이다.

★교훈. §17 의 잘못된 결론 하나가 §30 을 **직접** 세워 두었다(「좌표로 맞추는
길은 §17 이 막았다」). 두 항목 모두 원인은 같다 — **역참조 표(`edge_ref` ·
`node_ref`)를 대응의 근거로 믿은 것.** 그 표들은 노드정리 «전» 을 가리킨다.
대응을 의심할 때는 «짝짓기를 바꿔 같은 것을 다시 재는» 대조를 먼저 하자.

### ✅ 해소 — 2026-09-03 · 아이소 밑그림 구현

지시서가 요구한 그대로 만들었다. 화면을 board 좌표로 갈아 끼우지 않고 **설계
뷰(아이소 포함) 안에** board 망을 깐다 — 토글 `#dg-under`.

**별도 수학 없음.** 서버가 엔진이 쓰는 수를 그대로 실어 보내고(`view.underlay`)
화면은 얹기만 한다. `node_norm_params` 를 갈라 `normalize_node_coords` 와 같이
쓰게 했고, `bake_isometric` 은 `e_ref`·`lift` 를 함께 돌려준다.
①②(board mm → kfp mm → 정규화)는 **합쳐서** 보낸다 — 여기서 한 번 틀렸다.
표는 kfp 좌표를 mm 로 올려 싣는데(§T3) m 로 셈해 1000배가 어긋났다. 두 단계를
화면에 맡기면 그 함정을 화면에도 물려준다.

**표고는 접속점의 것.** board 는 평면도라 그 배관은 수평망이 되고, 그 높이가
접속점 표고다. lift 영점(e_ref)에 두면 실측(B1F)으로 0.300m × lift 2,821.9 =
846.9 단위 = 한 변의 21.8% 가 그대로 어긋났다. 영점은 «보기» 의 기준일 뿐
board 평면이 실제로 놓인 높이가 아니다.

검증
- 시험 6건(`tests/test_g_underlay_xf.py`) — 평면·아이소 모두 board 점이 표
  절점에 **1e-6 미만**으로 떨어진다. JS 식을 소스에서 뽑아 node 로 돌려 파이썬
  셈과 맞대는 시험도 있다(밑그림이 조용히 어긋나는 가장 그럴듯한 길이다).
- 실측(`data/_probe_underlay_xf.py` · B1F): 평면 최대 0.145(0.0048%) ·
  아이소 접속점 0.301(0.0078%). 헤드가 아이소에서 921.6 떨어져 보이는 것은
  세움(stub 75.00) + 표고(0.300 × 2,821.9 = 846.6)로 **전부 설명된다.**
- 화소(`scripts/_verify_module_f_underlay.py`): 켜면 12,313 → 29,063
  (+136.0%) · 평면 보기 35,075 · 아이소 복귀 29,063 · 콘솔오류 0.

**해소 — 2026-08-28 사용자 결정: ⑵ 「밑그림을 아이소가 아니라 평면 화면으로
본다」.** 설계 패널에 「평면에서 보기 — 배관 밑그림 위에서 그 자리 수정」 토글을
두어 구현했다(F-10e). 평면에서는 밑그림과 최불리망이 같은 세계 좌표라 어긋날
수가 없고, 클릭은 손질과 같은 경로를 탄다. 끄면 종전 아이소 그대로다.

**남은 것(미결).** ⑴ 은 여전히 열려 있다 — 아이소 «아래» 에 정확히 깔려면
엔진이 board↔설계 대응을 내보내야 한다. 지금은 필요가 없어졌으므로 서두를
이유는 없다.

**아래는 결정 당시의 선택지 기록이다.**

1. 엔진이 «직선 위치 복원» 의 결과를 board 절점 대응과 함께 내보내게 할까?
   (`node_ref` 는 이미 설계 노드 → board 절점을 들고 있다 — `N1`→2058 꼴.
   부족한 것은 **역방향**, 즉 board 절점 → 설계 좌표다. 없는 절점은 여전히
   답이 없다.) → `cad_project_editor_g/` 수정이 필요하고, 이번 지시서에서
   그 트리는 읽기 전용이다(§3).
2. 밑그림을 «등각 좌표» 가 아니라 **평면(손질) 캔버스** 로 보여줄까?
   전사 24:47 의 요구(「밑에 배관이 흐릿하게 · 안 맞는 게 있으면 클릭」)는
   평면에서도 그대로 만족되고, 그쪽은 등록이 정확하다(F-10c 가 이미 배경
   도면을 깔았다). 다만 「아이소 밑에」라는 그림과는 다르다.
3. 어긋남을 감수하고 대략 겹치게 그릴까? → 지시서가 명시적으로 배제했다.

**임시 우회: 부분적으로 있음** — F-10c 에서 **손질 화면**에 배경 도면을 깔았다.
「밑에 배관이 흐릿하게 보이고 그 자리에서 클릭해 고친다」는 손질 화면에서는
이미 된다(F-10d). 못 하는 것은 **아이소 화면 아래**에 까는 것 하나다.

**F-10f 는 이 항목과 무관하므로 그대로 진행한다.**

### 24-4. 함께 고친 정직성/일관성 3건
- `core/has_converter.py`: `.has` 파이프의 `diameter_inner_mm=0.0` → `PipeDiameter`
  + `PipeMaterial` 로 KSD 실내경 조회(없으면 호칭경). 100A 기준 105.3 vs 100 →
  마찰손실 약 12% 과소 산정이던 구간.
- `core/hydraulic_solver.solve_network`: 소스에서 **도달 못 한 헤드가 남아도**
  나머지가 안정되면 `converged=True` 였다. `len(reached) == len(head_nodes)` 를
  요구하도록 변경 (커밋 `3eb9c4f` "근거 없는 자동합격 제거" 와 같은 취지).
- `sprinkler_remote30_extractor`: ⓐ `diameter_m` 은 fallback 을 쓰는데
  `diameter_label` 은 `None` 에서 파생돼 빈 문자열이 되던 불일치를 한 값으로 통일.
  ⓑ `_refine_edge_diameters` 가 추정한 관경에 `dia_estimated` 표시.
  ⓒ `counts` 에 `path_edges` / `path_edges_estimated_dia` / `path_edges_closure`
  추가 — 전체 closure 수만 보면 **실제 계산에 쓰인 경로**가 얼마나 추정에
  의존하는지 알 수 없었다.

## 25. 직류/분류티 구분 + 엘보 각도 구멍 — 부속 계상 변경 + 골든 1건 재생성 보류
- **항목**: `core/fitting_rules.py` (신설), `build_input_tables` 의 Fittings 블록
  (remote30_prototype.py) — 2026-08-05 실무자 강의 반영.
- **조치 1 (티)**: 차수 ≥ 3 노드에서 하류 갈래를 **전부** 티로 세던 것을,
  유입 방향에서 45°(= `_classify_branch_edges` 의 trunk 임계와 같은 상수) 이내로
  직진하는 갈래는 직류티로 보고 **빼는** 규칙으로 바꿨다. 직류티는 분류티 대비
  마찰손실이 대략 1/10(구경에 따라 1/5)이라 함께 세면 손실이 통째로 부풀려진다.
- **조치 2 (엘보)**: 편향각 43.5~46.5° 와 ≥ 70° 만 인정하고 그 사이·아래를
  **조용히 버리던** 구멍을 없앴다. 22.5/67.5/95° 로 구간을 이어 붙이고, 어느
  부속으로도 보낼 수 없는 각도(≤ 22.5°, > 95°)는 지어내지 않고 "판정 불가"로
  세어 meta `부속 각도 판정 불가 (엘보 / 티)` 에 노출한다.
- **실측 (대명동 201동 단위세대, 관로 148)**:
  `tee` 58 → 29, `elbow` 23 → 29, `elbow-45` 19 → 42, 판정 불가 엘보 8 / 티 0.
  (B1F 현장조사: `tee` 15 → 10, 엘보 기록 0건.)
- **근거 대조 (손으로 만든 SDF 36종, 같은 건물)**: 분기 노드 29~35개에 대해
  실무자가 실제로 단 티는 **35~39개**. 옛 규칙 재현치는 58~69, 새 규칙은 29~34.
  새 규칙이 현업 계상에 훨씬 가깝다(과다 → 소폭 과소).
- **질문 1**: 같은 대조에서 실무자의 `elbow-45` 는 모델당 **1~3개**뿐인데
  (`elbow` 는 30~67), 새 규칙은 대명동에서 42개를 낸다. 도면에 기록된 편향각
  79건 중 정각(45.0° 14건 / 90.0° 10건)을 뺀 55건이 15.9~93.7° 사이에 흩어져
  있고, 지시서 문면("둘 중 가까운 쪽")대로 22.6~67.5° 를 전부 45° 로 보내면서
  생긴 수치다. 이 55건이 실제 45° 부속인지 도면 노이즈인지는 각도만으로 못 가린다.
- **임시 우회**: 예 — **지시서 문면대로 구현**하고 판단은 사람에게 넘긴다.
  좁히려면 `ELBOW_STRAIGHT_MAX_DEG`/`ELBOW_45_MAX_DEG` 를 정각 근방(±3° 등)으로
  조이고 나머지를 판정 불가로 돌리면 되지만, 그러면 판정 불가가 55건으로 늘어난다.
  어느 쪽이 맞는지는 도면 육안 확인이 필요하다.
- **질문 2**: 부속 등가길이는 여전히 내부 solver 에 들어가지 않으므로(§7 질문2)
  자체 수리 결과는 불변이다. 단 SDF 의 Fittings 섹션이 바뀌므로 **PIPENET
  재해석 시 등가길이 총합이 줄어든다** (직류티 제외분).
- **임시 우회**: 예 — 골든 `combined_build__plane_daemyeong` 은 §2/§7/§8/§10 과
  같이 사용자 육안 확인 전까지 재생성 보류.

## 26. 압력표 head_drop_m 의 표고 기준면 — 판정 불가, 부호·오프셋 조정 보류
- **항목**: `FloorRow.head_drop_m` (core/remote30_full_network.py), 작업지시서 4-2.
- **질문**: 압력표의 낙차압을 **층 바닥**에서 재는가, **알람밸브**에서 재는가.
  지시서는 (a) 기준면을 확정해 docstring 에 명시하고 (b) 확정된 뒤에 부호와 값을
  맞추라고 했다. (a) 가 안 되므로 (b) 는 손대지 않았다.
- **확정된 것**: 행 사이 낙차압 증가분 = **그 행 자신의 층고**. 대명동 201동
  `data/sample_problem/배관압력흐름.pdf` 57행을 전량 검산해 한 행도 어긋나지 않는다
  (예: 3층 142.5 − 10층 122.2 = 20.3 = 사이 층고 합). 첫 행만 예외로 수원→옥상
  측점 낙차가 따로 주어진다 (3.1 m — 옥상층 층고 6 m 와 무관한 별개 값).
- **왜 판정이 안 되나**: 증가분이 층고와 같다는 사실은 "측점이 층마다 같은 상대위치에
  있다" 까지만 말한다. **상수 오프셋은 증가분에 안 나타나므로** 그 상대위치가 바닥인지
  알람밸브인지는 가리지 못한다.
- **절대 기준을 대볼 유일한 자료**(같은 현장 수작업 PIPENET 모델 20여 건)의 알람밸브
  표고와 대조하면 차이가 계열마다 달라 하나의 기준면으로 안 모인다:

  | 계열 | Δ = 낙차압 − \|AV 표고\| |
  |---|---|
  | 자연낙차 LSP/MSP (3·10·16·17층) | 6.35 (일정) |
  | HSP (29·32·34·48·49층) | 6.50 ~ 6.65 |
  | 지하 LOOP (B1~B4) | 3.70 (일정) |
  | LLSP (B1~B4) | 4.20 / 4.70 / 5.20 / 5.70 |

  하나의 기준면이면 Δ 가 전 계열에서 같아야 한다. 계열마다 다르다는 것은 수작업
  모델의 AV 표고 자체가 계열별로 다른 관행을 따랐다는 뜻이고, 그렇다면 이 대조로는
  표의 기준면을 역산할 수 없다.
- **부수 확인 — 지시서의 부호 의심은 틀렸다**: 지시서는 "현재 코드는 T 를 AV 보다
  1m 아래에 둔다. 관행과 부호가 반대일 가능성" 을 적었으나, 수작업 모델 전량이
  `5→10 rise=+1.0` (알람밸브가 T분기점보다 **위**) 이고 강의 관행문과도 일치한다.
  현재 코드가 맞다. 그래서 부호는 그대로 두고 상수만 `TEE_TO_ALARM_VALVE_RISE_M`
  으로 올렸다.
- **현재 동작**: `-head_drop_m` 을 알람밸브 노드 표고로 그대로 쓴다. 이것은 확정된
  규약이 아니라 **기존 동작**이며, `FloorRow` docstring 에 그렇게 적어 두었다.
- **임시 우회**: 예 — `tee_branch_above_slab_m`(0.6) / `top_floor_extra_height_m`(0.25) /
  `water_hammer_arrester` 는 ProjectContext 에 받아 서류에만 남기고 표고에 반영하지
  않는다 (`_RECORD_ONLY_FIELDS`, 경고에 "기록 전용" 으로 표시). 기준면이 확정되면
  이 셋을 표고 계산에 넣는다.
- **필요한 것**: 압력표를 작성한 실무자에게 "낙차압 열의 0 점과 각 층 측점이
  물리적으로 어디인가" 를 직접 확인. 도면·파일만으로는 나오지 않는다.
- **부수 발견 (별건)**: 수작업 파일 `..._28F (자연낙차)-RV03_NEW.sdf` 의 AV 표고는
  −78.15 로, 압력표상 22층(84.5)에 해당하고 28층(67.1)과 맞지 않는다. 파일명과
  내용의 층이 어긋난 것으로 보이나 이번 작업 범위 밖이라 손대지 않았다.

## 6. fixture 파일명 불일치 (해소됨 — 기록용)
- **항목**: §0 검증 도면
- **질문**: 지시서의 `1__입력도면_대명동_단위세대_평면도.dxf` 는 저장소에 없음.
- **임시 우회**: 불필요 — 증거로 확정. `samples/dxf/대명동201동 단위세대_layer정리.dxf` 가
  지시서 실측 근거와 정확히 일치 (L4 SPLINE 11,770개, 범례 블록 `A$C60792707`
  @ (288201,−233417) / `A$C3F157AFD` @ (288201,−234617) 동일).

---

# BLOCKED — ModuleF 완성 작업 (ModuleF_완성_작업지시서 §6 기록)

## F-0 · B1F 작업본 충돌 — E/G 양쪽에 서로 다른 사람 편집

**항목.** F-0 작업폴더 마이그레이션(E→G).

**질문.** `B1F 현장조사 소화설비 평면도` 의 유저손질·찍은클릭·표시캐시 4파일이
양 트리에서 다르다. E 쪽(유저손질 923KB · 08-25 10:30)은 F 웹 사용자의 것,
G 쪽(909KB · 08-26 09:05)은 사용자의 아침 G 데스크톱 세션 것 — 어느 쪽이
정본인지는 기계가 정할 일이 아니다.

**임시 우회.** 마이그레이션은 **덮어쓰지 않는다**(G 유지 · E 원본 보존).
`scripts/_migrate_f_workdir.py` 가 충돌 4건을 크기·시각과 함께 찍어 준다 —
E 쪽을 쓰려면 그 파일만 손으로 복사하면 된다.

## F-0 · B1F 저장본 표류 — 검증 기대치의 데이터 의존

**항목.** F-0 완주 검증(`scripts/_verify_module_f.py`).

**사실.** 08-26 아침 세션이 B1F 를 재저장해 board 가 19791pt/3163헤드 →
19040pt/3105헤드로 바뀌었다(`_kfp_baseline` 의 board 지문으로 «입력 변화»
확인 — 코드 회귀 아님). 이 판에서는 자동 이음이 물 닿는 헤드를 못 늘린다
(241→241 · 이음 자체는 됨 · 덩이는 줆).

**임시 우회.** 「1.5배 증가」 기대치는 옛 저장본의 사실이었으므로 «줄지 않음» +
델타 보고로 바꿨다. 증가 폭의 골든 검증은 F-7 이 사용자 데이터가 아닌
**고정 픽스처**로 한다.

## F-0 · E 시절 키들의 원본 DXF 가 이 PC 에 없다

**항목.** F-0 수용 기준 「E 작업폴더에 있던 키로 reopen 성공」.

**사실.** E 전용 키 4종(3F · apt · MF-301 · B1F_full_libredwg)의 찍은스펙이
가리키는 원본이 전부 다른 PC 경로(`C:\Users\yeoyo\…`)거나 지워진 업로드다.
즉 이 키들은 **재지정 전에도 이 PC 의 F 에서 열린 적이 없다** — 게다가 그때는
엔진의 `SystemExit` 를 잡 러너가 못 잡아 무한 «run» 으로 멈췄다(이번에 F 쪽
잡 러너를 고쳐 명확한 오류로 끝난다).

**질문.** `expand.py:101` 은 stage1 sqlite 캐시가 있어도 원본 파일 존재를 먼저
요구한다. 캐시만으로 열게 하려면 엔진을 고쳐야 하는데, 이 지시서에서 엔진 변경은
F-1 한 곳뿐이다.

**임시 우회.** 열리던 키(B1F 소화설비)는 계속 열리고, 안 열리던 키는 조용한
멈춤 대신 «DXF를 못 찾음» 으로 끝난다. 캐시 단독 열기는 엔진 소관으로 보류.

## F-2 · «diff 0» 은 Pipe label 정규화 후에만 성립 (G B7 승계)

**항목.** F-2 수용 기준 「G 데스크톱과 F 웹의 .sdf 완전 동일」.

**사실.** 엔진의 배관 id 부여가 set 순회 순서를 타서 같은 코드·같은 입력이라도
실행마다 P383↔P385 처럼 갈린다 — G BLOCKED B7 이 .kfp 에서 이미 확정한 그
비결정성이고, 엔진 수정은 이 지시서에서 금지다. 노드 label·좌표·bore·length·
rise 는 전부 바이트 동일하다.

**임시 우회.** 검증을 «Pipe label 정규화 후 바이트 동일 + label 집합은 순열» 로
세웠다(44,274B 전 구간 일치). .kfp 의 구조 지문과 같은 원칙이다.

## F-2 · design/build 의 급수원 seed 는 아직 전체

**항목.** F-1 규약을 design 경로까지 잇기.

**질문.** `select_and_expand` 는 내부에서 `worst_k_heads` 를 부르는데
`source_index` 를 아직 받지 않는다 — 엔진 변경이 F-1 의 worst.py 한 곳으로
제한돼 있어 여기서 멈췄다. 현 B1F 는 급수원 1곳이라 실차이는 없다.

**임시 우회.** build 의 `source` 인자는 전개 쪽(`selected_source`)에만 전달.
2급수원 도면이 실제로 오면 select_and_expand 에 keyword 를 더하는 엔진 커밋
(G 테스트 동시 통과 조건)으로 푼다.

## F-7 · G 데스크톱의 처분 (유지 vs 은퇴)

**항목.** 지시서 F-7 — 여기서 정하지 말고 사용 실태와 함께 올릴 것.

**사용 실태.** ① 사용자가 08-26 아침에도 G 데스크톱으로 B1F 를 손질·재저장했다
(작업폴더 mtime 실측 — 즉 현역이다). ② F 웹과 G 데스크톱은 이제 **같은
작업폴더**(cad_project_editor_g/docs/import)를 공유하므로 데스크톱에서 찍은
도면이 웹에서 그대로 이어진다 — 종전 F↔E 구도와 동일. ③ 엔진이 한 벌이라
유지 비용은 «Qt 창 코드» 뿐이고, G 테스트가 그 창(대화상자 offscreen)까지 덮는다.

**판단 재료.** 원격 사용은 F 만 가능하지만, 서버 PC 앞에서는 데스크톱이 더
빠르다(브라우저 왕복 없음). 은퇴시키면 4번째 창 대조(F-2 의 diff 검증 상대)도
사라진다.

**임시 우회.** 유지. 처분은 사용자 결정으로 남긴다.

## F-8a · 신뢰도 띠 분포가 지시서 예시와 반대다 (D-F8-4 기본값 영향)

**항목.** 지시서 F-8c 의 카드 예시 · D-F8-4 기본 채택 임계.

**질문.** 지시서는 카드 예시를 이렇게 적었다:

```
헤드 후보 3,163 (높음 2,891 · 중간 210 · 낮음 62)
```

B1F 실측은 **거의 반대**다(`scripts/_verify_module_f_recon.py`):

```
헤드 후보 3,338 (높음 72 · 중간 3,163 · 낮음 103)
```

「3,163」이 예시에서는 *총계*, 실측에서는 *중간 띠* 라는 점까지 겹쳐, 예시가
중간 띠 수를 총계로 옮겨 적은 것으로 보인다. 어느 쪽이든 결과는 같다 —
**D-F8-4 의 「≥0.9 기본 채택」을 그대로 적용하면 B1F 에서 3,338 중 72개(2.2%)
만 채택된다.** 혼합 차선의 존재 이유가 「자동으로 80% 를 잡는 것」(§0.1)인데
2% 를 잡게 된다.

원인은 A 의 신뢰도 규칙이다 — `KNOWN_HEAD_BLOCKS` 에 든 블록 참조만 0.95 이고,
그 외 HEAD 레이어 도형은 0.70~0.89 로 떨어진다(R1~R5). B1F 의 헤드는 대부분
블록이 아니라 레이어에 직접 그린 도형이라 중간 띠에 몰린다.

**두 번째 도면에서 더 나빴다** (F-8c 브라우저 검증, LH306동_평면도):

```
헤드 후보 42 (높음 0 · 중간 40 · 낮음 2)
```

높음이 **0** 이라 기본 기준으로는 혼합 차선이 아무것도 못 찍는다.

**세 번째 도면은 반대였다** (F-8d 검증, 대명동 단위세대 평면도):

```
헤드 후보 119 (높음 87 · 중간 14 · 낮음 18)
```

여기서는 0.9 기본값이 73% 를 잡는다. 즉 이것은 «도면이 헤드를 어떻게 그렸나»
에 달린 문제다 — 알려진 블록으로 넣었으면 높음, 레이어에 직접 그렸으면 중간.
세 도면 실측:

| 도면 | 후보 | 높음(≥0.9) | 비율 |
|---|---:|---:|---:|
| 대명동 단위세대 | 119 | 87 | 73% |
| B1F 현장조사 | 3,338 | 72 | 2% |
| LH306 평면도 | 42 | 0 | 0% |

사용자의 주력 도면(B1F)이 2% 라는 것이 요점이다.

**임시 우회.** 예 — **D-F8-4 를 그대로 구현한다**(변경 금지 항목). 기본값은
0.9 이고 카드도 0.9 로 뜬다. 다만 눌러도 아무 일이 없는 단추를 열어 두면
사람은 프로그램이 고장 났다고 읽으므로, ① 기준에 맞는 후보가 **0개면 단추를
잠그고 사유를 적는다**(「이 기준에 맞는 후보가 없습니다 — 기준을 낮추거나 다른
길로 가세요」), ② 임계를 화면에서 고를 수 있게 하고(D-F8-4 가 이미 요구),
③ 카드에 실측 분포를 그대로 띄워 사람이 즉시 보게 하며, ④ F-8e 실측표에
0.9 와 0.75 두 경우를 **나란히** 기록한다.

기본값을 0.75 로 내릴지는 그 표를 보고 사용자가 정할 일이라 여기서 바꾸지
않는다. 다만 셋 중 둘에서 — 그리고 사용자의 주력 도면에서 — 0.9 는 쓸 수 없는
기본값이다.
