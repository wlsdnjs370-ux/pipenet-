# BLOCKED — ModuleA 2앵커 개선 작업 (작업지시서 §5 기록)

형식: (항목, 질문/애매점, 임시 우회 여부)

## 1. W4 — shapely 전제 오류
- **항목**: W4 (HeadRegion)
- **질문**: 작업지시서는 "shapely `Polygon` 기반 — extractor가 이미 shapely 의존"이라 명시하나,
  실제로는 shapely 미설치 + 저장소 전체에 shapely import 0건 (전제가 사실과 다름).
  신규 외부 의존성(shapely)을 추가할지는 배포 환경(anaconda system site-packages) 정책 판단 필요.
- **임시 우회**: 예 — 동일 API(`from_rects`/`from_polygon`/`contains`/`dilate`)의
  순수 파이썬 HeadRegion으로 구현. 추후 shapely 도입 결정 시 내부 구현만 교체 가능.

## 2. 회귀 기준선 — golden 1건이 작업 시작 전부터 불일치
- **항목**: §3 비트동일 회귀 증명 (전 항목 공통)
- **질문**: `tests/characterization` 실행 결과, 작업 시작 시점(HEAD=ae05799)에 이미
  `combined_build__plane_daemyeong` 이 golden과 불일치 (직전 커밋 ae05799 적응형 tol/
  라우팅 penalty의 의도된 변경 + golden 재생성은 "사용자 육안 확인 후"로 보류 중인 상태).
  stale golden 을 기준으로는 비트동일을 증명할 수 없음.
- **임시 우회**: 예 — 작업 시작 시점 HEAD의 실제 산출을 `data/_w_baseline/`(비추적)에
  동결하고, 각 W 항목 후 전 케이스를 이 baseline과 diff 하여 비트동일을 증명.
  golden 파일 자체는 수정하지 않음(사용자 확인 게이트 유지).

## 3. W1.2 — audit 기록 시점
- **항목**: W1 (미도달 헤드 보고)
- **질문**: "파이프라인 후단에서 audit(→W7)에 기록" — audit 객체(ExtractionAudit)는 W7에서
  신설되고, anchored 파이프라인 경로 자체도 W2·W3에서 형성되므로 W1 시점에는 기록할
  장소가 없음.
- **임시 우회**: 예 — W1에서는 계산 함수(`find_unreachable_region_heads`)까지 신설·테스트하고,
  anchored 경로/audit 배선은 W2~W7 진행에 맞춰 완성.

## 4. W3 — 도면 전제 불일치 → 브릿지 양단 W-창 한정
- **항목**: W3 (표적 브릿지)
- **질문**: 지시서 전제("세대별 배관망이 분리돼 있고 가짜 봉합은 브릿지에서만 발생")가
  실측과 부분 불일치 — 대명동 fixture 는 weld 후에도 세대 경계를 넘는 컴포넌트가 존재
  (최대 comp x=245k→284k, comp12 x=252k→262k). 헤드 보유 컴포넌트만 대상으로 좁혀도
  그 컴포넌트의 *동측* 지점에서 봉합되면 동측 우회 경로가 최종망에 유입됨
  (수용기준 FAIL 재현: 노드 (259436, -229271) 포함).
- **임시 우회**: 예 — §0 설계원리("앵커가 봉합 방향을 유도")에 따라 브릿지 양단 후보를
  작업창 W(=convex_hull(head_region ∪ {alarm_xy}) + ANCHOR_W_MARGIN_MM 팽창) 내부로
  한정하는 `within` 파라미터를 `bridge_targeted` 에 추가. 짝짓기 대상(comp(source) ↔
  헤드 보유 컴포넌트)·tol 계단·merge-and-reevaluate 는 지시서 그대로.
  head_region 이 `.pts` 를 노출하지 않으면 W 제한 없이 동작(W4 HeadRegion 이 정식화).
- **후속 (2026-07-29, P1)**: 가짜 봉합의 원인이 브릿지 하나가 아니었음이 확인됨.
  `_weld_dangling_endpoints` 는 노드↔노드만 이을 수 있어, 가지관 끝점이 주배관
  *중간* 에 닿는 실제 T분기를 구조적으로 복원할 수 없었다. 그 끝점들은 전방
  콘 안의 엉뚱한 노드(주배관 반대편 끝, 다른 조각)에 붙어 오접합이 됐다.
  대명동 서측 세대 실측: 그런 후보 47건 중 32건이 오접합, 허위 배관 10.7m.
  해소: `_split_tee_branches`(P1) 를 weld *이전* 에 두어 edge 를 제자리에서
  쪼갠다. 배출망 79.25m → 73.54m, weld 364 → 299, 헤드 25/25 유지.
  잡힌 148건의 갭 중앙값 0.0mm / 최대 0.1mm — 전부 정확한 CAD T분기였다.

## 5. W5 — 음성 유형(SPLINE 등) 판정 전제와 파서 현실 불일치
- **항목**: W5 (공간한정 조건부 재선별)
- **질문**: 지시서는 재선별 범위에서 "SPLINE/ELLIPSE/… 음성 유형 승인 금지"를
  요구하나, 파서(parse_dxf_bundle)는 SPLINE/ELLIPSE 를 flattening 해 "PL" 타입
  dict 로 담으므로 파이프라인 입력(entity dict) 수준에서는 원 dxftype 을 구분할
  수 없음. entity dict 에 원천 태그를 추가하면 골든 characterization 의
  entities_sig(raw dict 해시)가 바뀌어 비-anchored 비트동일 요구를 위반.
- **임시 우회**: 예 — 재선별 헬퍼(collect_spatial_reselect_segments)가 DXF
  원본을 ezdxf 로 직접 스캔해 실제 dxftype 으로 승인/음성 판정. 이 때문에
  anchored 함수에 dxf_path 인자가 추가됨(재선별 발동 시에만 사용). 블록(INSERT)
  내부의 OTHER 선분은 이 스캔 범위 밖 — 저-prior 후보 특성상 수용.

## 6-1. W7 — bridges "layers" 필드는 그래프 수준에서 복원 불가
- **항목**: W7 (ExtractionAudit)
- **질문**: 스키마의 `bridges [{p1, p2, len_mm, layers}]` 중 `layers` — 브릿지는
  그래프(노드 좌표 + edge 길이만 보유) 수준에서 생성되므로 양단이 유래한 원
  entity 의 layer 정보가 이미 소실됨. 노드→layer 역매핑을 만들려면 _build_graph
  변경(골든 위험) 또는 중복 자료구조 신설이 필요.
- **임시 우회**: 예 — `layers: null` 로 스키마 자리만 채워 기록. 추후 필요 시
  _build_graph 에 선택적 node→layer 출력 kwarg 를 추가해 채울 수 있음.

## 7. 유속 수렴 사이징 — 골든 1건 재생성 보류 + 피팅 등가길이 미모델링
- **항목**: 통합 유속 수렴 (`core/hydraulic_solver.py`, V1~V6)
- **질문 1**: 내경 산정 근거가 "수요유량 1패스"에서 "과토출 유량 수렴"으로 바뀌어
  `combined_build__plane_daemyeong` 골든의 `dia`/`velocity_mps` 가 필연적으로 달라짐.
  골든 재생성은 §2 와 같은 이유(사용자 육안 확인 게이트)로 아직 못 함.
- **임시 우회**: 예 — 골든 파일 미수정. 대명동 201동 실측으로 무해함을 확인:
  `changed=0` (기존 내경 그대로 통과), 위반 0건, 과토출 1.27배, 소스압 2.10 bar.
  즉 이 도면에서는 내경이 하나도 안 바뀌고 stamp 값(`velocity_mps`)만 과토출 기준으로
  갱신됨 (max 1.92 → 1.97 m/s).
- **질문 2**: `<Fittings>` 등가길이를 해석에 넣지 않음(equipment 의 명시 `eq_len` 만 반영).
  손실 과소 → 헤드 간 압력차 과소 → 과토출 과소 평가 = **비보수측**.
- **임시 우회**: 예 — 사이징 판정에만 `safety=1.1` 유량 할증으로 상쇄. 피팅 등가길이
  테이블을 해석에 직접 넣는 것은 별도 과제.
- **질문 3**: 극단적으로 얇은 초기망(전 구간 25A 등)은 최원단 0.1 MPa 를 물리적으로
  만족시킬 수 없어 소스압이 폭주(실측 1772 bar)하고 고정점 반복이 발산함.
- **임시 우회**: 예 — 발산 회차에는 유량의 **크기**를 믿지 않고 방향("너무 얇다")만
  써서 한 치수씩만 승급. 대명동 백지설계 기준 최종 내경이 150/200A 일색 → 32~100A
  등급 분포로 개선됨 (승급 1279회 → 316회).
- **질문 4 (domain-slim 포팅)**: domain-slim 에만 있는 `size_combined_bores`
  (역할별 내경: 평면도=규약 유지 / 입상관=단일 균일경 / 기계실=한 단계 굵게)와
  유속 수렴이 같은 값을 두 번 정한다. 두 정책의 우선순위가 지시서에 없음.
- **임시 우회**: 예 — 수렴 루프를 `size_combined_bores` **뒤**에 두고
  `keep_existing=True` 로 실행. never-shrink 라 역할별 정책은 보존되고, 그 결과가
  유속을 못 견딜 때만 승급된다. 대명동 실측 `changed=0` (역할별 배정이 이미
  유속을 만족) — 두 정책이 충돌하지 않음을 확인.

## 8. AV 표고 기준 통일 — 정수압 변화 + 골든 1건 재생성 보류
- **항목**: `stitch_riser_and_heads` (core/remote30_full_network.py)
- **질문 1**: 평면도 추출은 전 노드에 평탄한 상대표고(2.8m)를, 계통도 추출은 수원=0
  기준 절대표고를 준다. 두 추출의 유일한 공통 노드인 AV 에서 이 기준 차이가 그대로
  남아, AV 를 잇는 배관 하나에 가짜 낙차가 실렸다 (대명동 실측: 라이저 AV −78.15m
  vs 헤드망 +2.8m = **80.95m**). 3D 통합 화면에서 계통도 밸브는 아래로, 평면도 밸브는
  위로 갈라져 보였고, 수리해석에는 없는 정수압이 들어갔다.
- **임시 우회**: 아니오 — 정식 조치. 헤드망 노드 전체를 `riser_av_elev − head_av_elev`
  만큼 평행이동해 AV 지점의 z 를 양쪽에서 동일하게 만듦. 배관의 `elev` 는 구간
  낙차(delta)라 기준 이동의 영향을 받지 않아 손대지 않는다. `prepend_machine_room_to_riser`
  가 이미 쓰는 rebase 패턴과 동일. 대명동 재측정: AV(10)·평면도측 이웃(71) 모두 −78.15m,
  헤드평면 145개 노드가 AV 와 같은 표고로 정렬.
- **질문 2**: 헤드망 표고가 바뀌므로 정수압이 달라져 SDF/KFP/HAS 수치 산출이 변한다
  (표시만의 변경이 아님). `combined_build__plane_daemyeong` 골든이 추가로 벌어진다.
- **임시 우회**: 예 — §2·§7 과 같은 이유(사용자 육안 확인 게이트)로 골든 미수정.
  테스트 결과는 조치 전후 동일하게 `1 failed, 80 passed` (실패는 그 1건뿐).

## 9. 기계실 이음매 정합 — 회귀 테스트 공백
- **항목**: `_layout_machine_room_plan` (core/remote30_full_network.py)
- **질문 1**: 기계실 평면 군집을 bbox 중앙 + 고정 gap 으로 펌프 노드 옆에 띄워
  배치해, 입상관 연결점(mK)이 펌프 노드에서 도면 대각의 **18.14%**(고가수조) /
  **14.47%**(펌프가압) 만큼 떨어졌다. mK 는 `prepend_machine_room_to_riser` 가
  라이저 Input 과 병합해 없앤 노드라 그 자리가 곧 펌프 노드여야 한다.
- **임시 우회**: 아니오 — 정식 조치. 변환의 영점을 bbox 중앙이 아니라 mK 의 raw
  좌표로 잡아 `_tf(mK) == pump_xy` 가 되게 함(AV 정합과 같은 패턴). 재측정 결과
  이음매 Δx=0.0, Δy 는 도면상 실제 배관 길이와 일치. 좌표만 바뀌므로 수리결과 불변.
  z 는 조치 불필요 — 고가수조는 `mr_ref_elev = riser_input_elev` 로 이미 동일하고,
  펌프가압의 차이(= `source_drop_below_lowest_m`)는 의도된 흡입 고저차로
  `core/hydraulic_solver.py:311` 이 노드 표고에서 실양정으로 소비한다.
- **질문 2**: 기계실 경로를 태우는 golden/fixture 가 하나도 없어(전 골든이
  `machine_room_labels: []`) 이 경로는 회귀 테스트로 보호되지 않는다. 실측은
  합성 기계실 dict 를 쓴 일회성 프로브로만 했다.
- **임시 우회**: 예 — 기계실 픽 좌표(수원·입상관 연결점)는 사용자 클릭 입력이라
  fixture 화하려면 `data/201동 기계실.dxf` 기준 픽 좌표 확정이 필요. 사용자 지정 대기.

## 10. 평면도 rooted traversal — tee 개수 증가 + 골든 1건 재생성 보류
- **항목**: `build_input_tables` (remote30_prototype.py)
- **조치**: 계통도·기계실처럼 시작노드(AV)에서 말단으로 뻗는 형태로 통일. AV 를 루트로
  BFS 를 한 번 돌려 (a) 노드 라벨을 BFS 순으로, (b) 배관 행 순서를 BFS 순으로,
  (c) 배관 `in`/`out` 을 부모→자식(상류→하류)으로 맞췄다. 기존엔 라벨 숫자 크기순이라
  흐름과 무관했다. meta 에 도달/미도달 노드, 최대 깊이, 분기 노드 수, **트리 밖 edge 수**
  (= "길이 잘못 트인" 후보)를 실었다.
- **질문 1**: 방향이 일관되면서 tee 규칙(`in` 노드 차수 ≥ 3)이 분기점마다
  하류 가지 수만큼 tee 를 내게 되어 대명동 기준 fittings **56 → 70** 으로 늘었다.
  기존 56 은 라벨 숫자 운에 좌우된 값이라 기준선으로 삼기 어렵다.
- **임시 우회**: 아니오 — 결정적 값으로 확정. 좌표 기준 서명 대조 결과
  pipes(144)·nodes(145)·nozzles(30)·equipment(31) 는 **완전 동일**, fittings 만 변경.
  자체 solver 산출(`source_pressure_bar` −5.844, `max_velocity_after` 1.97)도
  변경 전후 **바이트 동일** — 피팅 등가길이는 아직 미모델링(§7)이라 내부 수리결과 불변.
  단 SDF 의 Fittings 섹션은 늘어나므로 **PIPENET 재해석 시 등가길이가 증가**한다.
- **질문 2**: 골든 `combined_build__plane_daemyeong` 은 라벨·행순서 변경으로 계속 불일치.
- **임시 우회**: 예 — §2/§7/§8 과 동일하게 사용자 육안 확인 전까지 재생성 보류.

## 11. L2 — "격자배관은 네 변 모두 부하 ≥ 1" 전제가 L1 부하 정의와 모순
- **항목**: L2 (`prune_by_load`), 유량누적 작업지시서 §0·§2
- **질문**: §0 은 "부하 0인 간선만 자르므로 격자배관은 네 변 모두 부하 ≥ 1이라
  보존된다"고 하지만, L1 의 부하는 §1 이 못박은 대로 **단일 최단경로 트리 위에서만**
  정의된다. 사각 루프에서 최단경로 트리는 항상 변 하나를 비트리로 남기고 그 변의
  부하는 정의상 0 이다. 따라서 L1 출력을 그대로 L2 에 넣으면 **모든 사이클이 반드시
  하나 이상의 부하 0 변을 가져** 루프가 보존되지 않는다. L2.3 의 "잔여 사이클"
  (모든 변이 부하 ≥ 1) 은 L1 출력에서는 발생할 수 없다.
- **임시 우회**: 예 — 지시서 문면대로 구현하고 모순은 여기 기록한다. `prune_by_load`
  는 부하 dict 를 인자로 받으므로 루프를 보존하는 다른 부하 정의(예: 다중경로/유량
  분배 기반)를 넣으면 §0 의 의도대로 동작한다. L1 부하로 실행할 때의 실제 효과는
  **"SPT + 죽은 가지 제거"** 이며, 이것만으로도 `force_spanning_tree` 대비 개선이다
  (도달 가능하지만 헤드에 기여하지 않는 드레인·시험배관·막다른 가지를 제거).
  격자·환상 배관 지원은 §1 이 명시한 범위 밖 — `docs/load_extraction.md`(L6)에
  폴백 조건과 함께 문서화한다.
- **질문 2**: L2.1 은 제거 간선의 "소속 레이어" 기록을 요구하나 `graph`/`edge_len`
  은 좌표 키만 보존해 레이어를 복원할 수 없다(§6-1 과 동일한 제약, §3 자료구조
  변경 금지).
- **임시 우회**: 예 — `edge_layers` 선택 인자를 두어 호출측이 줄 때만 기록하고,
  주지 않으면 레이어 필드를 생략한다.

## 12. L3 — 담당 헤드 수는 통합했으나 "헤드 집합"이 두 곳에서 다르다
- **항목**: L3 (`compute_edge_load` → S430 담당 헤드 수), 유량누적 작업지시서 L3.2
- **실측(대명동 201동 서측 세대, `scripts/_l3_load_vs_s430.py`)**:
  같은 트리(`rooted_traversal` parent_map)·같은 헤드 집합을 주면 현행
  `_subtree_calc` 와 `compute_edge_load` 는 **79개 pipe 전 구간 일치, NFPC 별표1
  최소 호칭경 불일치 0** → L3.1 통합은 수용기준 PASS 로 수행함.
- **질문**: 남은 차이는 *헤드 집합*이다. S430(=별표1 최소 호칭경)은 **선정된 top-K
  헤드**만 세고, L1 을 파이프라인 앞단(L4)에서 쓰면 **승인된 전체 헤드**를 센다.
  대명동은 승인 25 = 선정 25(K=30 미만)라 두 값이 같지만, 헤드가 K 를 넘는 도면에서는
  선정 밖 헤드가 담당 수에서 빠져 **최소 호칭경이 과소 산정될 수 있다.**
  같은 실측의 [B] 대조에서 부하가 달라지는 구간 11/79 가 나왔으나(헤드 좌표 부착
  차이) 호칭경이 바뀌는 구간은 0 이었다.
- **임시 우회**: 예 — L3.1 은 "같은 트리·같은 헤드 집합" 조건에서만 통합했고,
  헤드 집합 자체는 **통합하지 않았다**(지시서 L3.2: 잘못된 통합은 법정 최소
  호칭경을 틀리게 만든다). L4 의 파이프라인 부하는 추출 소속 판정용, S430 의 부하는
  호칭경 산정용으로 분리 유지한다. K < 승인 헤드 수인 도면에서 별표1 적용이
  타당한지는 도메인 판단이 필요 — 사용자 확인 대상.

## 13. O1·O2·O3 직교화/평면화 — §11 격자 보존 모순을 트리 채택으로 종결 + 골든 영향
- **항목**: O1 (루프 금지 게이트), O2 (`orthogonalize_edges`),
  O3 (`planarize_edges`)
- **사용자 지시**: "모든 배관망의 각도는 꼭 90도가 되도록 조치하고, 일단은 루프는
  안생겨도되니까. 루프만 안생기게 조치해" → §11 의 격자 보존 요구는 **철회**되고
  가지식 트리가 정식 채택됐다. `on_residual_cycle` 기본값을 `preserve` →
  `force_tree` 로 뒤집었고, 최단경로 합집합 단계에 union-find 게이트를 넣어
  load_mode 와 무관하게 최종망이 항상 트리다.
- **실측(대명동 201동 서측 세대, `scripts/_ortho_probe.py`)**:
  원시 그래프 비직각 287/856(33.5%) → 병합 후 64.6% → **직교화 후 0/116(0.0%)**.
  독립 사이클 0, 총연장 79248.813mm 불변(오차 0.000000%), 소스·헤드 좌표와
  거리 배열 완전 동일, elbow fitting 34개 보존. 스냅 44노드(최대 17.3mm),
  L자 분해 37간선, 충돌 되돌림 2노드, 미해결 대각 0.
- **부작용(수용)**: 25mm 이하 짧은 세그먼트가 11개 → 13개, 최소 길이 3.65mm →
  1.70mm. 직교화 전에도 mm 급 간선이 있었고 수리적 영향은 무시 가능하다.
- **후속(O3 평면화) — 사용자 판단 필요**: "여전히 루프처럼 보이는 구간이 있어"
  보고로 재측정한 결과, 그래프 사이클은 0인데 **간선이 노드를 관통(T분기 19)·
  서로 교차(X 5)·같은 축선에서 겹침(5)** 하여 화면에 닫힌 영역이 생기고 있었다.
  `planarize_edges` 로 관통·교차점을 절단해 실제 분기로 만들고, 포개진 중복은
  한 줄만 남기고, 생긴 사이클을 소스 기준 SPT 로 자르고, 급수 대상 없는 막다른
  조각을 제거했다. 세 결함 모두 **0**(대명동 서측: 간선 112 · 노드 113 ·
  연결성분 1 · 사이클 0 · 비직각 0 · 헤드 25개 전원 잔존 · source 불변).
  **총연장이 79248.813 → 65406.836mm (−17.5%) 로 줄어든다** — ①② 는 여전히
  길이 보존이지만 ③ 은 중복 17 · 사이클 5 · 막다른 조각 8 간선을 실제로 걷어낸다.
  걷어낸 것이 전부 "포개진 배관·우회 배관·죽은 조각"이라는 판단은 형상 기준이며
  **수리계산 결과가 바뀐다**. 시각 확인 후 사용자 승인 필요.
  사라진 노드를 가리키던 추정-edge(브릿지·용접·head-drop) 기록도 함께 제거된다.
  잔여 미조치: 비인접인데 3.65mm·10.61mm 떨어진 노드쌍 2건(도면 축척상 비가시).
- **골든**: `combined_build__plane_daemyeong` 출력이 바뀐다. 단 이 골든은 **작업
  시작 전부터 이미 불일치**(§2·§7·§8·§10)이므로 신규 회귀가 아니다. 재생성은
  사용자가 5051 에서 대명동/B1F 를 눈으로 확인한 뒤로 보류.
- **미해결**: 비앵커 경로(`select_worst30_heads`)도 직교화가 기본 on 이지만 이
  경로에는 브릿지·용접 audit 좌표 재매핑 대상이 없어 검증 범위 밖이다.

## 14. R1~R6 추정 연결 폐지 — 실배관만으로 판단 + 조각 정직 보고
- **항목**: 사용자 지시 "추정 용접 추정 브릿지 싹 다 없애버려 그냥. 무조건 실배관으로
  판단해. 제대로." (2026-07-30)
- **조치**: 평면도 3개 경로(`select_worst30_heads` / `select_worst30_heads_anchored` /
  `run_stages_0_2` Stage 3)에서 배관↔배관 추정 연결(용접·브리지)을 전부 제거.
  남는 추정은 기호↔배관 head/source drop line 뿐이다(점선·별색 렌더 유지).
  §4 의 `bridge_targeted`·`_weld_dangling_endpoints` 는 평면도에서 더 이상 호출되지
  않는다(계통도 `extract_pipe_network(force_connect=True)` 경로에만 잔존).
- **삭제한 목발을 대체한 것**:
  - **R1 `auto_snap_eps`** — `SNAP_TOL_MM=50mm` 고정값이 실제 이음매 간격보다 작아
    연결 배관이 조각나던 것이 근본 원인이었다. 후보를 훑어 최대 연결성분을 키우는
    값을 채택(총연장 90% 하한). 실측 채택값: 대명동 75 · LH306 130 · B1F 90 ·
    LH지하 160mm.
  - **`_split_tee_branches` 무조건 실행** — 끝점 클러스터는 노드↔노드만 합칠 수
    있는데 실제 가지관은 주배관 *중간* 에서 갈라진다. 새 좌표를 만들지 않으므로
    추정이 아니라 실배관 복원이다. `tee_split` 플래그는 삭제했다.
  - **`attach_source` 순위 교체** — 망이 조각난 채로 남으므로 "최근접 헤드 보유
    컴포넌트"는 알람밸브 옆 헤드 2개짜리 파편을 뽑는다. `(-헤드수, 거리)` 로 정렬
    (`method="head_component_most_heads"`). 대명동 도달 헤드 2/25 → 23/25.
- **잠복 결함 1건 동시 해소(`planarize_edges`)**: 부동소수 오차로 1.25e-8mm 떨어진
  쌍둥이 노드가 남아, 그 사이 미세 간선의 양 끝이 최단경로 트리에서 **서로를 부모로
  지목**했다. 무방향 키로 접으면 1개가 되어 `force_spanning_tree` 가 서브트리를
  붙들고 있던 진짜 간선을 사이클로 오판해 잘랐다(노드 27 · 헤드 4 유실).
  좌표를 축별로 0.01mm(`_PLANAR_MERGE_MM`) 이내끼리 모아 해소. 헤드·소스 좌표는
  대표값으로 고정해 앵커는 안 움직인다.
- **R3 정직 보고**: `ExtractionAudit.fragments`
  (`count`/`attached_nodes`/`detached_nodes`/`detached_len_mm`) 신설. 안 붙은 조각은
  봉합하지 않고 센다. 통합 빌드 결과창에도 "소스 미연결 조각 N점 / Xm, 미도달 헤드
  M개" 경고 줄을 노출.
- **R4 실측(4도면, 비앵커 경로)**: 대명동 eps75 헤드 30/30 · 137.5m /
  LH306 평면도 eps130 헤드 26/26 · 77.5m / LH 지하층 eps160 헤드 30/30 · 538.0m.
- **R5 남은 느슨한 연결 2종 조이기** (사용자 지시 "둘다 조이고 커밋", 2026-07-30).
  R1~R4 로 배관↔배관 추정은 없앴지만 "멀리 있는 것이 이어진다"는 증상의 잔여 두
  경로가 남아 있었다.
  - **head drop 상한 5000 → 300mm** (`HEAD_DROP_MAX_MM` 신설). 종전 상한은 헤드
    기호가 수 m 떨어진 **남의 가지**에 얹히는 것을 허용했다. 결합선은 배관이 아니라
    "이 기호가 어느 배관에 달렸나"의 판정이므로 기호 도시 오차 수준이어야 한다.
    넘는 헤드는 붙이지 않고 `heads.unreachable` 로 보고. `HEAD_BRIDGE_MAX_MM` 는
    `_finalize_selection` 의 공간 격자 셀 크기로만 남는다(질의 상한과 무관).
    대명동 서측: 결합선 25 → 18개, **최대 2548 → 29.4mm**, 부착 헤드 23 → 16,
    미도달 2 → 9.
  - **평면화 X 교차 절단 폐지**. 끝점이 걸치지 않은 직교 교차가 티인지 층이 달라
    스쳐 지나가는 것인지 평면 좌표로는 못 가린다. 추정 티는 남의 가지에 급수를
    얹는 것과 같아 자르지 않고 `ortho.planar.unmarked_crossings` 로 센다(대명동
    서측 4곳, 결과창에 "층 미상 교차 N곳" 경고). 노드 관통 절단은 유지 —
    끝점이 얹혀 있으므로 티가 맞다.
  - 앵커 경로 대명동 서측 최종: 노드 96 · 배관 95 · 조각 68개(미연결 165노드
    27.6m) · 평면화 99477.977 → 98727.977mm(−0.8%, 관통 2 · 중복 2).
  - 불변식 완화: `test_pipeline_is_plane_embedded` 는 관통 0 · 겹침 0 만 요구하고
    교차는 감사값이 덮는지로 검사한다.
- **R6 알람밸브 결합만 완화** (사용자 지시 "알람밸브랑 가장 가까운 노드 및 엣지쪽
  연결은 좀 유하게 처리해줘(그것만)", 2026-07-30). 소스 위치는 사용자가 직접 찍은
  확정 입력이라 추정이 아니고, 여기서 어긋나면 추출이 통째로 죽는다 — 추정 연결
  폐지 원칙의 명시적 예외로 둔다. 두 가지:
  - **접속점에 배관 중간(수선발) 편입** (`_source_attach_points`). 종전에는 그래프
    *노드* 만 후보라 주배관 옆에 선 알람밸브가 먼 끝점으로 끌려갔다. 수선발이
    이기면 그 배관을 두 조각으로 쪼개 붙인다(`source_attach.attached_to =
    pipe_interior`). 쪼갠 조각이 `MIN_PIPE_EDGE_MM` 미만이 되는 수선발은 버린다 —
    그건 끝점 접속이고 노드 후보가 이미 덮는다.
  - **완화 단계(escalation=1)를 최근접 → 최대 연장으로**. B1F 실측에서 알람밸브
    0.3mm 옆의 **5.6m 스텁**이 1.6m 떨어진 **134m 주배관**을 이겨 추출이 6노드로
    끝났다. 헤드 보유 컴포넌트가 하나도 없을 때만 발동하므로 대명동(1순위에서
    결정)은 비트동일 — 노드 96 · 배관 95 · 결합선 18/29.4mm 그대로.
- **미해결(B1F) — 원인 확정**: `B1F …_도면정리_최소.dxf` 는 헤드 115개를 검출하지만
  최종 선정이 0 헤드다. 위 완화로 소스는 134m 주배관에 붙지만 **헤드는 여전히 0**.
  원인은 소스 결합이 아니라 도면 작도 규약이다:
  - 헤드 최근접 노드 거리는 중앙 **149mm** 로 전부 `HEAD_DROP_MAX_MM`(300) 안이다 —
    drop 상한과 무관.
  - 가지관 세로 런이 헤드마다 **300mm 씩 끊겨** 그려져 있다(예 x=203063.6 에서
    y=209398 ↔ y=209698). 그 틈에 헤드 기호(148.6mm 십자)가 들어간다. 끊긴 두
    끝점은 **같은 축 위 동일선상**이지만 300mm 는 `auto_snap_eps` 후보 상한
    (200mm)을 넘어 영원히 합쳐지지 않는다.
  - `_split_tee_branches` 간격 스윕(20→200mm)으로도 안 풀린다: 컴포넌트 478 → 148,
    최대망 160.7 → 207.6m 로 나아지지만 그 안의 헤드는 0 → 1 뿐. T분기 문제가
    아니라 **동일선상 300mm 단절** 문제이기 때문.
  - 남은 선택지는 "같은 축 · 동일선상 · 사이에 헤드 기호" 3조건을 모두 만족하는
    끝점쌍만 잇는 규칙(추정이 아니라 규약 해석)뿐이다. 이번 지시 범위 밖이라
    사용자 판단 대기 — 그때까지 조각 수를 정직히 보고하는 현 상태 유지(#46 참조).
- **골든**: `combined_build__plane_daemyeong` 출력이 또 바뀐다. §2·§7·§8·§10·§13 과
  같은 건이며 재생성은 사용자가 5051 에서 눈으로 확인한 뒤로 계속 보류.
- **테스트 변경**: `test_w3_anchored_fixture_excludes_other_units` 의 "최종망 노드
  x<255,000" 상한을 270,000 으로 완화했다. 추정 연결을 없앤 뒤 서측 가지 일부가
  공용 주배관(동측 우회, 실배관)을 타야만 소스에 닿는다 — 경유 노드 6개가 실제로
  존재한다. 범례(x≈288,000) 배제는 그대로 유지.

## 6. fixture 파일명 불일치 (해소됨 — 기록용)
- **항목**: §0 검증 도면
- **질문**: 지시서의 `1__입력도면_대명동_단위세대_평면도.dxf` 는 저장소에 없음.
- **임시 우회**: 불필요 — 증거로 확정. `samples/dxf/대명동201동 단위세대_layer정리.dxf` 가
  지시서 실측 근거와 정확히 일치 (L4 SPLINE 11,770개, 범례 블록 `A$C60792707`
  @ (288201,−233417) / `A$C3F157AFD` @ (288201,−234617) 동일).
