# 회랑 사슬좌표 지시서 — 시작 노드에서 «실제 길이» 를 8 방향으로 이어 붙인다

작성 2026-09-14 · 대상 저장소 `PycharmProjects/JupyterProject` · 앞 지시서 `ModuleF_아이소_평면보존_지시서.md` 의 **후속** · 그림 `docs/images/iso_plan_preserve/그림5~8.png`

---

## 0. 앞 지시서 §6 질문에 대한 오너 답 (2026-09-14)

| 질문 | 답 |
|---|---|
| §6-1 격자 스냅(≤31.8 mm 이동)을 훼손으로 볼지 | 「표시 오차」로 두지 **않는다.** 대신 회랑 망의 좌표를 만드는 방식을 이 지시서의 **사슬 규칙**으로 바꾼다. 스냅 코드 자체는 건드리지 않는다(전체망이 쓴다) — 회랑 경로에서 좌표와 길이를 **덮어쓴다** |
| §6-4 상하향식 ③ 방향 (실도면 없음) | §4-2 합성 시험으로 갈음한다 |
| §6-3 팔 없는 하향식 5개 (① 없이 ② 만) | 그대로 둔다 |

에이전트가 세지 않은 두 값 — **병합 개수**(두 board 노드가 한 kfp 노드로 눌린 수)와 **최원 경로 길이의 스냅 전/후 차** — 는 이 작업의 §2 계측에 들어간다.

---

## 1. 규칙 — 오너 확정 (2026-09-14)

### 1-1. 한 줄

**최불리 회랑(나무)의 좌표는 재지 않고 «만든다».** 시작 노드(알람밸브 = 급수원)의 좌표만 도면 값 그대로 두고, 거기서부터 배관마다 (8 방향 중 가장 가까운 방향, board 좌표에서 잰 실제 길이) 의 벡터를 **앞 배관의 끝점에 이어 붙여** 모든 노드의 좌표를 정한다. 표에 실리는 길이는 그 실제 길이다. (그림 7)

정본이 바뀐다 — 종전은 «좌표» 를 지키고 길이를 좌표에서 다시 쟀다. 이제는 **«위상 + 길이»** 를 지키고 좌표는 사슬로 만든다. 마찰손실을 정하는 것은 길이이므로 수리계산이 쓰는 값 쪽을 정본으로 삼는 것이다.

### 1-2. 범위

- 대상: **최불리 회랑** 경로만 — `expand_worst` → `select_and_expand` → 설계 표 → SDF · 아이소. 회랑은 급수원에서 뻗은 최단경로들의 합집합이라 **나무**다(고리 없음) — 이 규칙은 나무에서만 정의된다.
- **전체망 `.kfp` 경로는 종전 그대로** (`convert_to_kfp` 직접 호출 · `planar.py` 의 격자 스냅·맨해튼 길이 포함). 바이트 불변이 수용 기준이다(§4).

### 1-3. 정의

| 기호 | 뜻 |
|---|---|
| S | 시작 노드. 선택된 급수원(Z1…)의 kfp 노드. 좌표 = board 좌표 그대로(mm→m). 이것만 «진짜» 좌표 |
| 평면 배관 | board 간선에서 온 kfp 배관 — `edge_ref` 에 있는 것(노드정리로 병합된 배관도 `node_ref` 로 양 끝 board 노드를 안다) |
| 세로 토막 | `apply_vertical` 이 만든 것 — 가지 상승 · 헤드 ①②③④ · 알람밸브 ①② |
| L_e | 평면 배관의 길이 = **양 끝 board 노드 좌표의 유클리드 거리**(mm)/1000. 세로 토막은 입력값 그대로. `edge_len_mm` 선언(신축배관 접기)이 있는 배관은 표 길이만 선언값, 사슬 벡터는 board 거리(좌표 거리 ≠ 표 길이가 «정상인 부류» — 종전 규약 유지) |
| u_e | 평면 배관의 방향 = board 좌표의 방향을 **8 방향**(0·45·90·135·180·225·270·315°) 중 가장 가까운 것으로(같으면 작은 각). 세로 토막은 ±z. 상하향식 ③ 은 엔진이 정한 가지 방향을 8 방향으로 |
| 사슬 | S 에서 나무를 따라 걸으며  p(자식) = p(부모) + L_e · u_e  (부모→자식 방향으로 잰 u_e) |

오너 조건(셋째 답)을 그대로 식으로 쓰면:

    모든 헤드 h 에 대해   p(h) = p(S) + Σ_{e ∈ path(S→h)} L_e · u_e      (3차원 · 세로 토막 포함 · «정확히»)

이것이 성립하면 헤드 좌표가 평면도와 달라져도 **허용**한다. 어긋남의 상한은 Σ L_e · sin(회전각_e) — 직교 배관과 45° 배관은 회전각 0 이라 어긋남 0 이다(그림 8 ③).

### 1-4. 이 규칙이 바꾸는 것 · 안 바꾸는 것

| 바뀐다 | 안 바뀐다 |
|---|---|
| 회랑 노드 좌표 (스냅 값 → 사슬 값) | 노드·간선 집합, 연결(위상) — 앞 지시서 P2·P3 그대로 |
| 회랑 평면 배관의 `length_m` (맨해튼·스냅 → board 유클리드) | 세로 토막 ①②③④ 의 값·모양·붙는 자리 — 앞 지시서 H1·H3 그대로 (읽기 A) |
| 병합 가능성 → **0** (노드마다 부모가 하나라 좌표가 하나로 정해진다, 그림 8 ①) | 헤드 종류(`disk_kinds`) · 재질 복사 · 후렉시블 자리 |
| 앞 지시서 P1·H2(좌표 = 평면) → 이 지시서 C1·C3 로 **대체** | 아이소 식 I1 · `bake_isometric` |

부수 효과: 노드에서 만나는 배관 각도가 정확히 90°·45°·135° 가 되므로 부속(엘보·티) 판정이 깨끗해진다 — 바뀐 부속 개수는 보고만 한다.

---

## 2. 먼저 잰다 — 바꾸기 전에, 같은 프로브에 세 줄을 더한다

`scripts/_probe_plan_preserve.py` 에 추가 (대명동 K=30 무영역/유영역 · B1F 사진 조건):

    [회전각]  회랑 평면 배관 __개 · 8방향과의 편차: 0°(±0.5) __개 · ≤5° __개 · ≤10° __개 · ≤22.5° __개 · 최대 __° (배관 목록 상위 10)
              → 예상 헤드 이탈 상한 Σ L·sin(편차) 를 헤드마다 · 최대 __mm
    [병합]    G_K 노드 수 __ · 대응된 «서로 다른» kfp 노드 수 __ · 차 = 병합 __개 (그중 헤드에 먹힌 것 __)
    [최원길이] 손질 far_m(board 거리) __m · 표의 최원 경로 평면 길이 합(스냅·맨해튼) __m · 차 __m
    [나무]    회랑 |V| · |E| · 연결성분 수 — |E| = |V| − 1 이고 성분 1 인가

**멈추는 조건**: 회랑이 나무가 아니거나(고리·분리) 병합이 0 이 아니면 §3 로 가지 않고 보고한다. 나머지는 수치를 보고에 싣고 §3 로 간다.

---

## 3. 만든다 — 회랑 경로에 «사슬 좌표» 한 단계

### 3-1. 자리

`restrict.py` 의 `expand_worst` 안, **`apply_vertical` 다음 · `build_design_tables` 전**에 한 단계를 둔다. 이유: `convert_to_kfp` 는 접속표시(호)를 좌표로 노드에 앉히고(`sit_arcs`) 메인을 따라간다(`walk_main`) — 그 앞에서 좌표를 바꾸면 호가 엉뚱한 노드에 앉는다. 세로 처리가 끝난 뒤의 kfp 는 나무이고, 그 위에서 좌표만 다시 만든다.

전체망 경로는 `expand_worst` 를 타지 않으므로 자동으로 밖이다. **`planar.py` · `engine.py` 는 손대지 않는다.**

### 3-2. 단계의 일

1. 뿌리 = S 의 kfp 노드. 좌표는 지금 값(board 좌표 그대로)을 둔다.
2. kfp 배관을 분류한다 — `edge_ref` 에 있으면 평면 배관(양 끝 board 노드 = `node_ref`), 아니면 세로 토막(z 차 있음) 또는 엔진이 만든 가로 토막(상하향식 ③).
3. S 에서 BFS. 배관 (부모 → 자식) 마다  L_e · u_e 를 1-3 대로 정하고  p(자식) = p(부모) + L_e · u_e. 좌표는 **반올림하지 않는다**(float 그대로 — 반올림은 출력에서).
4. 평면 배관의 `length_m` 을 L_e(board 유클리드)로 덮어쓴다. `declared_pipes` 는 선언값을 둔다. 세로 토막은 그대로.
5. 회전각·이탈을 노드별로 기록해 응답에 싣는다(`chain_report`): 배관별 편차, 헤드별 |p(h) − p_board(h)|, 최대값. 화면은 이 값을 «참고» 로 보인다 — 오너가 허용한 어긋남이므로 경고가 아니다.
   이탈은 **같은 좌표계**에서 잰다 — board 점에 `xform` 의 평행이동(minx·miny·+1.0)만 적용하고 스냅은 하지 않은 값과 견준다. S 자체의 스냅 이동(≤35 mm)은 사슬 전체의 평행이동이라 길이·상대 위치에 무관하지만, 그 값도 보고에 적는다.
6. 단계 끝에서 §4 의 C1~C4 를 그 자리에서 검사하고, 깨지면 조용히 넘기지 않는다(응답에 위반 목록).

`tree_loads` · `edge_ref` · `node_ref` 는 위상이 안 바뀌므로 그대로 유효하다. `normalize_node_coords`(출력용 닮음변환) · `bake_isometric` 은 손대지 않는다 — 닮음변환은 C1 을 배율만큼 보존한다.

---

## 4. 검증

### 4-1. 판정식 (앞 지시서 P1·H2 를 대체)

| | 판정 | 허용 |
|---|---|---|
| C1 사슬 닫힘 | 회랑의 **모든** 노드 v: p(v) = p(S) + Σ_{path} L_e·u_e (3차원). 헤드 K개 전부 포함 | ≤ 1 mm |
| C2 길이 | 평면 배관 `length_m` = board 유클리드 거리. 세로 토막 = 입력값. 선언 배관 = 선언값. 그리고 모든 배관(선언 제외): `length_m` = |p(end) − p(start)| | ≤ 1 mm |
| C3 1:1 | G_K 노드 → kfp 노드가 **단사**(서로 다른 노드는 서로 다른 노드로). G_K 간선 → 사슬 유지(세분·직선 병합만). 미분류 노드 0 | 병합 0 · 끊김 0 |
| C4 방향 | 평면 배관 방향 ∈ 8 방향. 회전각 ≤ 22.5° (구성상 당연 — 넘으면 버그) | — |
| H1·H3 | 앞 지시서 그대로 — 세로 나무의 자리·길이·재질·후렉시블 | — |
| I1 | 아이소 식 그대로 | — |
| W | 표의 최원 경로 **평면 부분** 길이 합 = 손질 `far_m` | ≤ 1 mm |

W 가 이 작업의 «원래 목적» 이다 — 손질이 고른 최원 거리와 표가 계산에 쓰는 거리가 같은 수가 된다.

### 4-2. 수용 기준

| | 기준 |
|---|---|
| 1 | 대명동 K=30(무영역·유영역) · B1F 사진 조건: C1·C2·C3·C4·W 전부 통과 |
| 2 | 헤드 이탈(|p(h) − p_board(h)|) 최대값 보고 — 판정 아님, 회전각 실측과 부합하는지만 확인 |
| 3 | **전체망 `.kfp` 바이트 불변** · 손질 화면 · 최불리 선정 결과 불변 |
| 4 | `planar.py` · `engine.py` · `sdf_post.py` · `worst.py` · `api_edit.py` diff 0 |
| 5 | 기존 회귀 전부 통과 · 골든 재생성 없음 |
| 6 | 화면: 아이소를 켜면 헤드 K개가 사슬 좌표에 서 있고, 밑그림과의 어긋남이 `chain_report` 의 이탈값과 같다 |

### 4-3. 시험 `tests/test_chain_coords.py`

- 합성 나무 (그림 7 의 런 + 티 하나 + 헤드 4 = 상향식 2 · 하향식 1(팔 있음) · 상하향식 1). 배관 각도를 **3° · 87° · −2° · 40°** 로 두어 8 방향 회전이 실제로 일어나게 한다. C1~C4·W 를 식으로 검사. 예상값: 길이 3000·2200·2600·1600 그대로, 끝점 이탈 = 해석값(그림 7 의 208 mm 와 같은 식).
- 팔 없는 하향식 1개(① 없이 ②) · 선언 길이 배관 1개(표 길이 ≠ 좌표 거리가 정상) · 직교만인 망 1개(이탈 0 이어야 함).
- 고리가 있는 입력을 넣으면 §2 의 멈춤이 작동하는지.
- 실도면(대명동 K=30)은 판정식 0 건으로 고정 — 골든 아님.

---

## 5. 금지

- `planar.py`(격자 스냅·맨해튼 길이 포함) · `engine.py`(①②③④) · `sdf_post.py` · `worst.py` · `api_edit.py` 를 바꾸지 말 것. 전체망이 그 코드를 쓴다.
- 전체망 `.kfp` 경로에 사슬 단계를 끼우지 말 것 (오너: 종전대로).
- 회랑이 나무가 아닐 때 임의로 간선을 끊어 나무로 만들지 말 것 — 멈추고 묻는다.
- 회전각이 큰 배관을 «보기 좋게» 다른 방향으로 돌리거나 길이를 조정하지 말 것 — 가장 가까운 8 방향, 길이는 board 거리, 그뿐이다.
- 손질 · 최불리 선정 · 헤드 종류 판정에 되먹이지 말 것.
- 리팩터링 금지 · 골든 재생성 금지.

## 6. 애매하면 멈추고 묻는다

1. 회랑에 고리가 있다(|E| ≠ |V| − 1) — 어느 도면·어느 자리인지와 함께
2. 병합이 0 이 아니다 — 어느 board 노드 둘이 어느 kfp 노드로 눌렸는지
3. 회랑 배관 중 8 방향 편차가 10° 를 넘는 것이 있다 — 목록과 예상 이탈(오너가 허용했지만 크기를 보고 정할 수 있게)
4. 선언 길이 배관(`edge_len_mm`)이 회랑에 있는데 사슬 벡터(board 거리)와 표 길이(선언값)가 다르다 — 그 개수와 차이 (이 지시서는 종전 규약대로 두었다; 오너가 바꾸고 싶으면 그때)
5. 8 방향이 아니라 **4 방향(수직·수평만)** 이 오너의 뜻이었는지 — 오너 답의 「수직 혹은 수평 벡터」를 이 지시서는 «세로 토막(z) 과 평면 배관(8 방향)» 으로 읽었다. 다르면 여기서 멈춘다

## 7. 보고 형식

    [계측]   §2 표 — 도면별 회전각 분포 · 병합 · 최원길이 차 · 나무 여부
    [단계]   사슬 단계가 들어간 자리(파일·함수) · 분류 규칙(평면/세로/③) 실제 적용 결과 개수
    [판정]   C1~C4 · H1·H3 · I1 · W — 도면별 통과/위반과 최대 오차
    [이탈]   헤드별 |p(h) − p_board(h)| 최대·분포 (참고)
    [불변]   전체망 .kfp 바이트 비교 · 금지 파일 diff 0
    [시험]   신규 __건 · 회귀 __건 통과
    [묻는 것] §6 해당 항목과 수치
