# -*- coding: utf-8 -*-
"""수리계산 입력(SDF) 생성 — 최불리 확정 → 위상 정리 → 테이블 → 방출.

화면 없음(Qt import 금지). 지시서 §1 의 계약을 따른다.

왜 별도 계통인가: `.kfp` 는 K-Fire Solver 가 전체망을 받아 설계구역·최불리를
**제 안에서** 고른다. `.sdf` 는 PIPENET 이 주어진 망을 풀 뿐이라, 설계구역을
프로그램이 미리 확정해 주지 않으면 그것은 수리계산 입력이 아니라 «전체망을
PIPENET 문법으로 옮겨 적은 파일» 이다.
"""
