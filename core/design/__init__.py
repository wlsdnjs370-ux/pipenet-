# -*- coding: utf-8 -*-
"""모듈 C 설계 자동화 — 건축도면에서 스프링클러 배관망을 순방향으로 생성한다.

모듈 A(완성 소방도면 → 배관망 그래프, 역방향)와 **같은 자료구조**를 산출해야
하며 그 계약은 `docs/module_a_graph_schema.md` 에 고정되어 있다.

하위 패키지는 성격이 다르고 섞으면 안 된다:

- `deterministic/` — 규범(NFTC)과 기하로만 결정된다. 임계값·확률·휴리스틱 금지.
  단위 테스트로 "통과/실패"가 나오고, 한 번 맞으면 도면이 바뀌어도 안 깨진다.
- `recognize/`     — 도면에서 벽·실·개구부를 읽는 인식 셸. 통계로 평가하고
  결과에는 항상 `confidence` 와 `provenance` 를 붙인다. 도면이 바뀌면 깨진다.
- `checks/`        — 모델이 건물과 대응하는지 보는 차원 무결성 검사.

둘을 한 덩어리로 뭉치면 전체 신뢰도가 인식 셸 수준으로 하향 평준화된다.
"""
