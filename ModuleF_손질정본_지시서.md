# 손질이 정본이다 — 수리계산은 받아 쓰기만 한다

작성 2026-09-13 · 대상 저장소 `PycharmProjects/JupyterProject`

---

## 0. 계약 — 이 문서의 모든 조치는 이 한 방향이다

    ①  프로그램이 배관망을 자동으로 정의한다        (찍기 · 이음 규칙)
    ②  사람이 손질에서 더 정의한다                  (이음 · 삭제 · 헤드 종류 · 급수원 · 알람밸브)
    ③  그 배관망 위에서 알람밸브를 잡고 영역을 지정해 기준개수 K개를 뽑는다
    ④  수리계산은 ③이 정한 헤드와 배관망을 **그대로** 받는다

**정본은 ②·③의 결과다. 수리계산은 받아 쓰는 쪽이고, 스스로 다시 고르지 않는다.**

### ★절대 금지 — 되먹임

> **수리계산의 결과를 손질·평면 쪽으로 되돌려 쓰는 변경을 하지 않는다.**
>
> - `/design/build` 에서 `sess["worst"]` 를 **쓰지 않는다**(읽기만 한다).
> - 평면 보기(`drawEdit`)가 그리는 것을 설계 표에서 가져오게 **하지 않는다**.
> - `_worst_view` · `views.py` · `remote30.py` 의 «무엇을 그리나» 를 바꾸지 않는다.
>
> 전에 이 방향으로 고쳐서 되돌린 적이 있다. 평면이 수리계산을 따라가면 사람이
> 정의한 것이 무엇이었는지 알 길이 없어진다 — 그 순간 ②가 의미를 잃는다.

---

## 1. 지금 왜 못 받나 — 자가 두 벌이고, 거르는 자리가 거꾸로다

### 1-1. 「헤드가 배관에 붙었나」를 두 함수가 **다른 자**로 판정한다

| | 함수 | 자 |
|---|---|---|
| 느슨 | `pipeline/flow.py` `head_nodes()` | 중심 ≤5 mm **또는 테두리 ±50 mm — 차수 무관** |
| 엄격 | `pipeline/flow.py` `attach_heads_center()` | 중심 ≤5 mm **또는 테두리에서 «선이 끝나는» 차수 1 노드** |

손질 board 는 **둘 다** 들고 있다.

    edit/board.py:313  _complete_heads()  → self.head_centers   (엄격)
    edit/board.py:320  _head_nodes()      → self.hnodes         (느슨)

그런데 **최불리 선정은 느슨한 자를 쓴다**:

    routes/module_f/api_edit.py:398
    w = _worst_k_heads(b.pts, b.edges, b.hnodes, b.sources, k=k, only_heads=only, ...)
                                    ^^^^^^^^ 느슨

관이 헤드 원을 **스쳐 지나가기만** 하는 헤드(사유 `pass_under`)는 느슨한 자로는
젖었고 엄격한 자로는 안 붙는다. 그래서 손질이 고른 K개 안에 «전개가 못 붙이는
헤드»가 섞인다.

### 1-2. 그것을 거르는 자리가 수리계산에 있다 — 순서가 거꾸로다

`routes/module_f/attach.py` 의 머리말이 이미 그렇게 적어 두었다.

    손질     최불리 K개를 고른다            ← board 도달로만 센다
    수리계산 cand = 고른 것 ∩ 붙는 헤드     ← 여기서 처음 걸러진다
             worst_k_heads(k=K, only=cand)  ← 그래서 K 보다 적게 나온다

    「손질이 고를 때 이미 «붙는 헤드» 만 후보로 삼았다면 10개를 골랐을 것이고
      그대로 표에 왔을 것이다」

**진단은 이미 코드에 적혀 있고, 고친 자리만 틀렸다.** 거르기를 수리계산에 두었기
때문에 수리계산이 선정을 다시 해야 했고(`filled` 백필), 그래서 두 화면이 갈렸다.

### 1-3. 그 결과 지금 벌어지는 일

    손질이 고른 K개  ─(∩ 붙는 헤드)→  K보다 적음  ─(백필)→  다른 헤드로 채움
                                                              └→ 표에 «사람이 안 고른» 헤드가 든다

실측(사용자 화면 · K=30): 두 화면의 헤드 30개 중 **27개만 같고 4개가 바꿔치기**됐다.

---

## 2. 조치 — 거르기를 **손질로 옮긴다**

고칠 자리는 `routes/module_f/api_edit.py` 의 `/edit/worst` 한 곳이 중심이다.

### 2-1. 최불리를 고르기 **전에** 후보를 「표에 실제로 오는 헤드」로 좁힌다

`_worst_k_heads` 를 부르기 직전(`api_edit.py:398` 바로 위)에 넣는다.

    from routes.module_f.attach import wet_heads

    probe = wet_heads(sess, es, selected_source=picked_tag)
    drop_reason = {}
    if probe.get("ok"):
        # 「표에 노즐로 오는 헤드」 = 전개가 붙일 수 있고(wet),
        #   다른 헤드와 같은 중심 노드를 나눠 물지 않은 것(shared 제외).
        keep = set(probe.get("wet") or ()) - set(probe.get("shared") or ())
        before = set(range(len(b.disks))) if only is None else set(only)
        only = before & keep
        drop_reason = {i: (probe.get("reason") or {}).get(i, "unknown")
                       for i in sorted(before - keep)}
        if not only:
            return None, _wfail(
                "이 영역 안에 «배관에 붙은» 헤드가 하나도 없습니다 — "
                "손질에서 그 헤드의 배관을 먼저 이어 주세요.")

`probe` 는 `attach.wet_heads` 가 판 지문으로 캐시한다. **없으면 여기서 한 번
재고, 수리계산은 그것을 그대로 받아 쓴다** — 전체 비용은 종전과 같다(지금은
수리계산이 내던 값을 손질이 먼저 낼 뿐이다).

★`attachable_heads`(`design/restrict.py`)가 `shared` 를 아직 안 내면 더한다.
  `build_planar_graph` 가 이미 `shared_head_idx` 로 갖고 있으므로 **판정을 새로
  만들지 말고 그대로 들고 나온다** — `reason` 을 더할 때와 같은 방식이다.

### 2-2. 뺀 헤드를 **손질 화면에서 그 자리에 보인다**

후보에서 빠진 헤드는 사람이 고칠 대상이다. 응답에 실어 화면이 표시하게 한다.

    "not_attachable": {
        "n": len(drop_reason),
        "items": [{"disk": i, "xy": [x, y], "why": why} …],
        "by_why": {"pass_under": n, "chord_only": n, "no_center": n, "dry": n, "shared": n}
    }

사유별로 사람이 할 일이 다르다 — 한 덩어리로 「배관을 이어라」라고 하면 틀린 곳을
고치러 간다.

| 사유 | 뜻 | 사람이 할 일 |
|---|---|---|
| `pass_under` | 관이 헤드를 스쳐 지나간다(끝점이 아니다) | 손질 「이음」으로 그 관과 헤드를 잇는다 |
| `chord_only` | 원에 걸친 것이 문양(가로막대)이라 팔이 아니다 | 찍기에서 그 묶음을 헤드에서 뺀다 |
| `no_center` | 헤드 둘레에 배관이 아예 없다 | 가지관을 잇는다 |
| `dry` | 급수원에서 물이 안 닿는다 | 상류 이음을 잇는다 |
| `shared` | 다른 헤드와 같은 자리라 하나로 합쳐진다 | 찍기에서 겹친 묶음 하나를 뺀다 |

화면 표시는 이미 있는 규약을 쓴다 — 손질 단계의 마크 체계와 같은 색·같은
켜고끄기다. **새 화면을 만들지 않는다.**

### 2-3. K 미달 게이트가 **옳은 수로** 작동하게 둔다

`api_edit.py:412` 의 `if w["reachable"] < k:` 는 손대지 않는다. 2-1 로 후보가
좁아지면 `reachable` 이 저절로 「표에 오는 헤드 수」가 되어, 영역 안에 K개가
안 되면 **그 자리에서 막고 이유를 말한다.** 지금은 느슨한 자라 통과한 뒤
수리계산이 몰래 채우고 있었다.

### 2-4. `/design/build` 는 아무것도 «고르지» 않는다

    · `only = set(picked)` 를 **그대로** `select_and_expand` 에 넘긴다.
    · `probe` 와의 교집합(`only & wet`)은 이제 항상 `only` 와 같아야 한다.
      같지 않으면 **조용히 채우지 말고 멈추고 보고한다** — 손질과 수리계산
      사이에서 판이 바뀌었다는 뜻이고, 그건 고칠 결함이지 메울 구멍이 아니다.
    · 백필 블록(`filled` · `zone_confined_pool`)은 **지우지 말고 안전망으로
      남긴다.** 다만 정상 흐름에서는 `filled == 0` 이어야 하고, 0이 아니면
      그 사실을 로그와 응답에 올린다.
    · `sess["worst"]` 에 **쓰지 않는다**(§0 금지).

---

## 3. 배관망도 그대로 왔는가 — 위상 불변량으로 잰다

헤드가 같아도 배관이 달라질 수 있다. 손질 corridor 와 설계 표를 **불변량**으로
맞댄다. 좌표가 아니라 «모양의 성질»로 재야 격자 스냅·노드정리 같은 정당한
변형과 진짜 훼손이 갈린다.

    scripts/_probe_carryover.py  (신규)

| 불변량 | 손질 corridor | 설계 표 | 같아야 하나 |
|---|---|---|---|
| 헤드 수 | K | 노즐 수 | **같다** |
| 연결 덩어리 `b0` | 1 | 1 | **같다** |
| 독립 고리 `b1 = E − V + b0` | n | n | **같다** |
| 말단(차수 1) 수 | n | n | **같다** |
| 티(차수 3) · 크로스(차수 4) 수 | n | n | **같다** |
| 노드 수 `V` · 간선 수 `E` | n | n | 다를 수 있다 (일직선 중간 노드 병합·세로 구간 추가) |
| 총 연장 | m | m | 세로 구간(헤드 접속관·가지 상승)만큼만 늘어난다 |

말단 수는 `말단 = 2 + 티 + 2·크로스 − 2·b1` 로도 검산된다 — 이 식이 양쪽에서
성립하면 세는 과정 자체가 옳다.

**다르면 어느 단계에서 달라졌는지 그 단계가 말하게 한다.** `build_planar_graph`
안의 다섯 자리에서 전후 불변량을 찍는다(판정은 바꾸지 않는다):

    attach_heads_center → wet_from_sources → prune_dead_pipes
    → 격자 스냅 remap(50 mm) → cleanup_collinear_intermediate_nodes

특히 **격자 스냅 remap** 은 서로 다른 board 노드를 한 칸으로 접을 수 있다.
접힌 것이 헤드이거나 corridor 분기점이면 **세어서 보고한다**(지금은 조용하다).

---

## 4. 계측 — 조치 전/후 같은 표

같은 도면·같은 영역·같은 K 로 한 바퀴 돌려 잰다.

| 항목 | 조치 전 | 조치 후 (기대) |
|---|---|---|
| 손질이 고른 헤드 수 | K | K |
| 그중 전개가 못 붙이는 것 | **n > 0** | **0** |
| 백필(`filled`) | n | **0** |
| 표 노즐 수 | K | K |
| 손질 헤드 ↔ 표 노즐 좌표 1:1 | K−n | **K / K** |
| 손질 후보에서 뺀 헤드 | 안 보임 | 개수 · 자리 · 사유별로 화면에 |
| §3 불변량 5종 일치 | 미측정 | **전부 일치** |
| 최불리 버튼 응답 시간 | 빠름 | 느려진다(전개 1회 · B1F 실측 117초) |

★마지막 줄은 **의도된 대가**다. 그 비용은 지금 수리계산이 내고 있던 것을 앞으로
  옮긴 것뿐이라 총합은 같다. 「느려졌으니 캐시를 빼자」로 가지 말 것 —
  대신 진행 표시를 단다(무엇을 하는 중인지 문장으로).

---

## 5. 수용 기준

| | 기준 |
|---|---|
| 1 | 손질이 고른 K개가 **전부** 표에 노즐로 온다 (좌표 1:1 · 6~19 mm 안) |
| 2 | 백필이 한 번도 일어나지 않는다 (`filled == 0`) |
| 3 | 영역 안에 붙는 헤드가 K개 미만이면 **손질에서** 막고 이유를 말한다 |
| 4 | 후보에서 뺀 헤드가 자리·사유와 함께 손질 화면에 보인다 |
| 5 | §3 불변량 5종(헤드·b0·b1·말단·티/크로스)이 손질 corridor 와 표에서 같다 |
| 6 | `sess["worst"]` 는 `/edit/worst` 에서만 쓰인다 (되먹임 없음) |
| 7 | 평면 보기 코드(`drawEdit`·`_worst_view`·`views.py`) **무변경** |
| 8 | 기존 시험 전부 통과 |

**이 여덟이 서면 멈춘다. 더 고치지 않는다.**

---

## 6. 금지 사항

- **되먹임 금지** — §0. 수리계산 결과를 손질·평면으로 돌려보내지 않는다.
- **판정 규칙 변경 금지** — `head_nodes` · `attach_heads_center` 의 «붙었나» 판정을
  바꾸지 않는다. 이 지시서는 **어느 자를 쓰느냐**를 바꾸는 것이지 자 자체를
  깎는 것이 아니다. (스쳐 지나가는 헤드를 자동으로 이어 붙이는 것은 별건이고,
  하려면 사람 판단을 먼저 받는다 — 없는 배관을 그리는 쪽으로 갈 수 있다.)
- **백필 삭제 금지** — 안전망으로 남긴다. 다만 정상 흐름에서 0이어야 한다.
- **좌표 변환·아이소 손대지 말 것** — 실측으로 기하는 무결하다(27쌍이 회전 없는
  한 자로 맞았다).
- 골든 재생성 금지 · 리팩터링 금지 · 새 화면 만들기 금지.

---

## 7. 보고 형식

    [후보]     영역 안 헤드 __개 → 붙는 헤드 __개 (뺀 것 __개)
    [사유]     pass_under __ · chord_only __ · no_center __ · dry __ · shared __
    [선정]     K=__ · 손질이 고른 __개 · 표 노즐 __개 · 좌표 1:1 __/__
    [백필]     filled=__ (0이어야 한다 · 0이 아니면 왜)
    [불변량]   헤드 __/__ · b0 __/__ · b1 __/__ · 말단 __/__ · 티·크로스 __/__
    [단계]     불변량이 달라진 단계와 그 수 (있으면)
    [시간]     최불리 버튼 __초 (전개 1회 포함 여부)
    [시험]     신규 __건 · 회귀 __건 통과
    [막힌 것]  못 고친 것 · 이유 · 필요한 판단

**§2-1 을 넣었는데도 §5 의 1·2 가 안 서면 그 자리에서 멈추고 보고한다** —
남은 차이는 선정이 아니라 그 뒤 단계(격자 스냅·노드정리·세로 전개)에서 오는
것이고, 원인이 하나 더 있다는 뜻이다. 찾아 헤매지 말고 숫자만 들고 온다.
