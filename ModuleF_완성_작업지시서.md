# Module F 완성 작업 지시서 — 웹 워크벤치를 완성품으로

당신은 FNCADnet 저장소의 유지보수 엔지니어다. 아래를 **항목 순서대로, 항목당 커밋 1개씩**
수행하라. 각 항목은 수용 기준을 통과한 뒤에만 다음으로 진행한다. 판단이 필요한 애매점은
임의로 구현하지 말고 `BLOCKED.md`(저장소 루트)에 기록하고 다음 항목으로 넘어간다(§6).

작업 대상: **`routes/module_f/`** · **`templates/module_f.html`** · **`cad_project_editor_g/`**(엔진).
읽기 전용 참조(수정 금지): `cad_project_editor/`(모듈 E — 이 지시서로 **동결**된다),
`remote30_prototype.py`, `pipenet_converter/`, `routes/r30_*.py`.

---

## 0. 이 작업의 정체 (구현 전 필독)

### 0.1 왜 F 가 완성품인가

- 모듈 A 는 「도면이 이미 맞다」를 전제해, 지저분한 실측 도면 앞에서 판단을 사람에게
  물을 방법이 없다.
- 모듈 E/G 는 판단은 옳지만 Qt 창이 서버 PC 화면에 묶여 원격 사용이 불가능하다.
- **모듈 F 만 로그인 게이트 뒤 브라우저다.** 완성품 F 의 정의:
  *브라우저 하나로 도면 열기 → 사람 확정 → 최불리 → kfp/sdf 까지, E 의 판단 철학과
  A 의 증거 추출과 G 의 수리계산 입력 엔진을 모두 태운 것.*

### 0.2 결정적 사실 — G 의 `design/` 은 Qt 의존이 0 이다

`cad_project_editor_g/services/cad_import/design/`(worst · restrict · bore · fitting ·
tables · emit · sdf_post)은 전부 헤드리스로 지어졌고 실측으로 확인했다
(`grep PySide6` 0건). **G1~G18 은 사실상 F 를 위한 엔진 R&D 였다.** 이 지시서에서
새 계산 로직은 쓰지 않는다 — 웹으로 노출하고, 화면을 만들고, 흐름을 잇는 것이 전부다.

### 0.3 확정된 설계 결정 (변경 금지)

| # | 항목 | 결정 |
|---|---|---|
| D1 | 엔진 소유권 | **F 가 무는 트리를 `cad_project_editor_g` 로 재지정.** E 는 동결 레퍼런스로 은퇴. 세 번째 사본은 만들지 않는다 |
| D2 | 인식 주입 | A 의 `detect_heads`(R1~R5·신뢰도)를 찍기 단계의 **후보 제안**으로만. 확정은 항상 사람 — E 의 「표시가 없으면 추측하지 않는다」 철학은 게이트로 유지 |
| D3 | 산출 구성 | **3종 체크**: 전체망 `.kfp` / 최불리 `.kfp` / 최불리 `.sdf`(+`.slf`). 기존 문법 재직렬화 SDF(전체망 `_emit_pipenet`)는 은퇴 |
| D4 | 급수원 복수 | **사람이 1곳 지정.** kfp 변환의 `source_selection_required` 와 같은 규약으로 통일. G 의 BLOCKED **B2 는 이것으로 해소** |

### 0.4 순서의 원칙

수치를 바꾸는 작업은 F-1(급수원 지정) **하나뿐**이다. 그래서 F-1 을 앞에 두고, 그
이후의 모든 회귀 기준선을 새 수치로 고정한다. **F-1 이전의 어떤 산출물도 이후 회귀
기준선으로 쓰지 말 것.**

---

## 1. 신규 계약

```
routes/module_f/
  api_design.py     [신설] 수리계산 입력 라우트 — tables / preview / emit
  api_pick.py       [수정] /pick/suggest 추가 (D2)
  api_convert.py    [수정] 산출 3종 (D3)
  common.py         [수정] EDITOR_ROOT 재지정 (D1)
templates/module_f.html   [수정] 수리계산 패널 + 찍기 후보 레이어
```

새 엔드포인트(엔드포인트명 규약은 기존 `/api/module-f/*` 그대로):

```
POST /api/module-f/pick/suggest        찍기 후보 제안 (detect_heads)
POST /api/module-f/design/build        최불리 → 제한 전개 → 5표 (메모리)
GET  /api/module-f/design/preview      정규화+베이크 좌표 payload (저장 좌표 그대로)
POST /api/module-f/design/emit         .sdf + .slf 저장 → 내려받기 링크
```

엔진 함수는 **전부 G 의 `design/` 을 그대로 import** 한다. 여기서 엔진 로직을 고쳐야
할 일이 생기면 그 커밋은 G 의 테스트(`cad_project_editor_g/tests/`)를 함께 통과해야
한다 — F 를 위해 G 를 몰래 바꾸는 것을 금지한다.

---

## 2. 작업 항목 (순서 고정)

### F-0. 엔진 재지정 (D1)

`routes/module_f/common.py:16` 한 줄이 본체다:

```python
EDITOR_ROOT = Path(__file__).resolve().parents[2] / "cad_project_editor_g"
```

함께 따라오는 것들을 놓치지 말 것.

1. `IMPORT_WORK_ROOT = EDITOR_ROOT / "docs" / "import"` 가 자동으로 G 트리를 가리키게
   된다. **G 트리에는 `docs/` 가 아직 없다**(트리 diff 실측) — `_boot()` 의
   `os.makedirs` 가 만드는지 확인하고, 안 만들면 만들게 한다.
2. 결과적으로 **F(웹)와 G(데스크톱)가 같은 작업폴더를 공유**하게 된다. 이것은 의도된
   동작이다(종전에 F↔E 가 공유하던 것과 같은 구도). 대신 **E 와는 이제 완전히
   분리**된다.
3. 기존 F 사용자가 E 작업폴더(`cad_project_editor/docs/import`)에 쌓아 둔 찍은스펙·
   손질본은 재지정 후 보이지 않는다. **1회 복사 스크립트**(`scripts/_migrate_f_workdir.py`)
   로 G 작업폴더에 복사한다(원본은 보존 — E 는 동결이지 삭제가 아니다).
4. 같은 웹 프로세스의 `sys.path` 에 **옛 E 루트가 절대 올라가지 않게** 한다. 두 트리의
   `services`/`domain` 패키지 이름이 같아서, 둘 다 올라가면 어느 쪽이 import 되는지가
   순서 우연에 걸린다.

**수용 기준**
- 브라우저에서 열기 → 찍기 → 손질 → 변환 → `.kfp` 저장까지 무오류 완주(B1F 실측).
- 재지정 전 E 작업폴더에 있던 키로 `reopen` 이 성공한다(마이그레이션 증명).
- E 데스크톱과 F 를 동시에 띄워 각자 도면을 열어도 캐시·찍은스펙이 섞이지 않는다.
- `import services` 의 `__file__` 이 G 트리를 가리킨다(스모크 테스트로 고정).

### F-1. 급수원 지정 규약 통일 (D4 · G-BLOCKED B2 해소) ★수치가 바뀌는 유일한 항목

`design/worst.py:worst_k_heads` 에 keyword 인자 `source_index: int | None = None` 를
추가한다. 지정되면 그 급수원 **하나만** seed 로 Dijkstra 를 돌고, `None` 이면 종전
동작(전부 seed — 하위호환·G1 일치 조건 보존).

- F 의 `/api/module-f/edit/worst` 는 급수원이 **2곳 이상이면 `source_index` 를
  요구**한다 — kfp 변환의 `source_selection_required` 와 같은 방식으로 화면에
  후보(Z1, Z2…)를 돌려주고 사람이 고른다. 1곳이면 자동으로 그것.
- 손질 화면의 급수원 표시에 선택 상태를 드러낸다(어느 급수원 기준의 최불리인지).
- 이 커밋에서 **새 회귀 기준선을 기록**한다: B1F · 지정 급수원 Z1 기준의
  앵커 좌표·최원 유하거리·설계면적 폭·corridor 총연장·max_load.
  `cad_project_editor_g/tests/` 의 G1 대조 테스트도 같은 규약으로 갱신한다
  (같은 엔진이므로 F↔G 일치는 정의상 성립하지만, 테스트 문구가 옛 동작을
  전제하면 안 된다).

**수용 기준**
- 급수원 2곳인 B1F 저장본에서 Z1 지정과 Z2 지정의 앵커·최원 유하거리가 서로 다르게
  나오고, 각각 재실행 시 재현된다.
- 미지정 호출은 400 과 후보 목록으로 막힌다(2곳 이상일 때).
- `source_index=None` 경로의 산출이 변경 전과 **비트 동일**(하위호환 증명).
- G 의 `BLOCKED.md` B2 항목에 「D4 로 해소 — F-1 구현」을 추기한다.

### F-2. `design/` HTTP 노출

`routes/module_f/api_design.py` 신설. 세션의 `edit`(손질) + `worst`(최불리) 상태를
읽어 G 의 함수를 **그대로** 부른다:

```
restrict_to_worst → (제한 전개: convert 엔진의 메모리 진입점) → build_design_tables
→ normalize_node_coords → [iso 켜짐이면 bake_isometric(헤드 수직 포함)] → emit_design_sdf
```

- `design/build` 는 파일을 쓰지 않는다 — 표·요약(앵커, 유하거리, 폭, 총연장, max_load,
  관경 근거 집계, 부속 집계, 제외 헤드 수)을 세션에 캐시하고 JSON 으로 돌려준다.
- `design/preview` 는 **emit 에 넘길 좌표 그대로** 를 돌려준다(G16 원칙). 미리보기
  전용 좌표계를 따로 만들지 말 것 — 화면과 파일이 달라지면 미리보기가 무의미하다.
- `design/emit` 은 `UPLOAD_DIR/module_f/` 아래에 `.sdf`+`.slf` 한 쌍을 쓰고
  `/api/module-f/download` 규약으로 내려받게 한다. 자산 없으면 **실패**(G 정책 그대로).
- 설정 7종(K · 배관 규격 기본값 · iso on/off · 고도 펼침 배율 · 캔버스 크기 ·
  lift 영점 · 헤드 스텁 길이)은 요청 body 로 받고 세션에 기억한다.

**수용 기준**
- 같은 손질 저장본·같은 설정으로 G 데스크톱(4번째 창)과 F 웹이 만든 `.sdf` 가
  **완전히 동일**하다(같은 엔진의 증명 — diff 0).
- K 를 바꾸면 build 만 다시 돌고, iso 설정만 바꾸면 최불리 재계산 없이 preview 만
  갱신된다(캐시 동작).
- 무거운 단계는 기존 `_run_job` 잡 규약(세션당 하나)을 태운다.

### F-3. 브라우저 수리계산 패널

`templates/module_f.html` 에 변환 다음 단계로 「수리계산 입력」 패널을 붙인다.
G16 의 웹판이며 배치도 같다: 좌측 설정+요약, 우측 탭(아이소매트릭 캔버스 / 표 4종).

- 캔버스는 F 의 기존 도면 캔버스 인프라를 재사용한다. 배관 굵기 = 담당 헤드 수,
  헤드 △/▽(상향/하향), 급수원·알람밸브 별도 표시, 휠 줌·드래그 팬·화면 맞추기.
- 표 4종(노드·배관·노즐·부속)은 저장될 값 그대로 + 배관 표에 관경 근거 열
  (도면 텍스트 / 별표1 보강 / 별표1 폴백).
- 표 행 선택 → 캔버스 강조(가능하면).
- 「표시 전용 — 수리계산 결과는 바뀌지 않습니다」 문구를 보기 설정 옆에 명시.

**수용 기준**
- 저장 없이 아이소매트릭 형태와 표 값을 확인할 수 있다.
- 미리보기에 그린 좌표와 저장된 SDF 의 `<Position>` 이 일치한다(자동 테스트).
- 고도 펼침 배율 변경 시 도면만 바뀌고 표 값은 불변.
- B1F 규모(노드 62·배관 61)에서 설정 변경 후 다시 그리기 0.5초 이내.

### F-4. 산출 3종 체크 (D3)

변환 단계 UI 를 산출 선택으로 개편한다:

```
산출물   [v] 전체망 .kfp    [ ] 최불리 .kfp    [v] 최불리 .sdf(+.slf)
```

- **전체망 .kfp** — 기존 경로 그대로.
- **최불리 .kfp** — 기존 `remote_only=True` 경로 그대로(이미 있다). 파일명에
  `_최불리K<n>` 를 붙여 전체망본과 구분한다.
- **최불리 .sdf** — F-2 의 design 경로. **기존 `want_sdf` → `_emit_pipenet`(전체망
  문법 재직렬화) 경로는 UI 에서 제거하고 함수는 deprecated 주석과 함께 둔다** —
  설계구역 없는 SDF 는 수리계산 입력이 아니라는 것이 확정 결정이다.
- 하나도 안 고르면 「산출물을 하나도 고르지 않았습니다」로 막는다. 선택은 세션에 기억.
- 최불리 계열을 골랐는데 최불리 선정이 아직이면, 막지 말고 수리계산 패널로 안내한다.

**수용 기준**
- 세 체크의 8가지 조합 각각에서 만들어지는 파일이 정확히 그것뿐이다(자동 테스트).
- 전체망 `.kfp` 산출물이 이번 변경 전후로 비트 동일(회귀).
- `.sdf` 만 고른 흐름에서 `.kfp` 저장 대화가 한 번도 등장하지 않는다.

### F-5. 찍기 후보 제안 (D2) + 제외 사유 계측

`/api/module-f/pick/suggest` 신설. 열린 도면의 엔티티로 모듈 A 의
`detect_heads`(R1~R5)를 서버에서 호출한다 — F 는 이미 A 의 레이어 사전·장 나누기를
read-only 로 빌려 쓰는 전례가 있고, 이번에도 **A 소스는 수정하지 않는다**(import 만).

- 후보를 신뢰도와 함께 내려보내고, 찍기 화면에 후보 레이어로 깐다(신뢰도 구간별 색).
- 「전체 반영」·「이 후보만 반영/제외」 조작을 제공하되, **반영은 기존 찍기 경로
  (사람 클릭과 같은 API)로만 들어간다** — 엔진(E 계약)의 확정 규칙을 우회하는 별도
  주입 경로를 만들지 말 것. 화면 문구: 「후보일 뿐, 확정은 사용자의 반영입니다」.
- 같은 커밋에서 **제외 사유 계측**을 넣는다: 제한 전개·최불리에서 빠진 헤드를
  «찍히지 않음 / 이음 끊김(부착 실패) / 물길 미도달» 로 분류해 수리계산 패널 요약에
  개수로 보여 주고, 각 분류를 캔버스에서 켜고 끌 수 있게 한다. 지난 실측의
  「제외 2,899개」를 숫자로 쪼개는 것이 목적이다.

**수용 기준**
- 후보 제안 단계에서 board 는 불변이다(반영 전 저장본 diff 0).
- B1F 에서 후보 반영 후 물닿는 헤드 수가 반영 전보다 증가하고, 그 증가가 제외 사유
  집계의 감소와 맞아떨어진다.
- 인식이 실패해도(A import 불가 포함) 찍기는 종전대로 동작한다 — 제안은 부가 기능이다.

### F-6. 진행 스트리밍 (선택 — 앞 항목이 전부 끝난 뒤에만)

`_run_job` 에 진행 콜백을 달고 SSE 엔드포인트로 흘린다. `routes/r30_prototype.py` 의
SSE 패턴을 참조하되 세션·잡 규약은 F 것을 유지한다. 폴링(`/api/module-f/job`)은
하위호환으로 남긴다.

**수용 기준** — B1F 열기·자동 조립·변환에서 단계 진행이 실시간으로 보이고,
SSE 미지원 환경에서 폴링으로 자동 폴백한다.

### F-7. 골든 회귀 · 문서 · G 처분

- `tests/test_module_f_complete.py`: F-1 기준선으로 열기→찍기→손질→최불리→3종 산출
  전 구간 골든 비교(모듈 A 의 골든 방식).
- `docs/module_f_complete.md`: 완성품 F 의 흐름도·API 표·설정 표·G/E/A 와의 관계
  (무엇을 물고, 무엇을 빌리고, 무엇이 동결인지).
- **G 데스크톱의 처분**(유지 vs 은퇴)은 여기서 정하지 말고 `BLOCKED.md` 에 사용
  실태와 함께 올린다 — 같은 엔진을 무는 한 유지 비용은 낮다.

**수용 기준** — 골든 전 구간 통과, 문서에 F-0~F-6 의 결정·근거가 커밋 해시와 함께
남는다.

---

## 3. 금지·보존 목록

- **`cad_project_editor/`(E) 는 이 지시서로 동결이다.** 수정·삭제 금지. 참조만.
- `remote30_prototype.py` · `pipenet_converter/` · `routes/r30_*.py` 수정 금지 — F-5 의
  인식 호출은 import 뿐이다.
- G 엔진(`cad_project_editor_g/services/`, `design/`) 로직 변경은 F-1 의 `source_index`
  추가 **한 곳뿐**이다. 그 외 변경이 필요해 보이면 구현하지 말고 `BLOCKED.md` 로.
  엔진을 고치는 커밋은 G 테스트 동시 통과가 조건이다.
- 두 편집기 트리를 같은 프로세스 `sys.path` 에 올리지 말 것.
- 기존 엔드포인트명·`_run_job` 잡 규약·`_check_key` 입력 검사·세션 TTL 보존.
- 미리보기 전용 좌표 금지 — 저장에 쓰는 사본을 그대로 그린다.
- 전체망 `.kfp` 산출물은 F-0~F-7 전 구간에서 비트 동일해야 한다(F-1 의
  `source_index=None` 하위호환 포함).
- 공개 함수 시그니처 파괴 금지 — keyword 인자 추가만 허용.

---

## 4. 검증 체계

- 신설: `tests/test_module_f_design.py`(F-2·F-3·F-4), `tests/test_module_f_suggest.py`(F-5),
  `tests/test_module_f_complete.py`(F-7 골든).
- 재사용: `cad_project_editor_g/tests/` 전부 — 엔진이 하나가 되었으므로 G 테스트가
  곧 F 엔진 테스트다. CI 에서 두 묶음을 함께 돌린다.
- 회귀 원칙: **F-1 이전 산출물을 기준선으로 쓰지 않는다.** F-1 커밋이 새 기준선을
  만들고, 이후 모든 항목은 그것과 대조한다.
- 커밋 메시지 형식: `[F#] 제목 — 수용기준: PASS/FAIL(사유)`

---

## 5. BLOCKED 승계 (G → F)

G 의 `cad_project_editor_g/BLOCKED.md` 에서 넘어오는 것들. 상태를 여기 명시한다.

| 항목 | 상태 |
|---|---|
| B1 corridor 뿌리 vs 알람밸브 기기행 | **미결 승계** — F-2 에서 만나면 현행(뿌리=급수 시작 위치, 알람밸브=기기 1행) 유지하고 재기록 |
| B2 급수원 복수 | **D4 로 해소** — F-1 이 구현 |
| B3 기준개수 K 저장 위치 | **미결 승계** — F-3 에서는 세션 기억까지만. 프로젝트 저장 여부는 BLOCKED 유지 |
| B4 물닿음 판정 불일치 | **해소됨(1안)** — F-2 는 해소된 경로를 그대로 쓴다. 재발 시 재기록 |
| (신규) G 데스크톱 처분 | F-7 에서 BLOCKED 로 올린다 |

---

## 6. 진행 규칙

- 항목 순서 준수, 항목 병합 금지. F-6 은 선택 항목 — 시간이 없으면 건너뛰고 F-7 로.
- 애매점은 구현하지 말고 `BLOCKED.md` 에 (항목, 질문, 임시 우회 여부)로 기록 후 진행.
- 최종 산출물: 변경 diff 요약, 테스트 결과 전문(두 묶음), `BLOCKED.md`,
  `docs/module_f_complete.md`, 브라우저 전 구간 화면 캡처(찍기 후보 → 수리계산 패널 →
  3종 산출), PIPENET 에서 연 최불리 SDF 캡처 1장.
