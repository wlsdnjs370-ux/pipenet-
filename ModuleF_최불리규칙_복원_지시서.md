# 최불리 선정 규칙을 되돌린다 — 후보를 깎지 않는다

작성 2026-09-13 · 대상 저장소 `PycharmProjects/JupyterProject`
**`ModuleF_손질정본_지시서.md` 의 §2-1 은 틀렸다. 이 문서가 그것을 대체한다.**

---

## 0. 규칙 — 이것이 유일한 정의다

> **자동 추출 + 사람의 손질로 «정의된» 헤드 전부를 후보로 두고, 급수원에서
> 배관을 따라 잰 유하거리가 **긴 순서 그대로** 기준개수 K 개를 뽑는다.**
>
> 영역·도면 장을 지정했으면 그 안으로만 후보를 가둔다. **그 밖의 이유로
> 후보를 줄이지 않는다.**

이 규칙을 깨는 세 가지를 모두 금지한다.

| 금지 | 무슨 일이 벌어졌나 |
|---|---|
| ① 후보 깎기 | 「전개가 못 붙인다」는 이유로 헤드를 후보에서 뺐다 → **사람이 수동으로 지정한 헤드가 사라지고, 가장 먼 헤드가 안 뽑힌다** ← 지금 증상 |
| ② 백필(다음 순위로 채우기) | 뽑은 뒤 못 붙는 것을 다른 헤드로 바꿔 넣었다 → 두 화면이 다른 헤드를 그렸다 |
| ③ 되먹임 | 수리계산 결과를 손질·평면 쪽으로 되돌려 그렸다 → 사람이 정의한 것이 무엇이었는지 알 수 없게 됐다 |

셋 다 「못 붙는 헤드」를 **감추려는** 시도였다. 감추지 말고 **붙이거나, 멈추고
짚는다.** 그것이 이 문서의 전부다.

---

## 1. 지금 규칙이 깨진 자리 — 내가 시킨 것이 원인이다

`routes/module_f/api_edit.py` 의 `/edit/worst`, `_worst_k_heads` 를 부르기 직전:

    probe = wet_heads(sess, es, selected_source=...)
    keep = {int(i) for i in probe["wet"]} - {int(i) for i in probe["shared"]}
    before = (set(range(len(b.disks))) if only is None else set(only))
    only = before & keep          # ★여기가 규칙을 깬다

`worst_k_heads` 는 `only_heads` 밖의 헤드를 **순위 계산에서 아예 뺀다**:

    for hi, nodes in enumerate(hnodes):
        if only_heads is not None and hi not in only_heads:
            continue

그래서 두 증상이 **같은 원인**에서 나온다.

- **수동 지정 헤드가 인식 안 됨** — 사람이 이은 헤드라도 전개의 엄격한 자에
  안 걸리면 `keep` 에서 빠진다.
- **먼 순서 규칙을 어긴 것처럼 보임** — 가장 먼 헤드는 말단 가지관 끝에 있어
  «관이 헤드를 스쳐 지나가는»(`pass_under`) 모양이 되기 쉽다. 그 헤드들이
  통째로 후보에서 빠지니 2등·3등이 1등 자리에 온다.

**먼저 실측으로 확인한다**(고치기 전에):

    scripts/_probe_candidate_drop.py  — 같은 도면·같은 영역에서
      · 후보 좁히기 전 후보 수 · 후보 좁히기 후 후보 수
      · 빠진 헤드의 사유별 수 (pass_under / chord_only / no_center / dry / shared)
      · 빠진 헤드 중 «유하거리 상위 K 안에 들었을» 헤드 수   ← 이 수가 0 이 아니면 §1 확정

---

## 2. 조치

### 2-1. 후보 좁히기를 **철회한다**

`api_edit.py` 의 위 블록(`only = before & keep`)을 지운다. `only` 는 영역·장
제한만 담는다 — `ModuleF_손질정본_지시서.md` §2-1 이전 상태다.

    · `wet_heads` 호출 자체는 **남긴다** — §2-3 의 보고에 쓴다.
    · `keep` 으로 후보를 줄이는 줄만 없앤다.
    · `_worst_k_heads` 인자는 **한 글자도 바꾸지 않는다.** 특히 `b.hnodes` 를
      `b.head_centers` 같은 다른 것으로 갈아 끼우지 말 것 — 그것은 「붙었나」의
      자가 아니라 「어느 노드에서 물을 받나」의 목록이고, 바꾸면 유하거리가
      통째로 달라진다.

### 2-2. 뽑힌 K 개 중 «못 붙는» 것은 **붙인다** — 이미 있는 함수를 그대로 쓴다

`pass_under` = 관이 헤드 원 안을 지나가는데 그 자리에 노드가 없다.
**이 경우를 처리하는 함수가 이미 있다:**

    pipeline/flow.py  stage5_split_through_uprights(pts, edges, ups)
      · 대상: 중심(≤ARM_CTR)·테두리(±HEAD_TOUCH) 노드가 없고, 원 안(횡이탈 ≤ r)을
             관이 지나는 헤드
      · 동작: 그 관을 헤드 중심에서 쪼개 새 노드를 만든다
      · [2026-08-12 오너] 로 이미 승인된 규칙이다 — 지금은 **상향식에만** 쓰인다

하향식·상하향식에만 이 문이 닫혀 있는 것은 비대칭이다. 실제 배관은 가지관에
티로 물려 있고, 도면이 그것을 「관이 원을 지나가게」 그린 것뿐이다.

**구현 — 새 판정을 만들지 말고 그 함수를 다시 부른다.**

    `flow.attach_heads_center()` 안에서, ①(중심)·②(테두리 끝)로도 못 붙인
    헤드를 모아 `stage5_split_through_uprights(pts2, edges2, <그 헤드들>)` 를
    한 번 부르고, 그 결과로 ①을 다시 시도한다.

    · 그 함수는 `has_near_node` 로 «이미 붙은 헤드»를 스스로 건너뛴다 —
      멱등이 깨지지 않는다.
    · 상향식은 이미 5단계에서 처리돼 중심 노드가 있으므로 이 갈래에 안 온다.
    · 원 안을 지나는 관이 **둘 이상**이면 그 함수가 «횡이탈 최소 · 중앙에
      가까운 것» 하나를 고른다. 그 선택이 애매한 자리는 §2-3 으로 보낸다.

★이 변경은 `attach_heads_center` 를 쓰는 **두 곳에 함께** 적용된다
  (`edit/board.py:_complete_heads` · `convert/planar.py`). 그것이 목적이다 —
  손질이 「붙었다」고 본 것을 전개도 붙인다. **두 자가 하나가 된다.**

★**골든이 움직인다.** 물닿음 헤드 수가 늘어난다. 먼저 §4 로 재서 몇 개가
  늘어나는지 보고하고, **골든 재생성은 사람의 승인을 받은 뒤에 한다.**

### 2-3. 그래도 못 붙는 것은 **멈추고 짚는다** — 채우지도 빼지도 않는다

`dry` · `no_center` · `chord_only` · (§2-4 밖의) `shared` 는 §2-2 로도 안 붙는다.
뽑힌 K 개 안에 그런 헤드가 있으면:

    · 표를 만들지 않는다. 「이 n 개가 배관에 안 붙습니다」로 **막는다.**
    · 각 헤드의 자리(mm)와 사유, 그리고 사유별 «사람이 할 일» 을 함께 낸다.
    · **다른 헤드로 채우지 않는다.** 후보에서 빼지도 않는다.

| 사유 | 사람이 할 일 |
|---|---|
| `dry` | 상류 배관을 잇는다 (급수원에서 물이 안 닿는다) |
| `no_center` | 그 헤드로 가는 가지관을 잇는다 |
| `chord_only` | 찍기에서 그 «문양 묶음» 을 헤드에서 뺀다 |
| `shared` | 찍기에서 겹쳐 그린 묶음 하나를 뺀다 |

막는 것이 옳다 — K 미달이나 남의 헤드가 섞인 계산서보다, 멈추고 고칠 자리를
가리키는 편이 낫다. `/edit/worst` 의 기존 K 미달 게이트(`w["reachable"] < k`)와
같은 성격이다.

### 2-4. 같은 중심 노드를 나눠 문 헤드는 «한 자리» 로 센다

`shared` 중에서도 **같은 중심 노드**를 무는 쌍은 표에 노즐이 하나만 선다.
이것은 「못 붙는 것」이 아니라 「둘이 아니라 하나인 것」이다.

`worst_k_heads` 는 이미 «같은 자리» 를 하나로 접고 그만큼 다음 순위를 채운다
(`_place` · `rep_of` · 커밋 `a44ec64`). 지금은 자리를 **헤드 중심 좌표**로만
재는데, 좌표가 5~50 mm 떨어져 있어도 중심 노드를 공유하면 표는 하나다.

    `_place(hi)` 의 키에 «부착 중심 노드» 를 더한다 — 알면 그것을, 모르면
    지금처럼 좌표를 쓴다. 판정이 아니라 **세는 자** 를 맞추는 것이다.

이것은 §0 ①의 「후보 깎기」가 아니다. 후보는 그대로고, **둘로 세던 것을 하나로
세는** 것뿐이다.

---

## 3. 규칙을 시험으로 못박는다 — 매 요청마다 검사

말로 적힌 규칙은 또 깨진다. 식으로 적고 **선정할 때마다** 확인한다.

    C = 후보 (영역·장 제한을 통과하고 급수원에서 도달하는 헤드 · 자리로 접은 대표)
    S = 선정 결과 (worst_k_heads 의 heads · |S| = K)

    불변식 ①  |S| == K
    불변식 ②  min{ far(h) : h ∈ S }  ≥  max{ far(h) : h ∈ C − S }
    불변식 ③  S ⊆ C

②가 「먼 순서 그대로」의 정확한 뜻이다. 하나라도 깨지면 **응답에 실어 올린다**
(조용히 넘기지 않는다). 깨졌을 때는 양변의 값과 해당 헤드 번호를 함께 낸다.

시험도 둘 만든다.

    tests/test_worst_rank_invariant.py
      · 작은 인공 망에서 ①②③ 을 직접 검사
      · `only_heads` 를 영역으로 좁혔을 때도 ②가 «그 영역 안에서» 성립
      · 「붙는 헤드」로 후보를 좁히면 ②가 깨진다는 것을 **회귀로 못박는다**
        (지금 증상이 다시 들어오지 못하게)

---

## 4. 계측 — 조치 전/후

| 항목 | 조치 전 | 조치 후 (기대) |
|---|---|---|
| 후보 수 (영역 안) | 좁혀진 수 | **영역 안 도달 헤드 전부** |
| 후보에서 빠진 헤드 | n > 0 | **0** |
| 선정된 K 개가 유하거리 상위 K 인가 (불변식 ②) | **깨짐** | **성립** |
| 수동으로 이은 헤드가 선정 후보에 있나 | 없음 | **있음** |
| `pass_under` 헤드 수 | n | **0** (§2-2 가 붙인다) |
| §2-2 가 새로 이은 관 수 | — | n (목록으로) |
| 물닿음 헤드 수 (도면 전체) | n | n + m ← **골든 영향 · 승인 대상** |
| 표 노즐 수 | K 또는 K 미만 | **K** |
| 백필(`filled`) | n | **0** |

---

## 5. 수용 기준

| | 기준 |
|---|---|
| 1 | 후보는 영역·장 제한만 받는다 — 그 밖의 이유로 줄지 않는다 |
| 2 | 불변식 ①②③ 이 모든 선정에서 성립하고, 깨지면 응답에 뜬다 |
| 3 | 사람이 손질에서 이은 헤드가 선정 후보에 **반드시** 든다 |
| 4 | `pass_under` 헤드가 0 이 된다 (§2-2) |
| 5 | §2-2 로도 안 붙는 헤드가 K 안에 있으면 **막고** 자리·사유·할 일을 낸다 |
| 6 | 백필이 한 번도 안 일어난다 (`filled == 0`) |
| 7 | 손질 K 개 ≡ 표 노즐 K 개 (좌표 1:1) |
| 8 | 평면 보기 코드(`drawEdit`·`_worst_view`·`views.py`) **무변경** · 되먹임 없음 |
| 9 | 물닿음 증가분을 **수치로 보고**하고, 골든은 승인 전까지 재생성하지 않는다 |

**이 아홉이 서면 멈춘다. 더 고치지 않는다.**

---

## 6. 금지 사항

- **후보를 깎지 말 것** — §0 ①. 어떤 이유로도(붙음·겹침·종류·거리) 영역 제한
  밖의 필터를 후보에 걸지 않는다.
- **백필 금지** — 못 붙는 헤드를 다른 헤드로 바꿔 넣지 않는다. 기존 백필
  블록은 **비활성**으로 두되 지우지 말고, 발동하면 그 자체를 오류로 보고한다.
- **되먹임 금지** — `/design/build` 에서 `sess["worst"]` 에 쓰지 않는다.
  `drawEdit` · `_worst_view` · `views.py` · `remote30.py` 를 열지 않는다.
- **`worst_k_heads` 의 순위 규칙을 바꾸지 말 것** — 정렬 키·자리 접기 외에는
  손대지 않는다. `hnodes` 를 다른 목록으로 갈아 끼우지 않는다.
- **`head_nodes` 의 판정을 바꾸지 말 것** — §2-2 는 `attach_heads_center` 쪽만
  넓힌다(두 자를 맞추는 방향이지, 젖음 자를 헐겁게 하는 것이 아니다).
- **새 판정 함수를 만들지 말 것** — §2-2 는 `stage5_split_through_uprights` 를
  **재사용**한다. 같은 규칙이 두 벌이 되면 한쪽만 고쳐지는 날이 온다.
- 리팩터링 금지 · 새 화면 만들기 금지 · 골든 무단 재생성 금지.

---

## 7. 보고 형식

    [원인확인]  후보 좁히기로 빠졌던 헤드 __개 · 그중 상위 K 안 __개
    [후보]      영역 안 도달 헤드 __개 (= 후보 수 · 줄지 않았음을 확인)
    [불변식]    ① |S|=K __ · ② min(S) __ ≥ max(C−S) __ · ③ S⊆C __
    [붙이기]    pass_under __ → 0 · 새로 이은 관 __개 (자리 목록)
    [막음]      못 붙어 막은 헤드 __개 (사유별 · 자리)
    [일치]      손질 K __개 ↔ 표 노즐 __개 · 좌표 1:1 __/__
    [백필]      filled=__ (0이어야 한다)
    [골든]      물닿음 __ → __ (+__) · 재생성 **안 함**(승인 대기)
    [시험]      신규 __건 · 회귀 __건 통과
    [막힌 것]   못 고친 것 · 이유 · 필요한 판단

**§2-1 을 되돌린 직후 §3 불변식이 서는지 먼저 확인한다.** 그것이 서면 규칙은
복원된 것이고, §2-2 부터는 «못 붙는 헤드를 어떻게 할까» 라는 별개의 문제다 —
순서를 섞지 말 것.
