# Module G 보정 지시서 ② — SLF 경로 · 헤드 수직 표시 · 미리보기 · 산출 선택

당신은 FNCADnet 저장소의 유지보수 엔지니어다. `ModuleG_보정지시_라이브러리_아이소매트릭.md`
(G9~G13)로 관종 바인딩과 좌표 정규화는 해결됐다. 실사용에서 **네 가지 결함**이 새로
확인되었다. 아래를 **항목 순서대로, 항목당 커밋 1개씩** 수행하라. 각 항목은 수용 기준을
통과한 뒤에만 다음으로 진행한다. 애매점은 구현하지 말고 `BLOCKED.md` 에 기록하고 넘어간다(§6).

작업 대상은 **`cad_project_editor_g/`** 다. `cad_project_editor/`(모듈 E),
`routes/`, `remote30_prototype.py`, `pipenet_converter/` 는 **읽기 전용 참조**다.

---

## 0. 증상과 원인 (조사 완료 — 다시 조사하지 말 것)

### 증상 1 — `Type` 은 KSD 3507 로 채워졌는데 `Diameter` 가 전부 "Unset"

G9 의 Pipe-type 주입은 **정상 동작했다**. `bore` 값도 정상이다(`emit.py:124` 가
`dia/1000` 로 m 단위를 만든다). 문제는 **라이브러리 파일을 못 찾는 것**이다.

- **원인**: 템플릿 SDF(`assets/3-1형…알람밸브.sdf`)의 `<Libraries>` 블록이
  **템플릿 작성자 PC 의 절대경로**를 그대로 담고 있다.

  ```xml
  <Libraries>
      <User-lib file="C:\Users\admin\Desktop\3-1Çü_ÀÚ¿¬³«Â÷_…_¾Ë¶÷¹ëºê.slf"/>
  </Libraries>
  ```

  (CP949 가 깨진 문자열이며 그런 파일은 존재하지 않는다.) `sdf_post.py` 는 이 블록을
  손대지 않으므로, 산출 SDF 는 **없는 .slf 를 가리킨 채** 나간다. PIPENET 은 Pipe-type 의
  `<Name>` 은 SDF 에서 읽어 `Type` 열에 보여 주지만, 호칭경↔내경 표는 라이브러리에서
  오므로 **모든 `Diameter` 가 "Unset"** 이 된다. 우리가 옆에 저장한 `.slf` 는 아무도
  읽지 않는다.

- **모듈 A 는 어떻게 하나**: `remote30_prototype.py:7063-7082` 에서 방출 후
  ① `<Graphics>` 의 `Text-element` 를 전부 제거하고,
  ② `<Libraries>` 의 `User-lib` 를 **전부 지운 뒤 동봉한 SLF 의 «파일명만»** 으로 새로
     넣고(같은 폴더면 PIPENET 이 자동 로드한다),
  ③ `<Network-spray>` 의 여분 `Title` 과 `Network-description` 을 제거한다.
  G 의 `sdf_post.py` 에는 이 세 가지가 **하나도 없다.**

- 덤으로 정보 누출도 있다. 지금 산출 SDF 에는 템플릿 원본의
  `Officetell` · `WATER TANK_PH2F` · `3-1 type_LSP_4F` 제목과
  `PH1F 4.5M, 49F 3.1M …` 설명이 남는다. 남의 프로젝트 정보다.

### 증상 2 — 상향식 헤드가 30° 등각에 같이 굽는다

`sdf_post.bake_isometric` 은 **모든 노드**를 회전한다. 헤드 노드도 함께 돌아가
헤드 스텁이 가지배관과 같은 기울기로 눕는다. 사용자는 헤드가 **화면 수직**으로 서기를
원한다(상향식은 위로, 하향식은 아래로).

모듈 A 도 같은 문제를 알고 있고 주석에 남겨 두었다 —
「헤드 z-돌출은 여기서 적용하지 않는다(평면 Y 를 기울여 가지배관을 꼬이게 함)」
(`routes/r30_combined.py:_bake_isometric_node_coords`). A 는 SDF 에서 헤드를 돌출시키지
않는 쪽을 택했지만, G 는 **돌출시키되 수직으로** 세운다.

### 증상 3 — 저장하기 전에는 결과를 볼 수 없다

4번째 창은 숫자 요약만 준다. 아이소매트릭이 제대로 섰는지, 표 값이 맞는지 확인하려면
저장 → PIPENET 실행 → 열기를 매번 반복해야 한다.

### 증상 4 — `.kfp` 를 저장해야만 다음 창으로 넘어간다

`ui/controllers/cad_import_flow.py:213-217` 이 `_save_converted_kfp()` 를 **무조건** 부른 뒤에야
`_open_design_input()` 을 부른다. `_save_converted_kfp` 는 경로가 없으면
`QFileDialog` 를 띄우므로, SDF 만 필요한 사용자도 파일 저장 대화상자와 완료 알림을
매번 통과해야 한다. **불필요한 강제 단계다.**

### 이 보정이 건드리지 않는 것

증상 1·2 는 **표시/참조 계층**이고 증상 3·4 는 **흐름**이다. 배관망 위상과 수리계산 값
(`length` · `elevation` · `rise` · `bore` · 부속 · 노즐 유량)은 **한 개도 바뀌면 안 된다**(§5 회귀).

---

## 1. 작업 항목 (순서 고정)

### G14. 템플릿 잔재 정리와 SLF 경로 재작성 ★증상 1의 해결

`design/sdf_post.py` 에 `sanitize_template(sdf_path, slf_filename)` 을 신설하고
`emit_design_sdf` 에서 **`inject_pipe_types` 다음에** 부른다. 모듈 A
(`remote30_prototype.py:7063-7082`)와 같은 순서로 세 가지를 한다.

1. `<Libraries>` 안의 `User-lib` 를 **전부 제거**한 뒤, 실제로 옆에 저장한 SLF 의
   **파일명만**(경로 없이) 담은 `User-lib` 하나를 새로 넣는다.
   절대경로를 쓰지 말 것 — 폴더를 옮기면 그 순간 다시 "Unset" 이 된다.
2. `<Graphics>` 의 `Text-element` 를 전부 제거한다.
3. `<Network-spray>` 의 `Title` 은 **첫 개만 남기고** 나머지를 제거하고,
   `Network-description` 은 전부 제거한다.

`_write_sdf_tree` 를 그대로 써서 DOCTYPE 을 보존한다.

**수용 기준**
- 산출 SDF 의 `<Libraries>` 가 `<User-lib file="<저장한 파일명>.slf"/>` **하나뿐**이고,
  그 파일이 SDF 와 같은 폴더에 실제로 있다.
- PIPENET 에서 열면 `Diameter` 열이 호칭경(65/25/150/40 …)으로 채워지고 "Unset" 이 **0건**.
- `Type` 열은 종전대로 KSD 3507 이 유지된다(G9 회귀 없음).
- 산출 SDF 문자열에 `Officetell` · `WATER TANK` · `3-1 type` · `PH1F` 가 **0건**.
- 파일 첫 두 줄이 `<?xml version="1.0" encoding="UTF-8"?>` + `<!DOCTYPE Project SYSTEM "spray.dtd">` 그대로다.
- SDF 를 다른 폴더로 .slf 와 **함께** 옮겨도 관경이 유지된다.

### G15. 헤드는 화면 수직으로 세운다 ★증상 2의 해결

`bake_isometric` 의 시그니처를 확장한다(기존 호출부가 기본값으로 그대로 돌아가야 한다).

```python
bake_isometric(tables, *, iso_z_scale=1.0, ref_label=None, no_lift_labels=None,
               head_nodes=None, head_parent=None, head_stub_ratio=0.025)
```

동작:

1. **헤드 노드를 회전 대상에서 뺀다.** 나머지 노드는 종전 그대로
   `x' = (x-y)·cos30`, `y' = (x+y)·sin30 + lift`.
2. 헤드 노드는 **부착(부모) 노드의 등각 좌표를 그대로 물려받고**, 화면 Y 로만
   고정 길이 `stub = canvas_units × head_stub_ratio` 만큼 이동한다.
   - 방향은 `sign(head.elevation − parent.elevation)` 로 정한다. 양수면 위(상향식),
     음수면 아래(하향식), 0 이면 위로 본다. **DTO 값을 다시 읽지 말 것** — 표고 차가
     이미 그 정보를 담고 있고, 상하향식(헤드 둘)도 자동으로 갈린다.
   - 결과적으로 `head.x' == parent.x'` 가 **정확히** 성립해 스텁이 수직으로 선다.
3. `head_nodes` 는 호출부가 만들어 넘긴다 — `tables.nozzles[].in` 의 노드 라벨 집합이
   곧 헤드 노드다. `head_parent` 는 `{헤드 라벨: 부모 라벨}` 이며 헤드 노드에 붙은
   유일한 배관의 반대편 끝에서 구한다(`tables.pipes` 의 `in`/`out` 매칭).
4. 헤드에 배관이 둘 이상 붙어 있으면 수직화하지 말고 **경고를 세어 로그에 남긴다**
   (관말이 아니라는 뜻이라 위상이 의심스럽다).
5. `stub` 은 4번째 창에서 조절할 수 있게 한다(§G16 표 참조). 기본 2.5 %.

**수용 기준**
- 산출 SDF 에서 모든 헤드 노드의 `<Position x>` 가 부모 노드와 **완전히 같고**,
  `y` 차이의 절대값이 전부 `stub` 과 같다(상향은 +, 하향은 −).
- PIPENET 화면에서 헤드 심볼이 가지배관 기울기와 무관하게 수직으로 선다.
- **회귀**: 이 변경 전후로 `length` · `rise` · `elevation` · `bore` · `<Fitting>` ·
  `<Nozzle>` 이 전부 동일하다. `<Position>` 만 다르다.
- 아이소매트릭을 끄면(평면 보기) 헤드 스텁도 적용하지 않는다 — 평면에서는 헤드가
  부착점과 같은 자리에 있는 것이 옳다.

### G16. 미리보기 — 저장 전에 보이게 ★증상 3의 해결

`ui/dialogs/dialog_design_input.py` 를 좌우 분할로 다시 짠다. 창 기본 크기는
**1200 × 780**(최소 1000 × 640), 스플리터로 사용자가 비율을 바꿀 수 있게 한다.

```
┌──────────────┬───────────────────────────────┐
│ 설계 범위     │  [ 아이소매트릭 ] [ 표 ]        │
│ 보기 설정     │                               │
│ 결과 요약     │      (미리보기 · 창을 채운다)    │
│ [확정][저장]  │                               │
└──────────────┴───────────────────────────────┘
```

**왼쪽(기존 유지 + 추가)**

| 칸 | 기본값 | 비고 |
|---|---|---|
| 기준개수 K | 30 | 기존 |
| 배관 규격(전체 기본값) | KSD 3507 | 기존 |
| 아이소매트릭 보기 | 켬 | 기존 |
| 고도 펼침 배율 | 1.00 | 기존 |
| 캔버스 크기 | 3000 | 기존 |
| lift 영점 | 알람밸브 | 기존 |
| **헤드 스텁 길이** | 2.5 % | **신설** — G15 |

**오른쪽 — 탭 ①「아이소매트릭」**

- `QGraphicsView` 로 그린다. **반드시 emit 에 넘길 좌표 그대로** 그릴 것 —
  미리보기 전용 좌표계를 따로 만들면 화면과 파일이 달라져 미리보기의 의미가 없다.
  `normalize_node_coords` → `bake_isometric` 을 적용한 **사본**을 그리고, 같은 사본을
  저장에 쓴다.
- 배관은 선, 헤드는 삼각형(상향 △ / 하향 ▽ 구분), 급수원과 알람밸브는 따로 표시.
- 간선 굵기는 담당 헤드 수에 비례시킨다 — 주배관이 한눈에 보인다.
- 마우스 휠 줌, 드래그 팬, 「화면에 맞추기」 버튼.
- 왼쪽 설정을 바꾸면 **즉시 다시 그린다.** 최불리 선정 결과는 캐시하고 표시 변환만
  다시 적용해 K 를 바꾸지 않는 한 재계산하지 않는다.

**오른쪽 — 탭 ②「표」**

- 하위 탭 또는 콤보로 노드 · 배관 · 노즐 · 부속 넷을 보여 준다.
- **저장될 값 그대로** 보여 준다. 배관 표에는 관경 근거(`도면 텍스트` / `별표1`) 열을
  함께 둔다 — 지금 요약에만 있는 집계를 행 단위로 확인할 수 있어야 한다.
- 행을 고르면 왼쪽 도면에서 해당 배관이 강조된다(가능하면).

**수용 기준**
- 저장하지 않고도 아이소매트릭 형태와 표 값을 창 안에서 확인할 수 있다.
- 「고도 펼침 배율」을 바꾸면 도면이 즉시 바뀌고, 표 값은 **바뀌지 않는다**(표시 전용 증명).
- 미리보기에 그린 좌표와 저장된 SDF 의 `<Position>` 이 **일치**한다(테스트로 비교).
- B1F 실측 규모(노드 62 · 배관 61)에서 설정 변경 후 다시 그리기가 0.5 초 안에 끝난다.
- 창을 최대화해도 레이아웃이 깨지지 않는다.

### G17. 산출 대상 선택 — `.kfp` 강제 제거 ★증상 4의 해결

변환 창(`ui/dialogs/dialog_kfp_convert.py`)에 **산출 대상** 선택을 넣는다.

```
산출물   [v] .kfp 저장     [v] 수리계산 입력(.sdf) 만들기
```

`ui/controllers/cad_import_flow.py:211-220` 의 분기를 이렇게 바꾼다.

- `.kfp` 를 체크했을 때만 `_save_converted_kfp()` 를 부른다. 체크 안 했으면
  **파일 대화상자도 완료 알림도 띄우지 않는다.**
- `.sdf` 를 체크했을 때만 `_open_design_input()` 을 부른다.
- 둘 다 체크면 종전 순서(kfp 저장 → 수리계산 입력 창)를 유지한다. 이 순서는 그대로 둔다.
- 둘 다 해제면 변환만 하고 흐름을 끝낸다 — 경고 없이 조용히 끝내지 말고
  「산출물을 하나도 고르지 않았습니다」로 막는다.
- 선택은 세션에 기억해 다음 실행에서 같은 값으로 뜬다.
- `.kfp` 저장 성공 알림(`QMessageBox.information`)은 **실제로 저장했을 때만** 띄운다.

**수용 기준**
- `.sdf` 만 체크하면 변환 완료 즉시 수리계산 입력 창이 뜬다. 파일 저장 대화상자가
  **한 번도** 뜨지 않는다.
- `.kfp` 만 체크하면 종전과 동일하게 저장하고 끝난다. 수리계산 입력 창은 뜨지 않는다.
- 둘 다 체크한 경우의 동작이 이번 변경 전과 **완전히 동일**하다(회귀 없음).
- 변환 창을 취소하면 종전대로 손질 창으로 돌아간다.

### G18. 대조 검증

같은 도면(B1F)으로 처음부터 끝까지 한 번 돌리고 `docs/module_g_vs_a.md` 를 갱신한다.

**수용 기준**
- PIPENET 에서 연 화면 캡처 2장: `Type`·`Diameter` 열이 모두 채워진 표, 헤드가 수직으로
  선 아이소매트릭 도면.
- 미리보기 화면과 PIPENET 화면의 형태가 같다.
- 노드·배관·노즐 개수, `length` 합계, 앵커·최원 유하거리·max_load 가 G13 결과와 동일하다.

---

## 2. 지금 산출물에서 확인된 수치 (참고 · 회귀 기준선)

이번 실행의 요약이다. 위상 관련 값은 이 보정 뒤에도 **같아야 한다**.

- 설계면적 헤드 30개 · 앵커(최원 유하거리) 171.87 m · 설계면적 폭 54.25 m
- corridor 총연장 207.31 m · 주배관 담당 헤드 수 30
- 표: 노드 62 · 배관 61 · 노즐 30 · 기기 0 · 루프 잔여 0
- 관경 근거: 도면 텍스트 27 · 별표1 보강 0 · 별표1 폴백 34
- 부속: 20건 · 판정 불가 3 · 등가길이 미해결 0

**따로 볼 것** — 「전개가 배관에 붙지 못한 헤드 2,899개를 후보에서 제외」는 이번 보정의
대상이 아니지만 수치가 크다. 도면 3,163개 중 264개만 후보로 남았다는 뜻이다. 원인
분류(찍기 누락 / 이음 끊김 / 물길 미도달)를 §6 의 `BLOCKED.md` 에 한 줄로 남겨 두라.
다음 지시서에서 다룬다.

---

## 3. 금지·보존 목록

- `pipenet_converter/` · `remote30_prototype.py` · `routes/` · `cad_project_editor/` 수정 금지.
  템플릿 정리는 방출 **후처리**로만 한다.
- 템플릿 SDF 자산 파일 자체를 편집하지 말 것. 잔재 정리는 산출물에서만 한다.
- SLF 자산을 편집하지 말 것.
- 좌표·표시 이외의 값은 한 개도 바뀌면 안 된다(§5).
- 공개 함수 시그니처 파괴 금지 — keyword 인자 추가만 허용. 기본값으로 부르면 종전 동작.
- 미리보기용으로 좌표를 따로 계산하지 말 것 — 저장에 쓰는 사본을 그대로 그린다.

---

## 4. 검증 체계

`cad_project_editor_g/tests/test_sdf_post.py` 확장 · `tests/test_design_dialog.py` 신설.

- G14: 합성 SDF(User-lib 2개 · Text-element 3개 · Title 3개) → 정리 후 User-lib 1개,
  Text-element 0, Title 1, DOCTYPE 보존.
- G15: 합성 망(상향식 1 · 하향식 1) → 헤드 x 가 부모와 동일, y 차이 부호가 표고 차 부호와 같음.
- G16: 미리보기가 그린 좌표 == 저장된 SDF 의 Position(부동소수 오차 1e-6 이내).
- G17: 세 조합(kfp만 / sdf만 / 둘다) 각각에서 호출되는 함수가 정확히 그것뿐임을 확인.
- **회귀(가장 중요)**: B1F 실측으로 종전 산출과 새 산출을 비교해 `length` · `rise` ·
  `elevation` · `bore` · `<Fitting>` · `<Nozzle>` 이 전부 동일하고, `<Position>` ·
  `<Libraries>` · `<Graphics>` · `<Title>` 만 달라진 것을 diff 로 보인다.
- 커밋 메시지 형식: `[G#] 제목 — 수용기준: PASS/FAIL(사유)`

---

## 5. 진행 규칙 · 미정 사항

- 항목 순서 준수, 항목 병합 금지.
- 아래는 정하지 말고 만나는 시점에 `BLOCKED.md` 에 올릴 것.
  - 배관별(구간별) 관종 지정 방법 — 아직 전체 기본값 하나뿐이다.
  - 헤드 2,899개 제외의 원인 분류(§2 참고).
  - 보기 설정을 프로젝트에 저장할지, 매번 입력할지.
- 최종 산출물: 변경 diff 요약, 테스트 결과 전문, `BLOCKED.md`,
  `docs/module_g_vs_a.md` 갱신, PIPENET 화면 캡처 2장(§G18).
