# Module A 작업 지시서 — 유량 누적(load) 기반 배관망 확정

당신은 FNCADnet 저장소의 유지보수 엔지니어다. 아래를 **항목 순서대로, 항목당 커밋 1개씩**
수행하라. 각 항목은 수용 기준을 통과한 뒤에만 다음으로 진행한다. 판단이 필요한 애매점은
임의로 구현하지 말고 `BLOCKED.md`에 기록하고 다음 항목으로 넘어간다(§6).

---

## 0. 이 알고리즘의 정체 (구현 전 필독)

도입하려는 것은 새 발명이 아니라 **여러 분야에서 이름이 확립된 표준 기법**이다.
정확한 이름을 알아야 올바른 자료구조와 복잡도로 구현할 수 있다.

- **유수 누적(flow accumulation)** — 수문학/GIS에서 지형도로 하천망을 뽑는 표준 절차.
  ① 흐름 방향 결정 ② 상류 셀 개수 누적 ③ 임계치로 절단. D8 알고리즘 계열.
- **서브트리 단말 수(subtree terminal count)** — 루트 있는 트리에서 각 간선 아래
  서브트리에 속한 단말 개수. **후위순회(post-order DFS) 한 번에 O(V+E)**로 전부 구해진다.
- **간선 매개중심성의 제한판** — 모든 정점 쌍이 아니라 "단일 소스 대 단말 집합"으로 한정한 것.
- **죽은 코드 제거(dead code elimination) / mark-and-sweep** — 출력(헤드)에서 거꾸로
  생존성을 전파하고 표시 안 된 것을 쓸어내는 구조. 불리언 대신 카운터를 쓴 판.

우리 도메인에서 이 누적값의 정체는 **담당 헤드 수**다. 즉 NFPC 103 별표1 '가'칸이
최소 호칭경을 정할 때 쓰는 바로 그 값이다. **이것이 이번 작업의 핵심이며, §2 L3의
근거다: 같은 값을 추출 판정과 관경 결정 양쪽에 쓴다.**

### 현행 방식과의 차이

현재 `force_spanning_tree(graph, source)`는 최단경로 트리를 강제하며 트리에 속하지 않는
edge를 `graph`와 `edge_len`에서 **파괴적으로 in-place 제거**한다. 문제는 두 가지다.

1. **절단 기준이 위상뿐이다.** 사이클을 없애려고 자를 때 그 관이 실제로 물을 나르는지
   보지 않는다. 격자식(루프) 배관에서는 진짜 배관을 잘라낸다.
2. **죽은 관을 못 지운다.** 배수관·시험배관·막다른 가지는 소스에서 도달 가능하므로
   SPT에 남는다. "도달 가능"은 "물이 흐름"보다 넓다.

유량 누적은 두 문제를 동시에 푼다. 부하 0인 간선만 자르므로 격자배관은 네 변 모두
부하 ≥ 1이라 보존되고, 죽은 관은 부하 0이라 제거된다.

### 적용 한계 (반드시 문서화할 것)

- 부하는 **단일 소스 + 단일 최단경로 트리** 위에서 정의된다. 다중 급수원, 완전 격자식,
  환상 배관에서는 정의 자체를 다시 세워야 한다. 이번 범위 밖 — 조건 불충족 시 기존
  경로로 폴백하고 그 사실을 audit에 남긴다.
- 헤드 집합이 먼저 확정되어야 한다. 헤드 검출(S240) 실패는 부하 전체를 0으로 만든다.

---

## 1. 신규 계약

- `load_mode=True` 모드 신설. 요구 입력: 소스 절점(알람밸브, 기존 anchored 경로와 동일),
  확정된 헤드 집합. 둘 중 하나라도 없으면 진입 불가 — 명시적 에러.
- **`load_mode` 미사용 시 기존 산출물은 비트 동일해야 한다(골든 불변).** 신규 로직은
  전부 이 분기 안에서만 발동한다.
- 이 작업은 **자료구조를 바꾸지 않는다.** `graph: dict[tuple, set[tuple]]`,
  `edge_len: dict`, `_NodeIndex.canonical` 전부 그대로 쓴다. 유량 누적은 좌표 키
  그래프에서 그대로 성립한다.

---

## 2. 작업 항목 (순서 고정)

### L1. 부하 산출 함수 신설

`compute_edge_load(graph, edge_len, source, heads) -> dict[edge, int]`

1. 소스에서 Dijkstra로 `dist`, `prev`를 구한다. (S310이 이미 같은 계산을 하고 있다 —
   중복 계산하지 말고 기존 결과를 재사용하거나 공용 헬퍼로 뽑아라.)
2. `prev`로 정의되는 최단경로 트리에 대해 **후위순회 1회**로 각 간선의 서브트리 단말
   수를 구한다. 헤드가 부착된 절점은 자기 몫 +1을 갖는다.
   - **헤드마다 소스까지 거슬러 올라가는 방식(O(단말 수 × 깊이))으로 구현하지 말 것.**
     후위순회는 O(V+E)다. 대형 도면에서 갈린다.
3. 소스에서 도달 불가인 헤드는 `unreachable_heads`로 별도 수집한다. 조용히 버리지 마라 —
   추출 결함 신호다.
4. 반환은 트리 간선에 대한 부하. 비트리 간선(사이클을 닫는 간선)의 부하는 0으로 둔다.

**수용 기준**
- 합성 단위 테스트: 소스 → 분기 → 헤드 3개 + 헤드 없는 막다른 가지 1개인 인공 트리에서
  주관 부하 3, 중간 2, 말단 1, 막다른 가지 0.
- 헤드 수 N인 임의 트리에서 소스 인접 간선들의 부하 합 = N.

### L2. 부하 기반 가지치기

`prune_by_load(graph, edge_len, load, audit) -> pruned_edges`

1. 부하 0인 간선을 전부 제거한다. 제거 전 `audit`에 (양단 좌표, 길이, 소속 레이어)를 기록.
2. 남은 사이클을 처리한다. 사이클을 이루는 간선 중 **부하 최소값이 0일 때만** 그 간선을
   자른다. **최소값이 1 이상이면 자르지 않는다** — 실제 격자배관이므로 보존한다.
3. 2의 결과로 사이클이 남을 수 있다(모든 변이 부하 ≥ 1인 경우). 이때의 동작은
   `on_residual_cycle` 인자로 제어한다: `"preserve"`(기본) 또는 `"force_tree"`(기존
   `force_spanning_tree` 호출). 어느 쪽이든 audit에 남은 사이클 수를 기록.

**수용 기준**
- 합성 테스트 A: 네 변 부하가 (2,1,2,1)인 사각 루프 → 절단 0건, 사이클 유지.
- 합성 테스트 B: 네 변 부하가 (3,2,1,0)인 사각 루프 → 부하 0인 변 1개만 절단.
- 대명동 도면 실행 시 부착 헤드가 1개도 손실되지 않을 것.

### L3. 담당 헤드 수 단일화 ★이번 작업의 핵심

현재 **같은 값이 두 곳에서 따로 계산된다.**
- 추출 쪽: L1의 부하
- S430 쪽: NFPC 103 별표1 최소 호칭경 결정을 위한 BFS 트리 하류 헤드 수

1. L1의 `compute_edge_load` 결과를 S430의 담당 헤드 수 입력으로 **그대로 전달**한다.
   S430 내부의 별도 BFS 산출 경로는 제거하고 전달받은 값을 쓰도록 바꾼다.
2. 두 값이 달라지는 조건이 있으면(예: S430이 가지식 정규화 후 그래프를 쓰고 L1은 정규화
   전 그래프를 쓴다면) 그 차이를 `BLOCKED.md`에 적고 **통합하지 말 것.** 잘못된 통합은
   법정 최소 호칭경을 틀리게 만든다. 통합 가능성이 확인될 때만 통합한다.
3. 통합에 성공하면 코드 주석에 근거를 남긴다:
   `# 담당 헤드 수 = 유량 누적값. 추출 소속 판정과 별표1 최소 호칭경이 같은 양을 쓴다.`

**수용 기준**
- 대명동 도면에서 통합 전후 S430 산출 호칭경이 전 구간 동일(회귀), 또는 차이가 있으면
  각 구간의 차이 원인이 `BLOCKED.md`에 설명되어 있을 것.

### L4. 파이프라인 편입

1. `select_worst30_heads_anchored` 경로에서 호출 순서를 다음으로 한다:
   `_restrict_to_branch_region`(S315) → **`compute_edge_load` → `prune_by_load`** →
   거리 산출(S310) → 상위 K 선정(S320).
2. **`force_spanning_tree`는 `on_residual_cycle="force_tree"`일 때만 호출**한다.
   기본 경로에서 무조건 호출하던 것을 조건부로 바꾼다.
3. `force_spanning_tree`가 파괴적이라는 점에 유의 — `prune_by_load` 뒤에 호출되면
   이미 정리된 그래프를 다시 자른다. 순서를 지켜라.

**수용 기준**
- `load_mode=False` 전체 실행의 주요 산출(JSON/SDF/KFP) diff 없음.
- `load_mode=True`에서 최종 그래프의 모든 간선이 부하 ≥ 1.

### L5. Audit 확장

기존 W7 audit 객체에 다음을 추가한다.
`load {min, max, median}`, `pruned {dead_edge_count, dead_len_mm, cycle_cut_count}`,
`residual_cycles {count, policy}`, `unreachable_heads [xy]`,
`fallback {triggered, reason}`.

응답 JSON에 직렬화한다. 프론트 렌더링은 범위 밖.

**수용 기준**
- 대명동 실행 시 audit JSON이 스키마대로 채워지고, L2의 헤드 무손실 검증이 이 audit로 수행됨.

### L6. 적용 한계 문서화

`docs/load_extraction.md` 신설. 내용:
- 알고리즘의 표준 명칭(§0)과 O(V+E) 근거
- 전제 조건: 단일 소스, 헤드 집합 확정, 가지식 우세
- 성립하지 않는 경우: 다중 급수원, 완전 격자식, 환상 배관 — 각각 어떻게 폴백하는지
- 특허 정합: 현행 명세서가 "가지식 트리 강제(Dijkstra SPT)"를 청구 메커니즘으로 기재하고
  있으나, 본 구현은 부하 기준 절단이며 격자배관을 보존한다. **명세서가 아직 미제출이므로
  이 차이를 반영할 여지가 있다.** 코드에는 `# [문서정합]` 주석만 남기고 명세서 수정은
  범위 밖 — 최종 보고서에 목록화한다.

---

## 3. 금지·보존 목록

- 자료구조 변경 금지: `graph: dict[tuple, set[tuple]]`, `edge_len`, `_NodeIndex.canonical`,
  `SNAP_TOL_MM`. 유량 누적은 좌표 키에서 그대로 동작한다.
- 로직 변경 금지: 헤드 검출 R1~R5, `collapse_parallel_ladders`,
  `_weld_dangling_endpoints`, `_restrict_to_branch_region` 내부, S320 상위 K 선정 규칙,
  `size_pipes_by_velocity`의 유속 상한과 단조성 강제.
- 전면 리포맷, 파일 이동/리네이밍, 공개 함수 시그니처 파괴 금지 — keyword 인자 추가만 허용.
- `load_mode` 미사용 경로 산출물은 비트 동일해야 한다. 회귀 스크립트로 증명할 것.

---

## 4. 검증 체계

`tests/test_load_extraction.py` 신설.
- L1·L2의 합성 단위 테스트(위 수용 기준 그대로).
- 대명동 도면 통합 테스트: 부착 헤드 무손실, 최종 전 간선 부하 ≥ 1,
  audit 스키마 충족.
- 회귀: `load_mode=False` 전체 실행 diff 없음.
- 커밋 메시지 형식: `[L#] 제목 — 수용기준: PASS/FAIL(사유)`

## 5. 참고 — 참조 구현

외부 PoC(`_tmp_dxf_extract_poc3.py`)의 `water_cleanup` 함수가 같은 알고리즘의
동작하는 구현이다. **코드를 옮겨오지 말고 동작만 참고하라.** 그 구현은
헤드마다 소스까지 거슬러 올라가는 O(단말 수 × 깊이) 방식이며, 본 지시서는
후위순회 O(V+E)를 요구한다. 또한 그 구현은 인덱스 키 그래프를 전제하므로
자료구조 가정이 다르다.

## 6. 진행 규칙

- 항목 순서 준수, 항목 병합 금지.
- 애매점은 구현하지 말고 `BLOCKED.md`에 (항목, 질문, 임시 우회 여부)로 기록 후 진행.
- 최종 산출물: 변경 diff 요약, 테스트 결과 전문, `BLOCKED.md`,
  `docs/load_extraction.md`, `# [문서정합]` 목록.
