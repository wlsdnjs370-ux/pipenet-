# 아이소·상하향 지시서 — 평면 그래프는 그대로, 헤드 종류별 나무를 «덧붙인다»

작성 2026-09-13 · 대상 저장소 `PycharmProjects/JupyterProject` · 그림 `docs/images/iso_plan_preserve/그림1~4.png`

---

## 0. 요구 — 한 줄

**손질 정본 G(자동 + 수동으로 정의된 평면 배관망)는 노드·간선·XY 하나도 바뀌지 않은 채 수리계산으로 가고, 상향식·하향식·상하향식의 세로 배관은 G 위에 «덧붙는» 종류별 나무 T_h 다. 아이소는 그 결과를 등각으로 돌려 그린 그림일 뿐, G 를 고치는 자리가 아니다.**

지금은 아이소가 **G 를 훼손한 채** 그려진다는 것이 오너의 관찰이다. 어디서 훼손되는지는 **먼저 재고**(§2), 잰 것만 고친다(§3). 소스를 읽고 짐작한 자리를 고치지 않는다 — 그 방식이 이번 주에 세 번 틀렸다.

---

## 1. 모델 — 무엇이 «그대로» 이고 무엇이 «덧붙는» 것인가

### 1-1. G 와 G_K

| 기호 | 뜻 | 소스 |
|---|---|---|
| G | 손질 정본. `EditBoard` 의 `pts`(mm) · `edges` · `disks`(헤드 점 = 기호 원의 중심) · `disk_kinds` · `sources` · `valves` | `edit/board.py` |
| G_K | G 중 최불리 K개 헤드로 가는 회랑(corridor) 부분그래프 — 수리계산에 실리는 평면 부분 | `restrict_to_worst` |
| G′ | 세로 처리 뒤의 3차원 망 (노드 x, y, z · 배관) — 설계 표의 재료 | `apply_vertical` → `kfp` |
| π | 사영 π(x, y, z) = (x, y) | — |
| T_h | 헤드 h 에 덧붙는 종류별 나무 (아래 1-2) | `convert/engine.py` |

### 1-2. 종류별 나무 T_h — 「수직 전개 값」 창의 로직 **그대로** (그림 2′)

오너가 2026-09-13 확정: **읽기 A** (그림 4). 하향식 도면 기호는 「원 + 위의 점 + 양옆 십자」이며 이 방향으로 간다.

| 종류 | 토막 | 길이 | 재질·구경 | 어디에 붙나 |
|---|---|---|---|---|
| 상향식 | ① 위로 → 위 헤드 U | 입력값 (기본 0.3 m) | H 에 붙어 있던 배관 복사 | **H** |
| 하향식 | ① 위로 | 입력값 (0.3) | 배관 복사 | **T = 팔이 갈라지는 티** |
| | 팔 | 도면값 (≤0.15 → 0.15 m) | 평면의 그 팔을 ① 만큼 들어 올린 것 | 평면의 팔 T–H 그대로 |
| | ② 아래로 → 헤드 | 입력값 (0.3) | **후렉시블** C·거칠기 (체크 시) | 끝 = **H** |
| 상하향식 | ① 위로 → ② 위 헤드 U | 0.2 · 0.3 | 배관 복사 | **H** |
| | ③ 옆으로 → ④ 아래 헤드 D | 0.3 · 0.5 | ③ 배관 복사 · ④ 후렉시블 (체크 시) | D 는 H 에서 ③ 만큼 옆 (가지관 방향) |

- 길이는 전부 **입력값** 이고 좌표로 재지 않는다 (`make_vert_pipe`). 팔만 도면값.
- 팔이 없는 하향식 헤드(가지 끝·관이 스쳐 지나가는 헤드)는 ① 없이 H 에서 ② 만 내린다 (`_pendant_arm_to_tee` 가 None 을 주는 경우). 이것은 현 규칙이다 — 바꾸지 않는다.
- 헤드 종류는 **손질의 `disk_kinds`(찍기 분류 + 사람의 `kind_overrides`)** 에서 온다. 수리계산이 다시 판정하지 않는다.
- 위 표는 `engine.py` 에 **이미 있다**. 이 작업은 규칙을 만드는 것이 아니라 **이 규칙이 훼손되지 않은 G_K 위에서 돌게 하는 것**이다.

### 1-3. 판정식 — «보존» 과 «훼손»

    G′ = G_K ∪ ⋃_h T_h        G_K 의 노드·간선·XY 는 그대로, T_h 만 덧붙는다

이것이 성립하려면 다음이 **전부** 성립한다. 하나라도 깨지면 훼손이다.

| | 판정 | 재는 법 |
|---|---|---|
| P1 | G_K 의 모든 노드 v 에 π(v′) = p(v) 인 G′ 노드가 있다 | `node_ref` 로 되짚어 XY 거리. 최대·분포 보고 |
| P2 | G_K 의 모든 간선 (u, v) 가 π(G′) 에서 u′–v′ 를 잇는 **같은 XY 의** 사슬로 남아 있다 (세분·병합만 허용 — 위상수학 그림 4 의 ≅) | `edge_ref` 역참조 + 사슬 XY 대조 |
| P3 | π(G′) 에 G_K 에도 T_h 에도 속하지 않는 노드·간선이 **없다** | G′ 노드를 셋으로 분류: G_K 대응 · T_h · 미분류. 미분류 = 훼손 (c) |
| H1 | 헤드 h 의 T_h 가 1-2 의 자리에 붙어 있다 — 상향식·상하향식은 H, 하향식은 T (팔 없으면 H) | T_h 뿌리 노드의 π 와 p(H)/p(T) 거리 |
| H2 | 헤드(노즐) 노드의 π = p(H) (상하향식 D 만 p(H) + ③·u) | 노즐 `in` 노드 XY |
| H3 | T_h 토막 길이 = 입력값 · 재질 = 템플릿 복사 · 후렉시블은 하향 ②·상하향 ④ 에만 | 표의 `length_m` · `C` · `roughness_mm` |
| I1 | 아이소 좌표 = 같은 식 x′ = (x−y)cos30 · y′ = (x+y)sin30 + (z−z_ref)·lift, 헤드는 부모 x′ 위에 세운다 | `bake_isometric` 반환값으로 재계산해 대조 |

훼손의 세 얼굴 (그림 3 오른쪽): **(a)** 뿌리가 H/T 가 아닌 다른 노드에 선다 → H1 위반 · **(b)** 헤드 점이나 노드의 XY 가 옮겨졌다 → P1/H2 위반 · **(c)** 평면에 없던 노드·간선이 생겼다 → P3 위반. 여기에 **(d)** G_K 의 노드·간선이 사라졌다 → P1/P2 위반.

---

## 2. 먼저 잰다 — `scripts/_probe_plan_preserve.py`

### 2-1. 무엇을 도는가

`/design/build` 가 부르는 **바로 그 함수를 같은 순서로** 부른다. 새 경로를 만들지 않는다 (import 해서 호출만).

    open_board(key) + load_edits(board)            # 손질 정본 G (유저손질 JSON 포함)
    worst_k_heads(...)                              # 최불리 K개 — 인자로 K·영역·급수원을 받는다
    select_and_expand(payload, board, k=K, only_heads=…, selected_source=…)
        → restrict_to_worst → build_planar_graph → apply_vertical(convert_to_kfp)
    build_design_tables(kfp, worst, edge_ref, …)
    bake_isometric(view, …)                         # I1 용

인자: `--key` · `--k` · `--zone x0 y0 x1 y1`(선택, 여러 번) · `--source Z1`(선택) · `--dto path.json`(수직 전개 값, 없으면 `default_dto()`).

### 2-2. 무엇을 내는가 — 표 한 장 + 목록

    [P1] G_K 노드 __개 · 대응 __개 · 미대응 __개 · XY 오차 최대 __mm · >5mm __개 · >50mm __개 · >150mm __개
    [P2] G_K 간선 __개 · 사슬 유지 __개 · 끊김 __개 · XY 이탈 __개
    [P3] G′ 노드 __개 = G_K 대응 __ + T_h __ + 미분류 __      (미분류 목록: 노드·XY·붙은 배관)
    [H1] 헤드 K개 · 뿌리 자리 맞음 __ · 틀림 __ (목록: 헤드 번호·종류·뿌리 π·p(H)·거리)
    [H2] 노즐 XY = p(H) __/K · 틀림 목록
    [H3] 토막 길이 = 입력값 __/__ · 재질 복사 __/__ · 후렉시블 자리 맞음 __/__
    [I1] 아이소 재계산 일치 __/__ (최대 오차 __)
    [훼손 분류] (a) __ · (b) __ · (c) __ · (d) __  — 각 항목의 **첫 원인 함수** 를 스택으로 짚는다

「첫 원인 함수」는 짐작하지 않는다. 단계마다(planar 직후 · restrict 직후 · apply_vertical 직후 · tables 직후) 같은 판정을 돌려 **어느 단계에서 처음 깨지는지** 를 표로 낸다.

### 2-3. 어느 도면으로

- **대명동** (111개 전부 하향식 · 읽기 A 의 ①·팔·② 가 전부 나오는 도면) — K=30, 영역 없이 · 영역 있이 각 1회
- **B1F** (오너가 두 화면 사진을 찍은 도면) — 사진과 같은 K·영역으로
- 상하향식이 있는 도면이 있으면 1회 (없으면 §4-2 의 합성 시험으로 갈음하고 그렇게 보고)

**§2 결과를 오너에게 먼저 보고한다. §3 은 그 뒤다.** 재 보니 훼손이 0 이면 — 그렇게 보고하고 멈춘다(오너가 본 것은 다른 층위일 수 있다).

---

## 3. 고친다 — 잰 것만, 한 원인에 한 커밋

- §2 의 「첫 원인 함수」 마다 **그 함수 안에서** 최소로 고친다. 원인 하나 → 커밋 하나 → 프로브 재실행 → 그 항목 0 확인.
- 고칠 수 있는 자리: `convert/planar.py` (G_K 가 여기서 옮겨지거나 지워지면), `design/restrict.py`, `convert/engine.py` 의 **붙는 자리 결정**(`attach_head_to_line` · `_pendant_arm_to_tee` · `_takeoff_dir` — ①②③④ 의 값·모양은 아님), `design/tables.py` · `design/emit.py` 의 헤드↔부모 대응, `sdf_post.bake_isometric` (I1 이 깨질 때만).
- 고친 뒤에도 남는 것이 있으면 **감추지 않는다**: 화면과 응답에 「평면 노드 n개가 사영에서 빠졌습니다 / 옮겨졌습니다」 를 자리(XY)와 함께 싣는다. 다른 헤드로 채우거나 노드를 지워 맞추지 않는다.

---

## 4. 검증

### 4-1. 수용 기준

| | 기준 |
|---|---|
| 1 | 대명동 · B1F 프로브: P1 미대응 0 · XY 오차 >50 mm 0 (5~50 mm 는 개수 보고 — GRID_M 스냅은 오너 결정, §6) |
| 2 | P2 끊김 0 · P3 미분류 0 |
| 3 | H1 뿌리 자리 K/K · H2 노즐 XY K/K · H3 길이·재질·후렉시블 전부 일치 |
| 4 | I1 아이소 재계산 일치 100 % |
| 5 | 화면: 「평면도로 보기」 켬/끔에서 헤드 K개가 **같은 XY** 에 있고, 밑그림을 깔면 G_K 가 원도면 위에 겹친다 (이미 그런 항목은 그대로) |
| 6 | 손질 화면 · 최불리 선정 결과 · 전체망 `.kfp` 산출 — **바이트 불변** |
| 7 | 기존 회귀 전부 통과 · 골든 재생성 없음 |

### 4-2. 시험 `tests/test_plan_preserve.py`

- 합성 망 하나 (그림 1 과 같은 꼴: 교차배관 + 가지관 2 + 헤드 4 = 상향식 2 · 하향식 1(팔 있음) · 상하향식 1) 로 P1·P2·P3·H1·H2·H3 을 **식으로** 검사. 하향식은 T 에 ①, H 에 ②, 팔이 ① 만큼 올라감을 확인. 팔 없는 하향식 1개를 더 두어 「① 없이 ② 만」 을 확인.
- 프로브를 시험에서 함수로 부른다 (스크립트 = 시험의 얇은 껍데기).
- 실도면 수치(대명동 K=30)는 회귀 값으로 고정하되, 골든이 아니라 **판정식 0 건** 으로 고정한다.

---

## 5. 금지

- ①②③④ 의 값·모양·순서(`dto.py` · `engine.py` 의 규칙)를 바꾸지 말 것 — 붙는 **자리** 만 대상이다.
- 손질(`api_edit.py` · `edit/board.py`) · 최불리 선정(`worst.py` · `/edit/worst`)을 건드리지 말 것.
- 수리계산 결과를 손질·세션 선정(`sess["worst"]`)에 되먹이지 말 것 (절대).
- 헤드 종류를 수리계산에서 다시 판정하지 말 것 — `disk_kinds` 가 정본.
- 못 붙는 헤드를 다른 헤드로 채우지 말 것. 안 맞는 노드를 지워서 맞추지 말 것.
- `GRID_M` · `attach_heads_center` 의 허용치 · `SNAP` 을 바꾸지 말 것 (§6 로 올린다).
- 리팩터링 금지 · 골든 재생성 금지 · 새 경로(두 번째 전개 함수) 신설 금지.

## 6. 애매하면 멈추고 묻는다 — 오너 결정이 필요한 자리

프로브가 아래 중 하나를 원인으로 짚으면 **고치지 말고 수치와 함께 묻는다.**

1. `planar.py` 의 GRID_M=0.05 스냅으로 노드가 옮겨진다 (≤35 mm) — 이것을 훼손으로 볼지
2. 헤드가 원의 중심이 아니라 **테두리 끝 노드** 에 붙어 헤드 점이 반지름만큼 벗어난다 (`attach_heads_center` 의 rim 규칙)
3. 가지 끝 하향식 헤드에 ① 이 없다 (팔 없음 규칙) — 그대로 둘지
4. 상하향식 ③ 의 방향 — 코드는 가지관 방향, 창의 그림은 가지관과 직각
5. `PipeEditor.cleanup_collinear_intermediate_nodes` 가 지운 차수 2 노드 중 **꺾인 점**이 있다 (직선 병합이 아니면 기하가 바뀐다)

그 밖에도 「어느 쪽인지 정해야 진행되는 것」이 나오면 같은 방식으로 묻는다. 두 갈래를 다 구현해 두고 고르게 하지 않는다.

## 7. 보고 형식

    [프로브]  §2-2 표 — 도면별 (대명동 K=30 무영역 / 유영역 · B1F 사진 조건) · 단계별 첫 깨짐 표
    [원인]    첫 원인 함수 목록 — 각각 훼손 (a)(b)(c)(d) 중 무엇
    [고친 것] 원인 → 커밋 → 프로브 전/후 수치
    [남은 것] 못 고친 항목과 화면에 실은 문구
    [시험]    신규 __건 · 회귀 __건 통과
    [묻는 것] §6 해당 항목과 수치

**§2 보고 → 오너 답 → §3.** 순서를 건너뛰지 않는다.
